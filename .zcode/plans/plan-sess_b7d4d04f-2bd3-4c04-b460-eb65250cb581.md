## Plan: Implement three LSP improvements as separate PRs

The scout already traced all exact insertion points. Here's the execution plan:

### PR 1: LSP Debounce (#1908) — simplest, highest UX impact

**What:** Add a 200ms debounce so rapid saves collapse into one analysis.

**Implementation:**
- Add `const REFRESH_DEBOUNCE: Duration = Duration::from_millis(200);` in `backend.rs`
- In `refresh_diagnostics` (`backend.rs:160-166`), before `self.refresh_scheduler.request(...)`:
  - Skip debounce for `ExplicitRefresh` / `ConfigReload`
  - For `DidOpen`/`DidSave`/`DidClose`: `tokio::select!` between `sleep(REFRESH_DEBOUNCE)` and cancellation
  - After waking, re-check authority/config
- Files: `crates/ripr/src/lsp/backend.rs` only

### PR 2: Diagnostic Budget Enforcement on Push (#1911)

**What:** Apply budget selector to push publication loop so large diffs don't overwhelm the client.

**Implementation:**
- Lift `diagnostic_canonical_id` from private to `pub(crate)` in `diagnostic_budget.rs:336`
- In push loop (`backend.rs:390-414`): build items, evaluate budget, filter `batch.diagnostics` to selected IDs
- Add `last_push_omitted` to `analysis_status_payload_for_health` (`backend.rs:954-997`)
- Files: `crates/ripr/src/lsp/backend.rs`, `crates/ripr/src/lsp/diagnostic_budget.rs`

### PR 3: LSP Progress Signal (#1909) — most complex

**What:** Add `window/workDoneProgress` around analysis so editors show a progress indicator.

**Implementation:**
- In `run_refresh_request` (`backend.rs:245`): generate token, call `client.create_work_done_progress(token).await`, then `client.progress(token, "ripr analysis").begin().await`
- Create `ProgressEndGuard` struct mirroring `RefreshCancellationGuard` (`backend.rs:1342`) — holds `Option<OngoingProgress>`, sends End on Drop via `tokio::spawn`
- On success (`backend.rs:461`): `guard.finish_with_message("complete")`
- On cancellation: Drop guard sends End automatically
- Files: `crates/ripr/src/lsp/backend.rs` only

### Execution order
1. PR 1 (debounce) — one file, simplest
2. PR 2 (budget enforcement) — two files, medium
3. PR 3 (progress signal) — one file, complex (Drop guard + async workaround)

### Constraints
- Each PR on its own branch
- `cargo fmt` + `cargo clippy --all-targets -- -D warnings` + `cargo test -p ripr --lib lsp::` before push
- Resolve bot threads before merging
- Each PR is independently mergeable