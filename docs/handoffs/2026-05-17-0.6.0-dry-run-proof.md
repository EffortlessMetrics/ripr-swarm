# Handoff: 0.6.0 Dry-Run Proof

Date: 2026-05-17
Branch / PR: `release-0.6.0-dry-run` / #1096
Latest merged PR: #1096 `release: record 0.6.0 dry-run proof`
(commit `e718b1ed6f9b7dc29f071a7f6cc94bdc292f89fc`)

## Current Work Item

`release: validate 0.6.0 dry-run proof`

This handoff records the local cut-readiness proof for 0.6.0. It does not tag,
create a GitHub Release, publish the crate, publish the VSIX, publish to Open
VSX, change branch protection, refresh badge endpoints, or promote preview
language evidence.

## Proof Summary

Required release gates passed locally:

```bash
cargo fmt --check
cargo check --workspace --all-targets
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo doc --workspace --no-deps
cargo xtask check-product-copy
cargo xtask check-generated-clean
cargo xtask release-readiness --version 0.6.0
cargo package -p ripr --list
cargo publish -p ripr --dry-run
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
npm --prefix editors/vscode run package
```

Additional release-operator report:

```bash
cargo xtask pr-triage-report
```

## Generated Proof Artifacts

Generated artifacts are local proof only and must not be committed:

- `target/ripr/reports/release-readiness.json`
- `target/ripr/reports/release-readiness.md`
- `target/ripr/reports/pr-triage.json`
- `target/ripr/reports/pr-triage.md`
- `target/ripr/release-readiness/install/bin/ripr.exe`
- `target/ripr/release-readiness/pilot/repo-exposure.json`
- `target/ripr/release-readiness/pilot/repo-exposure.md`
- `target/ripr/release-readiness/pilot/agent-seam-packets.json`
- `target/ripr/release-readiness/pilot/pilot-summary.json`
- `target/ripr/release-readiness/pilot/pilot-summary.md`
- `target/ripr/release-readiness/targeted-test-outcome.json`
- `target/ripr/release-readiness/agent-verify.json`
- `target/ripr/release-readiness/agent-receipt.json`
- `editors/vscode/dist/ripr-0.6.0.vsix`

`cargo xtask check-generated-clean` passed after these proof commands, so the
generated proof artifacts remained ignored/generated-only.

## Release-Readiness Result

`cargo xtask release-readiness --version 0.6.0` completed with overall
`status: warn`.

Required checks passed:

- `package-list`
- `publish-dry-run`
- `path-install`
- `installed-command-surface`
- `pilot-boundary-fixture`
- `outcome-boundary-fixture`
- `agent-verify-boundary-fixture`
- `agent-receipt-boundary-fixture`
- `lsp-cockpit`
- `github-workflow-defaults`
- `vsix-packaging-path`
- `known-limits-docs`

The only warning was non-required:

- `repo-exposure-latency`: report exists but has `status: warn`.

This does not block the cut, but it should remain visible to the release
operator. If maintainers want a zero-warning release-readiness report, rerun
or repair `cargo xtask repo-exposure-latency-report` before tagging.

## Package And Extension Proof

`cargo publish -p ripr --dry-run` packaged and verified `ripr v0.6.0`, then
aborted upload because it was a dry run.

`npm --prefix editors/vscode run package` produced:

```text
editors/vscode/dist/ripr-0.6.0.vsix
```

The VSIX package command emitted the existing packaging-size warning about
many JavaScript files. That warning is not new release behavior and does not
imply a publish failure.

`npm --prefix editors/vscode ci` reported two high-severity audit findings in
the existing dependency tree. This dry-run proof did not change dependencies
or run `npm audit fix`; dependency remediation should be a separate explicit
dependency lane if maintainers decide it blocks marketplace publication.

## Release Surface Repairs Made In This Slice

This dry-run slice also fixed release-surface drift found during proof:

- README now names the `v0.5.0` public release line as the current verified
  editor server-asset path and says 0.6.0 must refresh the same asset family
  before publication.
- `docs/RELEASE.md` now includes `cargo xtask check-product-copy`,
  `cargo xtask check-generated-clean`, `npm --prefix editors/vscode ci`, and
  `npm --prefix editors/vscode run compile` in the 0.6.0 proof path.
- The 0.6.0 release-readiness handoff now points at merged #1095 instead of
  the authoring-time pending PR.
- The release-readiness report summary no longer says "0.5 public loop
  commands"; it uses version-neutral public release-loop language.

## Open PR Board Disposition

`cargo xtask pr-triage-report` was rerun after #1096 and scanned 9 open PRs.
It returned advisory
`status: warn`.

Release-relevant, optional hardening:

- #1076 `test(policy_history): pin parse, blocker, ceiling, and warnings
  branches`: useful policy-history coverage, currently behind.
- #1082 `test(output): pin uncovered branches in gate.rs`: useful gate-output
  coverage, currently behind.
- #1073 `test(seam_inventory): pin walker drop branches uncovered by llvm-cov`:
  useful coverage but overlaps #1078 and needs owner selection.

Defer unless a release proof command exposes a real blocker:

- #1077, #1078, #1079, #1083: draft/behind optional coverage fills.
- #1072 and #1075: broad SRP refactors.

Already dispositioned during the release-prep window:

- #1093 `report: harden finding alignment coverage audit` merged.
- #1092 `Add xtask report: rust-conversion-candidates...` merged as the
  canonical Rust-conversion inventory path.
- #1085, #1087, #1089, and #1091 were closed as duplicate Rust-conversion
  inventory variants.
- #1086, #1088, and #1090 were closed as deferred release-helper automation
  variants.

Do not absorb broad conversion/refactor work into 0.6.0. The release is now in
proof and cut-readiness mode.

## Remaining Release Steps

The repo is prepared for a separate release operator to:

1. Decide whether to land #1076, #1082, or another small green coverage PR
   before tag.
2. Rerun the release proof commands on the final tag candidate.
3. Push `v0.6.0`.
4. Verify GitHub Release assets: VSIX, server archives, manifest, and
   checksums.
5. Publish crates.io and marketplace artifacts only after asset verification.
6. Refresh public install and marketplace receipts after publish.
7. Refresh badge endpoints only through the generated badge workflow.

## What Not To Do

- Do not publish from this PR.
- Do not push tags from this PR.
- Do not create GitHub Releases from this PR.
- Do not hand-edit badge endpoints or generated proof artifacts.
- Do not change analyzer behavior, policy authority, generated tests,
  provider behavior, mutation execution, default blocking, baselines,
  suppressions, branch protection, or preview-language authority.
