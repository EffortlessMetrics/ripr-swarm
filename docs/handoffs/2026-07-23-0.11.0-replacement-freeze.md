# RIPR 0.11.0 replacement swarm freeze

Status: **candidate selected and immutable ref created; exact-tree qualification in PR #2362**

## Exact identities

```text
old candidate      3a80c634f2157dee0e7f74fc72b7975f07699143
old ref            freeze/source-sync-2026-07-21
new candidate      c86807ecdbf359594ef88c0ff38b10b446139dca
new ref            freeze/source-sync-2026-07-23
audit source base  ec7dc6beaec8ad2efca4abd502f9253cdb4f939c
new source parent  2b45cffe4353cc055ce201070555450f4c85d6ac
audit delta        142 commits
record-set hash    ea7373162affc0ddb6e7e54cf5f3d8ab56c4f4f82d833b67e93b29565845c1e4
```

At selection time the audit head and `ripr-swarm/main` were identical. No supplemental post-audit range is included.

The machine-readable receipt is
[`docs/release-candidates/0.11.0-replacement-freeze.json`](../release-candidates/0.11.0-replacement-freeze.json).
The full per-commit audit and accepted category receipts merged through source PR `EffortlessMetrics/ripr#1511` at `2b45cffe…`.

The old source audit base is retained for provenance but is not the final promotion parent. The refreshed #1478 preflight must use `2b45cffe4353cc055ce201070555450f4c85d6ac`.

## Why the candidate changed

The July 21 candidate predates load-bearing behavior that the current release narrative depends on:

- producer-owned gate, gap, seam, repair-route, and command identities;
- provenance-bound static artifacts, canonical movement validation, and actual repository-HEAD receipt binding;
- explicit static-versus-executed assurance vocabulary;
- partial-diff and repository-scope limited states that cannot become clean or gate-authoritative by omission;
- saved-workspace currentness, dirty-buffer quarantine, bounded diagnostic and action delivery, real-binary lifecycle proof, deadlines, and transport bounds;
- analyzer scope, owner, oracle, cross-language, I/O, and fail-closed fixes;
- generated blocking-CI corrections and security dependency updates.

The audit accepted 94 semantic rows for inclusion, 13 release-infrastructure rows, and 19 zero-semantic-delta structural moves. Fifteen rows are safe to defer, and one version-copy row must be resolved by source metadata authority.

## Executed-verification boundary

The candidate includes the typed `VerificationExecutionResultV1` contract and validator. It deliberately defers `ripr-swarm#2332`–`#2334`.

Therefore release copy must say:

> RIPR 0.11.0 does not execute verification commands and does not issue RepairReceiptV2. Static movement and externally run command outcomes remain separate evidence axes.

## Source survivors

The source promotion must preserve:

- bounded subprocess adapter recognition and RIPR-SPEC-0133;
- source 0.10.1 staging until the separate 0.11.0 metadata PR;
- source release, publish, signing, asset, Marketplace/Open VSX, badge, repository-settings, and protected-check authority;
- the history-preserving promotion runbook.

The replacement candidate dependency graph and current swarm analyzer/editor structure are the integration base. The dry-merge preflight must record every textual and semantic conflict rather than reusing the July 21 conflict list.

## Qualification boundary

GitHub returned no workflow runs attached directly to candidate merge commit `c86807ec…`.

PR #2362 therefore qualifies the exact candidate **code tree plus only these two docs-only freeze receipts**. Its protected checks are valid exact-tree qualification, but must not be described as direct workflow attachment to the candidate commit.

The issue remains open until #2362's required checks and review are green and the qualification result is recorded on `ripr-swarm#2354`.

## Next sequence

```text
qualify and merge ripr-swarm#2362; close ripr-swarm#2354
→ refresh source preflight ripr#1478 with source parent 2b45cffe…
→ run source editor/Workspace Trust proof ripr#1507
→ construct direct two-parent join ripr#1508
→ merge promotion-only PR ripr#1465
→ stage 0.11.0 metadata under ripr#1466
```

## Authority boundary

This freeze does not merge source history, change versions, tag, publish, sign, use release secrets, mutate marketplaces, or authorize a public release.
