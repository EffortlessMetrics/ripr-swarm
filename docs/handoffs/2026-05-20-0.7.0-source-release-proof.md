# Handoff: 0.7.0 Source Release Proof

Date: 2026-05-20

Branch / PR: `release/0.7.0-source-proof` / pending

Proof base: `f4e6635ee989768bd9b559c95aecfb00776f4a37`
(`#1350` `report: add ripr-swarm readiness`)

## Current Work Item

`release: prove 0.7.0 source artifacts`

This handoff records the final source-side proof run for the promoted and
version-bumped 0.7.0 release candidate. It proves the crate, command surface,
output contracts, traceability, capability metadata, fixture contracts, LSP
cockpit report, release-readiness packet, product copy checks, generated-clean
state, publish dry run, and VS Code package path from source `ripr`.

It does not tag, publish to crates.io, create a GitHub Release, publish VS Code
Marketplace or Open VSX artifacts, move release or signing secrets into
`ripr-swarm`, refresh public badge semantics beyond the explicit badge endpoint
PR, run providers, generate tests, edit user source, run mutation testing, or
change default CI blocking.

## Proof Summary

Result: source artifacts proved; ship decision still pending.

All required source release proof commands passed on the proof base. The proof
has three warnings or accepted limitations:

- accepted warning: `release-readiness` remains overall `warn` because
  `repo-exposure-latency` is advisory and not passing;
- accepted npm warning: `npm ci` reports three existing dependency audit
  findings in the VS Code dependency tree;
- accepted VSIX packaging warning: `vsce package` reports the existing
  JavaScript file-count bundling warning while still producing
  `editors/vscode/dist/ripr-0.7.0.vsix`.

None of those warnings changes release version metadata, analyzer semantics,
default gate behavior, preview-language authority, generated tests, source-edit
automation, provider execution, mutation execution, public badge semantics, or
publish state.

## Version Proof

- crate version: `0.7.0` in `crates/ripr/Cargo.toml`;
- lockfile ripr package version: `0.7.0`;
- VS Code extension version: `0.7.0` in `editors/vscode/package.json`;
- public CLI smoke: `ripr 0.7.0`.

## Commands Run

```bash
cargo fmt --check
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo doc --workspace --no-deps
cargo xtask check-output-contracts
cargo xtask check-traceability
cargo xtask check-capabilities
cargo xtask check-fixture-contracts
cargo xtask lsp-cockpit-report
cargo xtask release-readiness --version 0.7.0
cargo xtask check-product-copy
cargo xtask check-generated-clean
cargo xtask check-pr
cargo package -p ripr --list
cargo publish -p ripr --dry-run
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
npm --prefix editors/vscode run package
cargo run -p ripr -- --version
cargo run -p ripr -- first-pr --help
cargo xtask check-generated-clean
git diff --check
```

## Command Results

- `cargo fmt --check`: passed.
- `cargo check --workspace --all-targets`: passed.
- `cargo test --workspace --all-targets`: passed; 2526 tests passed.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `cargo doc --workspace --no-deps`: passed.
- `cargo xtask check-output-contracts`: passed.
- `cargo xtask check-traceability`: passed.
- `cargo xtask check-capabilities`: passed.
- `cargo xtask check-fixture-contracts`: passed.
- `cargo xtask lsp-cockpit-report`: passed.
- `cargo xtask release-readiness --version 0.7.0`: required checks passed;
  overall status remains `warn` because `repo-exposure-latency` is advisory and
  not passing.
- `cargo xtask check-product-copy`: passed; nine public surfaces checked and no
  unbridged internal vocabulary found.
- `cargo xtask check-generated-clean`: passed before and after package/VSIX
  proof commands.
- `cargo xtask check-pr`: passed. Expected negative-path stderr appeared for
  invalid CLI flags and temporary directories without `Cargo.toml`.
- `cargo package -p ripr --list`: passed and listed the packaged crate surface.
- `cargo publish -p ripr --dry-run`: passed; packaged 199 files, 4.6 MiB
  unpacked, 776.0 KiB compressed, and aborted before upload because this was a
  dry run.
- `npm --prefix editors/vscode ci`: passed with existing dependency warnings
  and three audit findings.
- `npm --prefix editors/vscode run compile`: passed.
- `npm --prefix editors/vscode run package`: passed and produced
  `editors/vscode/dist/ripr-0.7.0.vsix` at 540389 bytes.
- `cargo run -p ripr -- --version`: passed and printed `ripr 0.7.0`.
- `cargo run -p ripr -- first-pr --help`: passed and printed the public
  first-PR command surface.
- `git diff --check`: passed.

## Release-Readiness Details

`cargo xtask release-readiness --version 0.7.0` produced
`target/ripr/reports/release-readiness.md` with:

- version: `0.7.0`;
- status: `warn`;
- all required checks: `pass`.

Required checks that passed:

- `package-list`;
- `publish-dry-run`;
- `path-install`;
- `installed-command-surface`;
- `pilot-boundary-fixture`;
- `outcome-boundary-fixture`;
- `agent-verify-boundary-fixture`;
- `agent-receipt-boundary-fixture`;
- `lsp-cockpit`;
- `github-workflow-defaults`;
- `vsix-packaging-path`;
- `known-limits-docs`.

Non-required warning:

- `repo-exposure-latency`: accepted warning for 0.7.0. The JSON path timed out
  at 30049 ms after processing 500 of 38927 seams, so the Markdown path was
  skipped after the JSON timeout. This is not a publish blocker in the current
  release-readiness contract, but latency work remains a follow-up.

## Package And Install Proof

Crate packaging:

- `cargo package -p ripr --list`: passed.
- `cargo publish -p ripr --dry-run`: passed.
- Dry-run package size: 199 files, 4.6 MiB unpacked, 776.0 KiB compressed.
- No publish occurred.

Installed command surface, through release-readiness:

- path-installed `ripr.exe` exists under
  `target/ripr/release-readiness/install/bin/ripr.exe`;
- installed binary exposes `ripr --version`, `ripr --help`, and
  `ripr first-pr --help`;
- installed command surface reports `ripr 0.7.0`.

## VS Code Proof

- `npm --prefix editors/vscode ci`: passed.
- `npm --prefix editors/vscode run compile`: passed.
- `npm --prefix editors/vscode run package`: passed.
- VSIX artifact: `editors/vscode/dist/ripr-0.7.0.vsix`.
- VSIX size: 540389 bytes.

Warnings:

- accepted dependency warning: `npm ci` reported three audit findings in the
  existing VS Code dependency tree. This proof did not run `npm audit fix` or
  change dependencies. The ship decision should explicitly accept this warning
  or name it as a blocker.
- accepted packaging warning: `vsce package` reported the existing JavaScript
  file-count bundling warning. Packaging still succeeded and produced the
  0.7.0 VSIX.

## Public Command Smoke

```text
ripr --version -> ripr 0.7.0
```

`ripr first-pr --help` is available and reports:

```text
Create the start-here packet for one PR from existing RIPR artifacts.
```

Usage:

```text
ripr first-pr [--root <path>] [--base <rev>] [--head <rev>] [--gap-ledger <path>] [--first-action <path>] [--review-comments <path>] [--agent-packet <path>] [--gate-decision <path>] [--receipts-dir <path>] [--out-dir <path>] [--check]
```

## Boundaries Verified

This proof preserves the 0.7.0 release claim as the actionable static repair
loop:

```text
existing evidence
-> actionable gap packet
-> ranked swarm plan
-> dry-run repair context
-> focused human or agent edit outside RIPR
-> verify command
-> receipt command
-> actionable-gap outcome
-> next packet or no-action
```

This proof does not claim:

- autonomous repair;
- generated tests;
- provider or model calls;
- source-edit automation;
- runtime mutation execution;
- killed or survived mutation outcomes;
- runtime adequacy;
- coverage adequacy;
- correctness proof;
- default CI blocking;
- gate eligibility by default;
- TypeScript, JavaScript, or Python preview promotion;
- current full `ripr-swarm` live audit completion or live current-repo repair
  selection.

## Publish Decision Still Required

This proof is sufficient to prepare the 0.7.0 ship-decision packet, but it does
not authorize release execution.

Next required handoff:

```text
docs/handoffs/2026-05-20-0.7.0-ship-decision.md
```

That handoff must choose exactly one:

```text
Decision: ship now
Decision: do not ship yet
Decision: ship after named blocker(s)
```

If the decision is `ship now`, it must explicitly accept or block on:

- the advisory `repo-exposure-latency` warning;
- the VS Code dependency audit findings;
- the VSIX JavaScript file-count packaging warning;
- the accepted 0.7.0 limitation that the current full `ripr-swarm` live audit
  timed out and did not select a live current-repo repair packet.

Do not tag, publish, create a GitHub Release, or publish editor marketplace
artifacts from this proof handoff.

## Recommended Next Action

Add the 0.7.0 ship-decision handoff. If maintainers accept the warnings above,
the decision can be `ship now`. If any warning is promoted to blocking, the
decision must name the blocker and the proof needed to clear it.

## What Not To Do

- Do not reopen product lanes to improve the release after this proof.
- Do not absorb unrelated swarm or source refactors into 0.7.0.
- Do not claim live current-repo repair proof from the timed-out
  `ripr-swarm` audit.
- Do not refresh badge endpoints as part of this proof.
- Do not cut the tag, publish the crate, create a GitHub Release, or publish
  VS Code/Open VSX artifacts from this handoff.
