# Handoff: 0.6.x Final Proof

Date: 2026-05-18
Branch / PR: `release/0.6x-final-proof-20260518` / #1215
Proof base: `ad44518ffb3455774bfff7150efe31af0f20ef5f`
(`#1213` `docs(release): freeze 0.6.x candidate`)
Product freeze base: `b43a092d94d1a7e9eb3b7df29fc7d2a7ee69a66b`
(`#1210` `analysis: resolve same-test struct activation values`)

## Current Work Item

`release: record 0.6.x final proof`

This handoff records the final local proof run on the merged 0.6.x release
freeze. It does not tag, publish, create a GitHub Release, refresh badge
endpoints, publish marketplace artifacts, change branch protection, change CI
blocking, promote preview-language evidence, run providers, run mutation tests,
generate tests, edit source files, adopt baselines, or create suppressions.

## Proof Summary

Result: ship decision still pending.

All required local release proof commands passed on the proof base. The run has
three known warnings, all explicitly dispositioned below:

- accepted warning: `repo-exposure-latency` remains advisory `warn`;
- deferred dependency warning: `npm ci` reports two high-severity audit
  findings in the existing VS Code dependency tree;
- accepted packaging warning: `vsce package` reports the existing JavaScript
  file-count bundling warning while still producing `dist/ripr-0.6.0.vsix`.

No warning in this proof changed analyzer semantics, release version metadata,
default gate behavior, preview-language authority, generated badge endpoints,
or source files.

## Commands Run

```bash
cargo fmt --check
cargo check --workspace --all-targets
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo doc --workspace --no-deps
cargo xtask release-readiness --version 0.6.0
cargo xtask check-product-copy
cargo xtask check-generated-clean
cargo xtask check-pr
cargo package -p ripr --list
cargo publish -p ripr --dry-run
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
npm --prefix editors/vscode run package
cargo run -p ripr -- --version
cargo run -p ripr -- first-pr --help
git diff --check
```

## Command Results

- `cargo fmt --check`: passed.
- `cargo check --workspace --all-targets`: passed.
- `cargo test --workspace`: passed; 2336 tests passed, 1 doctest ignored.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `cargo doc --workspace --no-deps`: passed.
- `cargo xtask check-pr`: passed. Expected negative-path stderr appeared for
  invalid CLI flags and temporary directories without `Cargo.toml`.
- `cargo xtask release-readiness --version 0.6.0`: required checks passed;
  overall status remains `warn` because `repo-exposure-latency` is advisory
  and not passing.
- `cargo xtask check-product-copy`: passed.
- `cargo xtask check-generated-clean`: passed before and after generated proof
  commands.
- `cargo package -p ripr --list`: passed and listed the packaged crate surface.
- `cargo publish -p ripr --dry-run`: passed and reached the expected dry-run
  abort before upload.
- `npm --prefix editors/vscode ci`: passed with existing dependency warnings
  and two high-severity audit findings.
- `npm --prefix editors/vscode run compile`: passed.
- `npm --prefix editors/vscode run package`: passed and produced
  `editors/vscode/dist/ripr-0.6.0.vsix` at 515.2 KB.
- `cargo run -p ripr -- --version`: passed and printed `ripr 0.6.0`.
- `cargo run -p ripr -- first-pr --help`: passed and printed the public
  first-PR command surface.
- `git diff --check`: passed.

## Release-Readiness Details

`cargo xtask release-readiness --version 0.6.0` produced
`target/ripr/reports/release-readiness.md` with:

- version: `0.6.0`
- status: `warn`
- all required checks: `pass`

Required checks that passed:

- `package-list`
- `publish-dry-run`
- `path-install`
- `installed-command-surface`
- `pilot-boundary-fixture`
- `outcome-boundary-fixture`
- `agent-verify-boundary-fixture`
- `agent-receipt-boundary-fixture`
- `lsp-cockpit`
- `github-workflow-defaults`
- `vsix-packaging-path`
- `known-limits-docs`

Non-required warning:

- `repo-exposure-latency`: accepted warning for 0.6.x. The JSON path timed out
  at 30061 ms after processing 2000 of 38511 seams, so the Markdown path was
  skipped after the JSON timeout. This is not a publish blocker in the current
  release-readiness contract, but latency work should stay deferred until after
  0.6.x unless a maintainer explicitly makes it blocking.

## Package And Install Proof

Crate packaging:

- `cargo package -p ripr --list`: passed.
- `cargo publish -p ripr --dry-run`: passed.
- Dry-run package size: 187 files, 4.4 MiB unpacked, 749.2 KiB compressed.
- No publish occurred.

Installed command surface, through release-readiness:

- path-installed `ripr.exe` exists under
  `target/ripr/release-readiness/install/bin/ripr.exe`;
- installed binary exposes `ripr --version`, `ripr --help`, and
  `ripr first-pr --help`;
- installed command surface reports `ripr 0.6.0`.

## VS Code Proof

- `npm --prefix editors/vscode ci`: passed.
- `npm --prefix editors/vscode run compile`: passed.
- `npm --prefix editors/vscode run package`: passed.
- VSIX artifact: `editors/vscode/dist/ripr-0.6.0.vsix`.
- VSIX size: 515.2 KB.

Warnings:

- deferred dependency warning: `npm ci` reported two high-severity audit
  findings in the existing VS Code dependency tree. This proof did not run
  `npm audit fix` or change dependencies. The publish decision should either
  accept this for 0.6.x marketplace publication or name a dependency blocker.
- accepted packaging warning: `vsce package` reported the existing JavaScript
  file-count bundling warning. Packaging still succeeded and produced the
  0.6.0 VSIX.

## Public Command Smoke

```text
ripr --version -> ripr 0.6.0
```

`ripr first-pr --help` is available and reports:

```text
Create the start-here packet for one PR from existing RIPR artifacts.
```

Usage:

```text
ripr first-pr [--root <path>] [--base <rev>] [--head <rev>] [--gap-ledger <path>] [--first-action <path>] [--review-comments <path>] [--agent-packet <path>] [--gate-decision <path>] [--receipts-dir <path>] [--out-dir <path>] [--check]
```

## Boundaries Verified

This proof does not claim:

- runtime mutation proof;
- coverage-completeness proof;
- generated tests;
- provider execution;
- source edits;
- default CI blocking;
- branch-protection changes;
- baseline adoption;
- suppression creation;
- preview-language gate authority;
- badge endpoint refresh.

Preview-language evidence remains preview/advisory and non-gating by default.
Rust remains the stable evidence path for 0.6.x release claims.

## Publish Decision Still Required

This proof is sufficient to prepare the publish-decision packet, but it does
not itself authorize release execution.

Next required handoff:

```text
docs/handoffs/2026-05-18-0.6.0-publish-decision.md
```

That handoff must choose exactly one:

```text
Decision: ship now
Decision: do not ship yet
Decision: ship after named blocker(s)
```

If the decision is `ship now`, it must include exact tag, GitHub Release,
crates.io publish, VS Marketplace, Open VSX, post-publish smoke, and
abort/rollback-limit steps. Do not tag or publish without explicit maintainer
instruction.

## Recommended Next Action

Add the publish-decision handoff. If maintainers accept the advisory
repo-exposure latency warning and VS Code dependency warnings, the decision can
be `ship now`. If either warning is promoted to blocking, the decision must name
that blocker instead of leaving vague release anxiety.

## What Not To Do

- Do not reopen product lanes to improve the release after this proof.
- Do not absorb more broad refactors or optional coverage into 0.6.x.
- Do not refresh badge endpoints as part of this proof.
- Do not cut the tag, publish, create a GitHub Release, or publish VSIX/OpenVSX
  artifacts from this handoff.
