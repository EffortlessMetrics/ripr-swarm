# Handoff: 0.9.0 Freeze Re-Pin

Date: 2026-06-08
Supersedes the candidate SHA in: [2026-06-05-0.9.0-swarm-freeze.md](2026-06-05-0.9.0-swarm-freeze.md)
New candidate SHA: `f932cb1fbf0d780f4dd388343cef1c92e449b5ad` (swarm `main`, PR #1072 merge)
Release target: `0.9.0 — multi-language evidence-to-repair foundation` (unchanged)

## Why this re-pin exists

The 2026-06-05 freeze note pinned candidate `1cce26df` and deferred 28
open PRs. Since then the merge wave was drained and a proof-aware repo
validation phase (Phase P) landed. The candidate must advance to current
`main` so source and swarm stay connected and the mandatory back-sync
accounting works. This handoff records the new candidate and the actual
disposition of everything the old note deferred. The 2026-06-05 note
stays as-is for history; only the candidate SHA is superseded here.

## The release content is unchanged

`0.9.0`'s product is exactly what the original freeze candidate carried.
The post-freeze delta is **38 commits, none of which add product
behavior**:

- **Published crate (`crates/ripr/`)**: 34 files, +257 lines, **all
  behavior-preserving module-split refactors** (oracle extraction,
  CLI commands, config, first-pr / first-useful-action / outcome
  output modules). `crates/ripr/Cargo.toml` is untouched. No analyzer,
  output-schema, LSP, or CLI behavior changed.
- **Not published** (~47 files): the use-case spec spine
  (RIPR-SPEC-0065..0073), RIPR-PLAN-0061, goals routing, CI
  scratch/guard fixes, and Phase P proof-routing (manifest, `proof
  route`/`proof preflight`, PR-summary section, CI dry-run artifact,
  CI-budget report, release-proof guardrails). All of it is
  `xtask/`, `docs/`, `policy/`, `.github/`, `.ripr/`, `editors/` —
  none ships in the crate.

The 0.9.0 release-notes Claim and Non-Claims from the original freeze
note remain authoritative; this delta adds no new claim.

## Deferred-PR disposition (replaces the old note's "Deferred" list)

Every PR the 2026-06-05 note deferred is now resolved — none stranded:

- **Merged**: #526, #776, #777, #782, #783, #784, #785, #787, #790,
  #791, #947, #401, #406.
- **Closed** (superseded by the merge-wave refactor chain or invalid):
  #525, #527, #528, #529, #775, #778, #779, #780, #781, #786, #788,
  #789, #810, #984, #402.

## Phase P status at re-pin

Proof-aware repo validation is operational as advisory infrastructure:
manifest → route report → preflight → PR-summary disclosure → CI
dry-run artifact, plus the CI-budget report and fail-closed
release-proof guardrails (`check-proof-packs` asserts the
`release-package` pack covers all release surfaces with the full
release command set, `never_routed = true`, `unknown_surface_policy =
full-proof`). No CI lane is skipped: routing stays
`advisory-report-only`. Real lane-skipping (gating the required
`routed-rust-small` check on route output) is deferred to a
post-release, evidence-backed slice — it was not taken right before a
release.

## Standing drift to repair in back-sync (unchanged from 2026-06-05)

- Swarm `crates/ripr/Cargo.toml` is still `0.7.0`; the `0.8.0` bump was
  source-side only and never back-synced. The 0.9.0 source release bumps
  source `0.8.0 → 0.9.0`, and the mandatory back-sync must restore swarm
  version parity (`0.7.0 → 0.9.0`).

## Release sequence from here

1. Refresh source release PR `ripr` #1424 to sync swarm `f932cb1f`
   (merge commit, no squash; source keeps release/publish/signing
   authority, swarm keeps analysis/report/spec/proof-routing behavior).
2. Validate source: `cargo test --workspace`, clippy,
   `cargo xtask check-pr`, `cargo publish -p ripr --dry-run --locked`.
3. Publish (owner-gated): tag `v0.9.0`, `cargo publish -p ripr --locked`,
   install smoke, GitHub release from the reviewed changelog.
4. Mandatory back-sync source `main` → swarm `main` (merge commit),
   restoring version + changelog parity.
5. Post-release cleanup sweep.

## Release blockers at re-pin

None known. No open swarm PRs. `main` is green; CI scratch tempfails are
infra (issue #1058), not product failures, and are distinguishable in the
new `cargo xtask ci-budget` report.
