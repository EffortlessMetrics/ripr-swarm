# Handoff: 0.11.0 Swarm Freeze

Date: 2026-07-14
Candidate SHA: `eaa011c03503171c244a802650ce10734f5e23cc` (swarm `main`, merged PR #1596)
Prior source-promotion swarm SHA: `ff9028858201a64cdf7fd44d9833bbe5d32ae526`
Candidate delta: 1,457 commits
Release target: `0.11.0`
Source release program: `EffortlessMetrics/ripr#1463`
Freeze issue: `EffortlessMetrics/ripr-swarm#1590`

## Why 0.11.0

The staged source-side `0.10.1` metadata was never published. The current
candidate includes that bounded-subprocess work plus a much larger, user-visible
swarm delta: productionized opt-in Perl routing, cross-language honesty,
Rust repair/readiness work, targeted rerun and trust receipts, and LSP snapshot
and health behavior. This is a minor release, not a patch release.

The source release PR must fold staged-but-unpublished `0.10.1` into `0.11.0`
without fabricating a public `0.10.1` release date.

## Included through the candidate

### Rust and evidence-to-repair

- current repair-route, actionable-gap, verification, and receipt surfaces;
- targeted rerun and causal PR-delta behavior;
- current Rust dogfood and governed-pilot receipts;
- static-analysis honesty and fail-closed limitation wording.

### Perl preview/advisory

- opt-in Perl parser facts and repair routing;
- preview cards and bounded actionability projections;
- advisory support-tier wording, with unsupported paths remaining named
  limitations rather than repair-ready output.

### TypeScript, JavaScript, Bun, and Python preview

- current bounded adapter and bridge facts;
- cross-language false-actionable suppression and limitation routing;
- preview/advisory output only; no stable support-tier promotion.

### LSP and editor behavior

- cooperative refresh cancellation and scheduler lifecycle;
- transactional diagnostic publication;
- last-known-good snapshot retention from #1596;
- typed analysis-health publication and stale-action suppression;
- current VS Code typed-status behavior.

## Source-only work that must survive promotion

The history-preserving merge into `EffortlessMetrics/ripr` must retain source
release authority and source-only commits, including:

- bounded subprocess adapter recognition (`9496353061b3d3fa9c7a2287b98243ba6d719ba5`);
- structured review-warning records (`0b4e1edfeea3eb7fc54a367174975b0838e00193`);
- staged `0.10.1` metadata, to be explicitly folded into `0.11.0`;
- source release workflows, release history, package metadata, and marketplace
  authority;
- source dependency updates completed under `EffortlessMetrics/ripr#1464`.

Swarm-only workflow and repository settings do not become source release
authority merely because histories are merged.

## Deferred from this candidate

The following open PRs are not part of `0.11.0` unless a maintainer explicitly
reselects the candidate and regenerates this receipt:

- #1552 — contributor local-setup documentation;
- #1588 — `@types/node` maintenance update;
- #1589 — ub-review action pin update.

Broader LSP work also remains post-release unless separately completed and the
candidate is deliberately reopened: root/config identity, scheduler performance
telemetry, pull diagnostics, headless-agent protocol, and broader real-repo
LSP dogfood.

## Non-claims

- No runtime mutation execution.
- No autonomous code editing.
- No provider-backed analysis requirement.
- No default blocking for preview-language findings.
- Perl, TypeScript/JavaScript/Bun, and Python surfaces remain bounded
  preview/advisory support where documented.
- Last-known-good LSP snapshots preserve inspectability; they do not make stale
  evidence current or repair-ready.
- The freeze does not authorize crates.io, tag, GitHub Release, server asset,
  VS Marketplace, Open VSX, or secret operations.

## Validation before source promotion

Run on the exact candidate or record an explicit bounded stop reason:

```text
cargo fmt --check
cargo check --workspace --all-targets --locked
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo doc --workspace --no-deps --locked
cargo xtask goldens check
cargo xtask check-product-copy
cargo xtask check-generated-clean
cargo xtask check-output-contracts
cargo xtask check-static-language
cargo xtask check-traceability
cargo xtask check-capabilities
cargo xtask check-support-tiers
cargo xtask lane1-evidence-audit
cargo xtask evidence-quality-scorecard
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
npm --prefix editors/vscode test
```

A bounded optional analysis may end in `warn` with a stop reason. It must not be
rendered as a complete pass.

## Promotion sequence

1. Complete the source dependency disposition (`ripr#1464`).
2. Produce the merge preflight receipt (`ripr#1478`).
3. Merge this exact swarm candidate into source with a two-parent merge
   (`ripr#1465`), without version or changelog edits.
4. Establish `0.11.0` metadata and release copy separately (`ripr#1466`).
5. Complete readiness, executable-build, badge, and consumer qualification
   contracts before the ship packet and authorization request.

## Freeze rule

This candidate does not float with swarm `main`. Only a demonstrated release
blocker may reopen the freeze. Any replacement candidate requires an explicit
new SHA, delta, validation receipt, and updated deferred-work list.
