# Handoffs

This directory stores closeout and handoff records for completed implementation slices.

Each handoff should capture:

- what landed;
- which proof commands passed;
- which claims or support tiers changed;
- what remains and where it is tracked.

Use `docs/templates/closeout.md` for consistent structure.
