# Handoff: 0.6.x PR Board Disposition

Date: 2026-05-18
Branch / PR: `release/0.6x-board-disposition-20260518` / #1212 merged
Audited base: `0d17501d0e097ef960437db44157dbab672bc243`
(`#1212` `docs(release): record 0.6.x board disposition`)

## Current Work Item

`ops(release): record 0.6.x PR board disposition`

This pass turns the open PR board into a release decision packet. It does not
merge, close, update, or comment on PRs. It does not tag, publish, create a
GitHub Release, refresh badge endpoints, change CI blocking, promote preview
evidence, or mutate branch protection.

## Board Snapshot

Command:

```bash
cargo xtask pr-triage-report
```

Observed result:

- Status: `warn`
- Mode: advisory
- Open PRs scanned: 13
- Current queue risk: stale or pending validation on deferred PRs, not an
  identified 0.6.x release blocker.

## Release Disposition

No open PR is an automatic 0.6.x blocker.

The board has one live release-relevant candidate, several optional hardening
PRs, and several defer/close candidates. The final tag candidate can be selected
once the maintainer either accepts #1210 as in-scope and lands it, or explicitly
defers it and does not claim that activation-value sub-shape in 0.6.x release
copy.

## PR Decisions

| PR | Disposition | Release decision |
| --- | --- | --- |
| #1210 `analysis: resolve same-test struct activation values` | `release-owner-decision` | Only live evidence-quality candidate. Land before 0.6.x if the release should include this named `activation_value_unresolved` burn-down; otherwise defer and omit the claim. Currently conflict/dirty despite green checks. |
| #1076 `test(policy_history): pin parse, blocker, ceiling, and warnings branches` | `merge-if-green-but-not-blocking` | Useful Lane 2 coverage, but Policy Operations is already closed and this is not required for 0.6.x release truth. Needs rebase and validation refresh. |
| #1082 `test(output): pin uncovered branches in gate.rs` | `merge-if-green-but-not-blocking` | Useful gate-output coverage, but not required unless final proof exposes a gate behavior defect. Needs rebase and validation refresh. |
| #1073 `test(seam_inventory): pin walker drop branches uncovered by llvm-cov` | `choose-canonical-or-defer` | Test-only seam-inventory coverage. It overlaps #1078 on `crates/ripr/src/analysis/seam_inventory.rs`; pick one canonical branch after release or refresh only if maintainers choose it before tag. |
| #1078 `test(analysis): cover call-presence, full seam-kind, and inline-test-owner branches` | `close-or-supersede-duplicate` | Draft, stale, and overlaps #1073. Do not treat both as release work. |
| #1083 `test(analysis): pin uncovered branches in language/python.rs` | `defer-after-0.6.x` | Draft, behind main, and has a failed Codecov product patch check. Preview-language coverage expansion should not block 0.6.x. |
| #1072 `refactor(analysis): split find_related_tests_with_context into SRP strategies` | `defer-after-0.6.x` | Draft broad refactor. Behavior-preserving if refreshed, but not release-critical. |
| #1075 `refactor(suppressions): split parse_suppressions_manifest into SRP submodules` | `defer-after-0.6.x-policy-sensitive` | Draft, dirty, and touches suppression/policy surfaces. Do not land during release finalization without owner review for policy authority drift. |
| #1077 `test(output): cover pilot timeout-summary and empty-recommendation paths` | `defer-or-supersede` | Draft and dirty. Its surface overlaps later pilot-output refactor work; do not block release on it. |
| #1079 `test(cli): cover help-printer wrapper functions` | `defer-or-supersede` | Draft and behind. Its surface overlaps #1198 CLI help refactor. Not a release blocker. |
| #1196 `Refactor badge output into focused modules` | `defer-after-0.6.x-generated-evidence-sensitive` | Broad badge-output refactor touching traceability/capability/source-of-truth surfaces. Do not land before tag unless release proof exposes a badge correctness blocker. |
| #1198 `Refactor CLI help into focused submodules` | `defer-after-0.6.x` | Broad refactor and validation is incomplete in the PR body. Not a release blocker. |
| #1212 `docs(release): record 0.6.x board disposition` | `landed` | Release-ops disposition artifact merged and is now the accepted board snapshot for the freeze packet. |

## Resolved During This Pass

- #1200 `Refactor pilot output into SRP submodules` landed while this
  disposition PR was waiting on CI. It was broad output organization and is now
  part of the final proof surface; the next proof rerun should cover it through
  normal output-contract and release-readiness checks.
- #1212 `docs(release): record 0.6.x board disposition` landed and selected the
  accepted board snapshot for the release freeze.

## Release-Blocking Set

Current release-blocking set from the board: none.

Conditional blocker:

- #1210 blocks only if the maintainer wants the 0.6.x release claim to include
  the same-test struct activation-value sub-shape. If #1210 is deferred,
  release copy should say the remaining `activation_value_unresolved` work is
  post-0.6.x.

## Next Action

Choose one:

```text
Option A: defer #1210, keep the board disposition as accepted, rerun final proof.
Option B: refresh/merge #1210, update changelog and release handoff, then rerun final proof.
```

Do not drain broad refactor PRs as part of release finalization.

## What Not To Do

- Do not close duplicate PRs from this handoff without maintainer approval.
- Do not merge broad refactors just because they are green.
- Do not let draft/stale coverage PRs become release blockers.
- Do not claim #1210's activation-value sub-shape in release copy unless #1210
  lands before the final proof.
- Do not tag, publish, refresh badge endpoints, change branch protection,
  change CI blocking, add generated tests, run providers, execute mutation
  testing, or promote preview-language evidence.
