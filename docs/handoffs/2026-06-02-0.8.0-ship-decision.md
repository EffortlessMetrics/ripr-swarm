# Handoff: 0.8.0 Ship Decision

Date: 2026-06-02

Branch / PR: `release/0.8.0-ship-decision` / pending

Proof base: `d65384b3acad1dc0d0acd6a235b0a49b5bcfebd0`
(`#1420` `ci: run badge endpoints on hosted runners`)

## Decision

Decision: ready for explicit release execution.

This is a release-operator decision packet, not release execution. It records
that source `ripr` has the 0.8.0 versioned candidate, the release-intended
`ripr-swarm` sync, and green source CI. It does not tag `v0.8.0`, publish to
crates.io, create a GitHub Release, upload server assets, publish VS Code
Marketplace or Open VSX artifacts, refresh public badge endpoint semantics, move
release or signing secrets into `ripr-swarm`, run providers, generate tests,
edit production source, run mutation testing, or claim runtime, coverage, or
proof adequacy.

## Execution Status

Status: not executed by this packet.

The next release execution step should be a deliberate operator action from
source `ripr`:

```text
release: publish ripr 0.8.0 crate
```

That execution must prove the source checkout, final no-drift checks, tag,
crates.io result, and no source diff after publish before any later server
asset, VSIX, marketplace, GitHub Release, or execution-closeout work.

## Basis

The source release base is `d65384b3acad1dc0d0acd6a235b0a49b5bcfebd0`, which
includes:

- `#1417` source sync of release-intended `ripr-swarm` work for 0.8.0;
- `#1418` source policy change allowing the history-preserving swarm sync merge;
- `#1419` 0.8.0 version, changelog, schema-example, README, fixture, and editor
  metadata prep;
- `#1420` hosted-runner repair for the advisory badge endpoint workflow so
  source `main` settles green before release execution.

Current public registry and release state at decision time:

- crates.io search still reports `ripr = "0.7.0"`;
- source `origin/main` has `crates/ripr/Cargo.toml` version `0.8.0`;
- source `origin/main` has `editors/vscode/package.json` version `0.8.0`;
- no `v0.8.0` tag exists on `origin`;
- no GitHub Release `v0.8.0` exists.

## Source CI State

Post-merge source `main` push checks for
`d65384b3acad1dc0d0acd6a235b0a49b5bcfebd0` completed successfully:

- `CI`;
- `Security`;
- `Coverage`;
- `Source of Truth`;
- `Badge Endpoints`;
- `Test Analytics`;
- `Routed Rust Small`;
- `Future Clippy (advisory)`;
- CodeQL dynamic runs.

The badge endpoint workflow completed successfully after `#1420`; it does not
change public badge semantics in this decision packet.

## Accepted Warnings And Limitations

### `repo-exposure-latency`

Disposition: accepted warning for 0.8.0; do not block release execution.

`cargo xtask release-readiness --version 0.8.0` passed all required checks and
reported overall `warn` only because the non-required `repo-exposure-latency`
report remained warning. This does not block the release under the current
release-readiness contract.

### VS Code dependency audit

Disposition: accepted warning for 0.8.0 source release prep; remediate in a
separate dependency lane if needed before or after marketplace publication.

`npm --prefix editors/vscode ci` passed from the lockfile during release prep
and reported one existing high-severity audit advisory. This packet does not run
`npm audit fix`, change dependencies, or modify lockfiles.

### Live full-repo repair selection

Disposition: do not claim a full live-repo repair queue unless the release
execution or closeout explicitly proves it.

0.8.0 may claim the evidence-to-repair foundation and bounded repair-routing
contract surfaces. It must not claim autonomous repair, generated tests,
provider execution, mutation execution, default blocking CI, public badge
semantic changes, or that every live run produces actionable packets. Limited
runs remain honest limitations.

## Release Claims Allowed

0.8.0 public copy may claim:

- runtime completeness is explicit across Lane 1 reports;
- public actionability projection is stricter and requires actionable gap state;
- incomplete repair packets fail closed instead of becoming swarm-ready work;
- readiness exposes blocked states, missing fields, and limitation routes;
- repair packets carry proof and edit-boundary expectations when they satisfy
  the public packet contract;
- static limitations are routed into analyzer backlog instead of fake
  actionability;
- cache report and GC support improve report operability;
- the source release preserves release/distribution authority in `ripr` while
  `ripr-swarm` remains the development trunk.

0.8.0 public copy must not claim:

- autonomous repair;
- source-edit automation;
- generated tests;
- provider or model calls;
- runtime mutation execution;
- killed or survived mutation outcomes;
- runtime adequacy;
- coverage adequacy;
- correctness proof;
- default CI blocking;
- public badge semantic switch;
- full-run completion when the output is limited;
- TypeScript, JavaScript, or Python preview promotion beyond documented
  advisory status.

## Release Notes Source

Use the `CHANGELOG.md` `0.8.0 - 2026-06-02` section as the detailed
release-note source.

The GitHub Release body should lead with user value:

```text
ripr 0.8.0 ships the evidence-to-repair foundation: stricter actionable
projection, explicit full/limited runtime state, bounded repair packet
contracts, named limitation routes, cache/runtime hygiene, and readiness
surfaces that help humans and agents choose safe repair work without inventing
certainty.
```

Then group details under:

1. Evidence-to-repair foundation.
2. Runtime completeness and cache/report operability.
3. Strict actionability and fail-closed packet projection.
4. Readiness, blocked states, and limitation routes.
5. Editor/source release alignment.
6. Advisory boundaries and known limits.

Do not lead with internal lane names, transcript history, or process vocabulary.

## Exact Next Execution Steps

These are the next release-operator steps. They intentionally separate public
crate publication, server assets, marketplace surfaces, badge semantics, and
release closeout.

### 1. Prepare the local release checkout

```powershell
git fetch origin --prune --tags
git switch main
git pull --ff-only
git rev-parse HEAD
git status --short
```

Expected `HEAD` is `d65384b3acad1dc0d0acd6a235b0a49b5bcfebd0`, unless a later
maintainer explicitly accepts a newer release base and reruns required proof.

### 2. Confirm final no-drift gates

```powershell
cargo xtask release-readiness --version 0.8.0
cargo package -p ripr --list
cargo publish -p ripr --dry-run --locked
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
git diff --check
```

Abort if any required release-readiness check fails, if the package dry-run
fails, if source is dirty, or if public copy would overstate the 0.8.0 claim.

### 3. Tag and publish crates.io

```powershell
git tag v0.8.0
git push origin v0.8.0
cargo publish -p ripr --locked
```

If Cargo times out while polling after upload, check crates.io manually before
retrying. Do not run a second publish until registry state is known.

The publish record must capture:

- source commit;
- tag target;
- final dry-run proof;
- crates.io publish result;
- package version proof;
- no source diff after publish.

### 4. Produce server and editor artifacts

After the crate publish result is recorded, trigger the release workflows:

```powershell
gh workflow run release-server-binaries.yml -f version=0.8.0
gh workflow run publish-extension.yml --field version=0.8.0 --field publish_vs_marketplace=true --field publish_open_vsx=true
```

Verify the GitHub Release has the VSIX, server manifest, checksums, and
supported server archives before claiming editor self-provisioning for 0.8.0.

### 5. Post-publish smoke

```powershell
cargo install ripr --version 0.8.0 --locked --root target/ripr/install-smoke-cratesio-0.8.0 --force
target/ripr/install-smoke-cratesio-0.8.0/bin/ripr.exe --version
target/ripr/install-smoke-cratesio-0.8.0/bin/ripr.exe first-pr --help
target/ripr/install-smoke-cratesio-0.8.0/bin/ripr.exe doctor
target/ripr/install-smoke-cratesio-0.8.0/bin/ripr.exe pilot --root fixtures/boundary_gap/input --out target/ripr/install-smoke-cratesio-0.8.0/pilot
target/ripr/install-smoke-cratesio-0.8.0/bin/ripr.exe outcome --before fixtures/boundary_gap/calibration/before-targeted-test.repo-exposure.json --after fixtures/boundary_gap/calibration/after-targeted-test.repo-exposure.json
```

## Abort Conditions

Abort release execution if:

- `origin/main` has advanced and required proof has not been rerun on the new
  release base;
- `v0.8.0` already exists and points anywhere other than the accepted release
  base;
- crates.io already reports `ripr 0.8.0`;
- required source checks are red, missing, or still queued;
- `release-readiness` has any required failure;
- `cargo publish --dry-run --locked` fails;
- local source is dirty before tag or publish;
- release notes overstate claims;
- server/editor workflows fail after tag without a documented fix-forward plan.

## Next Documentation After Execution

After crates.io, GitHub Release assets, and editor publication are verified,
add a separate execution closeout handoff that records:

- tag and release URL;
- crates.io publication proof;
- server asset proof;
- VSIX and marketplace proof;
- post-publish smoke output;
- accepted warnings;
- non-claims that remain in force.
