# Handoff: 0.11.0 Swarm Refreeze

Date: 2026-07-23

- Replacement candidate SHA: `c86807ecdbf359594ef88c0ff38b10b446139dca`
- Replacement freeze ref: `freeze/source-sync-2026-07-23`
- Superseded candidate SHA: `3a80c634f2157dee0e7f74fc72b7975f07699143`
- Superseded freeze ref: `freeze/source-sync-2026-07-21`
- Source parent used for the next preflight: `ec7dc6beaec8ad2efca4abd502f9253cdb4f939c`
- Post-freeze range: 142 commits
- Decision: `refreeze`
- Audit: `docs/handoffs/2026-07-23-0.11.0-post-freeze-audit.md`
- Machine ledger: `docs/handoffs/2026-07-23-0.11.0-post-freeze-audit.toml`

## Selected scope

The candidate is the exact audited swarm tree. It includes the integrated gate, artifact-provenance, receipt, LSP/editor, analyzer-honesty, package, CI, and repository-authority state reviewed in the audit.

No commit after `c86807ecdbf359594ef88c0ff38b10b446139dca` belongs to this candidate without a supplemental range receipt and an explicit reselection.

## Deliberately deferred

The following issues remain post-0.11 unless the candidate is explicitly reopened:

- `ripr-swarm#2332` — producer-controlled verification-command execution;
- `ripr-swarm#2333` — canonical `RepairReceiptV2` issuance;
- `ripr-swarm#2334` — adversarial execution/receipt journey and closeout.

Release copy must therefore preserve these non-claims:

- verification commands are described but not executed by RIPR;
- the current receipt path is not `RepairReceiptV2`;
- static movement and externally observed command results do not establish correctness, coverage adequacy, mutation adequacy, or merge approval.

## Source integration requirements

The history-preserving source integration must retain source-only work and authority:

- bounded subprocess recognition at `9496353061b3d3fa9c7a2287b98243ba6d719ba5`;
- structured review-warning records at `0b4e1edfeea3eb7fc54a367174975b0838e00193`;
- staged `0.10.1` metadata until the separate `0.11.0` metadata PR;
- source release workflows, history, settings, package metadata, distribution, signing, badge, and marketplace authority.

The source preflight must be regenerated. Conflict paths and resolutions from source PR `#1506` are useful historical input but are not current proof for this candidate.

## Required exact-head qualification

Run the full validation list in the post-freeze audit against this SHA. At minimum, the release packet must contain exact identities for:

- crate and Cargo lockfile;
- binary and package list;
- VS Code package and npm lockfile;
- generated workflow and output schemas;
- required CI aggregate and any accepted limitations;
- source merge parent, merge base, conflict list, and survivor/exclusion decisions.

## Promotion sequence

```text
replacement swarm freeze
→ refreshed source preflight (#1478)
→ source editor integration proof (#1507)
→ final direct two-parent join (#1508)
→ source promotion only (#1465)
→ 0.11.0 metadata and changelog (#1466)
→ readiness and consumer qualification
→ ship packet
→ separate publication authorization
```

Source PR `#1506` is superseded by this candidate and the replacement join. It must not be merged.

## Freeze rule

The ref is immutable release evidence. A later release blocker may trigger a new candidate only through a new exact SHA, supplemental delta ledger, validation receipt, and explicit update to the source release program.

## Authority boundary

This refreeze does not authorize source integration, metadata changes, tagging, publication, asset uploads, marketplace operations, signing, secret use, or support-tier promotion.
