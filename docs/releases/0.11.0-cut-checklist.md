# RIPR 0.11.0 release checklist

Status: **active pre-cut operator checklist, refreshed 2026-09-25 from
`ripr-swarm/main` at `106b45ebd13c872d0dfd58cc54976dcd60c6b889`.**

That SHA is an orientation snapshot, not a candidate. It may move while swarm
convergence is open. The candidate becomes immutable only when #2768 freezes
`FINAL_SWARM_CUT` and #1609 creates the protected candidate ref.

This file is an execution index. The linked issues own scope, commands,
schemas, receipts, actors, and prohibitions. If this checklist and an owning
issue disagree, the issue wins and this file must be corrected. Issue or PR
state is not proof: each stage exits only through its named receipt, and the
next stage consumes that receipt rather than inferring completion from labels,
checkboxes, merges, or green summary checks.

Use these documents with this checklist:

- [`docs/RELEASE_TRANSACTION.md`](../RELEASE_TRANSACTION.md) — canonical
  two-repository transaction, command labels, packet paths, guarded J/K
  construction, and external mutation boundaries.
- [`docs/RELEASE_COPY_CHECKLIST.md`](../RELEASE_COPY_CHECKLIST.md) — public
  release, crate, README, marketplace, install, vocabulary, and asset copy.
- [`0.11.0-release-notes.md`](0.11.0-release-notes.md) — draft public release
  body; it is revalidated after the source join.
- [`docs/RELEASE.md`](../RELEASE.md) — source release and recovery authority.

Nothing in this file authorizes a candidate ref, source merge, tag, GitHub
Release, registry upload, marketplace publication, signing action, secret,
credential, environment, ruleset, branch-protection change, or merge-method
exception. Those actions require the exact live authority named below.

## 0. Transaction law

### 0.1 Repository authority

```text
EffortlessMetrics/ripr-swarm
  product development, convergence, candidate selection, candidate proof

EffortlessMetrics/ripr
  public source, history-preserving integration, release, distribution
```

Never create a public release tag or publish a public artifact from
`ripr-swarm`.

### 0.2 Phase vocabulary

The release advances through these states in order:

```text
swarm_convergence
hard_cut_eligible
candidate_materialized
candidate_qualified
source_frozen
reviewed_join_tree
source_integrated
source_release_ready
public_rc_verified
stable_authorized
stable_published
stable_verified
back_synced
rolling_restored
```

A stage may be blocked, failed, or invalidated. It may not be skipped by
renaming a later receipt.

### 0.3 Evidence vocabulary

Every required row records one of:

```text
passed
failed
limited
not_run
not_proven
infrastructure_tempfail
stale_or_wrong_subject
incomparable
not_applicable_with_reason
```

Rules:

- skipped, pending, cancelled, timed out, unavailable, stale, wrong-artifact,
  zero-subject, or incomparable evidence is not `passed`;
- human prose may summarize machine state but may not strengthen it;
- predecessor evidence remains historical unless the owning contract explicitly
  proves it is candidate-independent;
- volatile timestamps and durations are telemetry unless a contract makes them
  identity;
- a product defect changes candidate bytes and requires a successor candidate;
- an infrastructure failure may be retried against the exact unchanged
  candidate, with all attempts retained.

### 0.4 Direct-cut law

```text
CANDIDATE_SHA  = FINAL_SWARM_CUT
CANDIDATE_TREE = FINAL_SWARM_CUT^{tree}
```

There is no candidate-only product patch, exclusion commit, cherry-pick, or
projected tree. Repair or revert unacceptable behavior through ordinary
reviewed `ripr-swarm/main` before the cut.

### 0.5 Actor vocabulary

- **Agent** — an authorized operator or automation session executing routine
  repository work under the linked issue and repository policy.
- **Independent reviewer** — a role that did not solely implement the object it
  judges.
- **Steven** — the owner action required where no other actor has authority.

Owner-only boundaries include:

- explicit authorization to create the new #1609 candidate ref;
- stable per-channel authorization on `ripr#1470`;
- new credentials, secrets, environments, or repository settings;
- any temporary merge-method or protection exception;
- any action whose owning issue explicitly reserves the actor.

Recorded delegation permits agents to deliver ordinary reviewed PRs and to
execute the guarded J merge when its exact transaction authority is terminal.
It does not grant implicit public or settings authority.

## 1. Operator reset, packet root, and lane hygiene

Complete this section at the start of a release-control session and after any
compaction, handoff, or ambiguous tool result.

### 1.1 Fresh truth

- [ ] Use fresh or freshly fetched `ripr-swarm` and `ripr` checkouts.
- [ ] Verify each `origin` is the expected EffortlessMetrics repository.
- [ ] Fetch branches and tags with prune enabled.
- [ ] Require both primary checkouts to be clean.
- [ ] Record exact `main` SHA and tree for both repositories.
- [ ] Record `Cargo.toml`, `Cargo.lock`, editor manifest, editor lockfile, and
      release-control registry digests.
- [ ] Record current branch protection, applicable rulesets, required checks,
      merge methods, review/thread rules, and bypass actors.
- [ ] Capture every open PR in both repositories with number, base, head SHA,
      draft/merge state, checks, unresolved material threads, owner, and current
      release disposition.
- [ ] Capture the current #2766 selected-claim packet and its digest.
- [ ] Record host, OS, shell, Rust, Cargo, Git, Node, npm, VS Code, and relevant
      external-tool identities for evidence-producing runs.
- [ ] Treat any partial GitHub/API response as `not_proven`; reread rather than
      filling missing fields from memory.

### 1.2 Transaction packet

- [ ] Create the untracked transaction packet root prescribed by
      `docs/RELEASE_TRANSACTION.md`.
- [ ] Keep machine JSON and human Markdown projections derived from one DTO.
- [ ] Store exact input identities, command lines, environment boundaries,
      outputs, digests, failures, retries, invalidators, and non-claims.
- [ ] Keep absolute checkout roots, PIDs, and timestamps outside semantic
      digests unless a contract explicitly requires them.
- [ ] Upload or otherwise durably retain a failed-stage receipt before cleanup
      when that failure is decision-bearing.

### 1.3 One writer and disposable lane law

- [ ] Use one active writer per semantic authority, schema, generated family,
      workflow, lockfile, or heavily overlapping source surface.
- [ ] Read-only scouts and independent reviewers may run in parallel.
- [ ] A waiting CI lane parks only that lane; another independent selected
      claim may proceed.
- [ ] Do not mutate a worktree while local proof processes are reading it.
- [ ] Use lane-specific target/build directories for concurrent work.
- [ ] Do not place shared compiler-cache temporary state inside a disposable
      worktree.
- [ ] Before removing a lane, terminate owned Cargo/rustc/Node/VS Code/child
      processes and release file handles.
- [ ] Stop or restart `sccache` before deleting a worktree when its server temp
      directory points into that lane.
- [ ] After merge, supersession, or abandonment, remove the worktree and local
      branch, prune worktree metadata, remove lane build caches, and verify only
      the primary checkout and genuinely active lanes remain.
- [ ] Preserve evidence before deleting scratch repositories or temporary
      fixtures.
- [ ] Never delete another lane's branch, worktree, cache, or retained evidence
      merely to make the host look clean.

## 2. Close live swarm scope under #2766

Current phase is `swarm_convergence`. Rebuild the live packet from current
GitHub and Git state; do not reuse the 2026-09-22 snapshot embedded in an older
checklist revision.

### 2.1 Selected-claim rows

- [ ] Every selected row has exactly one #2766 resolution:

```text
pending
landed_and_accepted
accepted_defer_with_non_claim
candidate_exclusion_with_manifest
failed_release_hold
final_candidate_execution_only
historical_evidence_only
```

- [ ] Each row binds its issue, release materiality, dependencies, exact
      implementation/merge/artifact identities, discriminating proof, review
      state, candidate effect, invalidators, and residual/non-claim.
- [ ] `pending` is absent before `hard_cut_eligible`.
- [ ] A merged PR or closed issue is not accepted without the required proof
      and review.
- [ ] A defer includes exact public wording, owner, trigger, and destination.
- [ ] A release hold names the failing predicate and smallest repair owner.

### 2.2 Open-PR rows

- [ ] Every open PR at the cut receives exactly one current disposition:

```text
release_required_product
release_required_control
release_required_test_infrastructure
release_optional_pending_decision
accepted_defer_with_non_claim
candidate_exclusion_with_manifest
blocked_on_named_authority
superseded
unrelated_to_selected_candidate
```

- [ ] Record exact head/base SHA, relationship to other PRs, issue authority,
      changed semantic resources, and whether it entered `FINAL_SWARM_CUT`.
- [ ] Distinguish a stack, replacement, partial foundation, and true duplicate.
- [ ] Close or supersede losing implementations only after recording why the
      retained implementation owns the contract.
- [ ] Retain PR #3529 as historical do-not-merge evidence until the accepted
      successor transaction explicitly consumes or supersedes it.
- [ ] Do not merge an optional PR merely to empty the queue.

### 2.3 Product and control convergence rows

The live packet must include terminal dispositions for all release-material
owners, including newly discovered rows. At minimum reconcile:

- [ ] process-abort and resource-bound defects, including #4101 / PR #4108;
- [ ] unsafe or false-actionable repair packets, including #4105 / PR #4107;
- [ ] selected-root command authority and foreign-CWD execution:
      #3948 → #3999 → #4000/#4001 → #3937;
- [ ] stable-absent to exact admitted RC selection under #3798;
- [ ] native Windows process and LSP terminality under #3802/#3803 and #3922;
- [ ] canonical baseline identity and migration under #1964;
- [ ] candidate lifecycle consumption under #3842/#3924;
- [ ] Git diff and path identity, including #4003/#4004/#4005, #4006
      residuals, and any current canonical-prefix owner;
- [ ] schema/producer honesty under #3920, including #3913's explicit
      include-or-defer decision;
- [ ] exact Cargo and advisory graph under #3787 and #3811;
- [ ] exact shared editor npm graph under #3813;
- [ ] performance/operability under #3794/#3796 and focused children;
- [ ] blind installed agent journey under #3797;
- [ ] behavior challenge under #3804/#3805/#3806;
- [ ] effective agent-rule runtime evidence under #3785/#3926;
- [ ] final adversarial audit under #3807;
- [ ] every new candidate-relevant issue or PR created before the cut.

### 2.4 Merge-before-cut rule

Include a change before cut when it repairs a selected release contract such as:

- hard process abort, nontermination, orphan, or unbounded resource use;
- wrong repository, root, revision, path, package, or artifact identity;
- false clean, false closure, false exposure, unsafe actionability, or evidence
  strengthening;
- generated command injection or unsafe copy/paste behavior;
- silent omission of unreadable, partial, stale, or unsupported input;
- required installed/package/editor behavior that cannot be exercised;
- supported producer bytes rejected by an advertised schema;
- historical/copied/renamed/digest-mismatched control evidence accepted as
  current.

Feature expansion, preview breadth, refactoring, and general optimization may
defer only when the shipped path remains honest, bounded, usable for its claimed
tier, and carries an exact public non-claim.

## 3. Terminalize pre-cut product and proof lanes

### 3.1 Critical product repairs

For every release-required implementation PR:

- [ ] Reproduce the failure on an exact current base or retain source proof when
      process termination prevents a normal report.
- [ ] Add a discriminating positive and negative/removal control.
- [ ] Run focused proof, repository policy, affected fixtures/goldens, and the
      required protected check on the exact published head.
- [ ] Read the full production diff and every semantic owner/consumer.
- [ ] Adjudicate every substantive review thread with a reply and evidence.
- [ ] Record `REVIEW_READY` only on the exact head that passed.
- [ ] Merge through normal protected transport without rule bypass.
- [ ] Verify the squash/merge delta and resulting `main` ancestry/tree.
- [ ] Reconcile the issue and every parent packet affected by the repair.
- [ ] Remove the lane worktree, branch, and lane build cache.

For #4101 specifically:

- [ ] Guard every production parser entry that can reach the recursive parser.
- [ ] Preserve a typed limitation for skipped deep input, including unchanged
      discovered files.
- [ ] Retain the process-level pre-fix abort and post-fix non-abort fixture.
- [ ] Record residual unguarded recursion shapes as explicit non-claims.

For the #3948 chain specifically:

- [ ] One producer-selected resolved-root authority owns generated displays and
      portable typed command identity.
- [ ] CLI/report and LSP/editor consumers use that authority rather than
      reconstructing `.` or process CWD.
- [ ] Bash and native PowerShell execute actual product-produced commands from
      a foreign CWD.
- [ ] Artifact placement, artifact content identity, and the next consumer bind
      the same selected repository.
- [ ] Spaces, non-ASCII paths, wrong-root substitution, traversal, stale root,
      multi-root, and Workspace Trust controls fail closed.
- [ ] Stage A merged-main receipts reach #3797/#3922; Stage B is repeated on
      the immutable candidate under #2769.

### 3.2 Behavior challenge selection and judgment

- [ ] #3805 freezes at least nine real rows across at least two explicitly
      authorized repositories.
- [ ] The real floor is at least 3 `should_gap`, 3 `should_stay_quiet`, and
      3 `should_limit`.
- [ ] Existing difficult or unfavorable rows are retained unless an exact
      identity invalidation is recorded.
- [ ] Selection is frozen before final candidate outcomes are observed.
- [ ] Repository read-only authorization records actor, exact subjects,
      operations, network/materialization policy, retention, bounds, and expiry.
- [ ] #3806 independently adjudicates every frozen row.
- [ ] Every limit, disputed, or blocking row has at least two independent
      roles.
- [ ] Missing evidence and disagreement remain visible; they are not forced
      into the expected direction.
- [ ] False-actionable, false-exposed, under-credit, and correct-limitation
      remain separate facts.
- [ ] Selection and judgment packets have stable digests and are handed to
      #3804 and #2769.
- [ ] The exact frozen set is replayed under #2769 without replacement.

### 3.3 Installed journey, Stage A

Before the hard cut:

- [ ] Build/install a current merged-main package outside the source checkout.
- [ ] Prove the invoked binary path, digest, version, and absence of
      workspace/PATH substitution.
- [ ] Give the agent only installed public surfaces, public docs/help, and an
      ordinary target repository.
- [ ] Exercise diagnose → select → understand → bounded test-only edit →
      verify → current receipt.
- [ ] Run quiet/already-observed, static-limitation, wrong/stale target,
      production-edit, failed-verification, incomparable, cancellation, and
      spaces/non-ASCII controls.
- [ ] Classify every manual intervention.
- [ ] Repair product/docs friction through focused swarm PRs; do not hide it in
      a private prompt.
- [ ] Produce a merged-main smoke receipt clearly distinct from final candidate
      proof.

## 4. Build the pre-cut packet set

Each packet must bind an exact subject SHA/tree and declare its invalidators.

### 4.1 Schema/producer packet — #3920

- [ ] Derive the published-schema inventory from repository files.
- [ ] Map every schema to a live producer, reserved/unsupported disposition, or
      retirement state.
- [ ] Validate representative real producer bytes with nonzero subject counts.
- [ ] Retain discriminating missing/wrong/extra-field negatives.
- [ ] Check docs, README/schema table, audit inventory, and producer parity.
- [ ] Give #3913 exactly one include-or-defer disposition.
- [ ] If deferred, publish the exact `review-comments --gap-ledger` non-claim
      and ensure no report calls it schema-covered.
- [ ] Emit one inventory digest and packet consumed unchanged by
      #2766/#2768/#1609/#2769.

Invalidators: schema bytes, producer bytes, producer route, validator version,
docs claim, candidate SHA, or a new schema/audit finding.

### 4.2 Candidate lifecycle packet — #3924

- [ ] Record the owner decision for how raw selection authority enters
      snapshots before implementation begins.
- [ ] Ensure lifecycle resolution authenticates exact artifact bytes or an
      equivalently immutable content-bound grant before parsing claims.
- [ ] Wire #2766, #2768, #1609, #2769, and the source-preflight handoff through
      the accepted resolver.
- [ ] Register and consume the exact release-input families:
      - #4003 badge-diff subject;
      - #4004 xtask PR-evidence subject;
      - #4005 generated-CI diff contract;
      - #4006 Git path inventory;
      - #3948/#3937 command-root packets;
      - #3922 Windows aggregate;
      - #3797 installed-journey receipt.
- [ ] Propagate the #4006 path-record count and an unambiguous NUL-framed digest
      into the denominator, qualification preflight, and source handoff; a
      newline-joined digest cannot represent filenames containing newlines.
- [ ] Bind both raw artifact and registry/lifecycle identity in the source
      handoff so another checkout need not reconstruct selection authority.
- [ ] Reject historical, template-only, unknown, copied, renamed,
      reserialized, digest-mismatched, and wrong-generation objects.
- [ ] Make template retirement and pinned-candidate creation deterministic and
      append-only.
- [ ] Add a repository check or integration control preventing direct bypass.
- [ ] Produce one integration receipt for #3807.

Invalidators: registry bytes/state, artifact bytes/digest, consumer code,
operation class, candidate generation, or source-handoff schema.

### 4.3 Native Windows packet — #3922

Pre-cut rows may end `landed_pending_candidate_repeat`; no row is
`passed_on_exact_candidate` before #1609.

- [ ] Process-tree owner contains descendants before user code executes.
- [ ] Timeout, cancellation, setup failure, parent-before-child exit, inherited
      pipe, cleanup failure, and unrelated-process controls are terminal.
- [ ] Poisoned LSP initialization reaches the intended typed failure under an
      inner bound and leaves no task/socket/file lock.
- [ ] Equal-size timestamp-restored source/test edits cannot reuse stale cache
      results on Windows.
- [ ] Path, URI, PowerShell, drive, mixed-separator, spaces, Unicode, package,
      server, and VSIX rows use exact admitted subjects.
- [ ] Required/advisory/skipped lane states and subject counts are explicit.
- [ ] Host, Windows build, shell, toolchain, Node/npm/VS Code, binary, candidate,
      and artifact identities are retained.
- [ ] One pre-cut aggregate reaches #2766/#2768/#1609.
- [ ] Candidate-bound rows are repeated under #2769 and, where integration
      changes subjects, source #1578.

Invalidators: product bytes, harness bytes, host/runtime class, package bytes,
candidate identity, or required-run contract.

### 4.4 Cargo graph and advisory packets — #3787/#3811

- [ ] Every direct and materially changed transitive package has one reviewed
      terminal disposition.
- [ ] Exact manifests, lockfile, features, target graph, source, license,
      advisory, MSRV, and shipped/tool-only exposure are retained.
- [ ] `oxc`, `sha2`, Unicode, parser, archive, test infrastructure, and Windows
      dependency include/reject/defer evidence is explicit.
- [ ] The six `unic-*` advisories are re-read against the final lock graph.
- [ ] Every advisory has a selected path, applicability, artifact exposure,
      owner, expiry, and revisit trigger.
- [ ] `cargo deny`, dependency policy, package list, publish dry-run, and
      native-required rows are terminal.
- [ ] Later manifest or lock movement invalidates both receipts.

### 4.5 Shared editor npm graph — #3813

- [ ] Give every direct and materially changed transitive package one terminal
      disposition.
- [ ] Give the outstanding `@types/node` movement an explicit current-graph
      disposition.
- [ ] Record manifest/lock digests, Node/npm engines, registry integrity,
      licenses/advisories, lifecycle scripts, process/network behavior, and
      runtime/development/release-only exposure.
- [ ] Reproduce a clean isolated `npm ci`.
- [ ] Run compile, unit tests, real `test-electron`, and package.
- [ ] Inspect the actual VSIX inventory and extension bundle.
- [ ] Keep source-only marketplace publication tools under their source
      authority.
- [ ] Later editor manifest or lock movement invalidates the receipt.

### 4.6 Performance/operability packet — #3794/#3796

Every child ends as:

```text
landed_for_0_11
already_satisfied
bounded_defer_with_measured_nonclaim
release_hold
```

- [ ] Retain file discovery/stat/read, file-fact reuse/recompute/store errors,
      index construction, semantic reuse, fallback, phase cost, and exact
      identity counters.
- [ ] Replay empty/docs-only, one production file, related test, unrelated
      test, config/manifest/source-role/schema, corrupt cache, and removal
      controls.
- [ ] Prove cold/warm semantic parity.
- [ ] Replay `ripr-swarm` self-dogfood and the authorized large consumer with
      exact work counts and host context.
- [ ] Exercise exact-base-proof/bounded-diff routing where landed.
- [ ] Do not infer whole-analysis reuse from file-fact hits.
- [ ] A defer is permitted only when ordinary first-hour/PR routes remain
      bounded and honest and the public non-claim states the measured residual.
- [ ] Hand terminal dispositions and receipts to #2766/#2769 and source
      qualification.

### 4.7 Agent-rule runtime packet — #3785/#3926

- [ ] Every available supported provider/runtime starts from exact current
      checked instruction files.
- [ ] Muse/AGENTS, Codex/AGENTS, and Claude-compatible routes preserve the same
      core repository authority in observable actions.
- [ ] Invented summary approvals, premature parent completion, local-as-landed
      state, chase-main behavior, untrusted review instructions, unknown
      evidence, and public-action boundaries are discriminated.
- [ ] Compaction/resume recovers the parent goal, exact objects, and next
      transition rather than stale summary claims.
- [ ] Missing providers remain `not_run`, not inferred from file inspection.
- [ ] Produce a pre-cut packet and schedule the release-significant
      exact-candidate repeat under #2769.

### 4.8 Documentation/copy packet — #3921

- [ ] Refresh release notes, cut checklist, user docs, changelog inputs, and
      runbook against the actual selected scope.
- [ ] Give every candidate-relevant documentation PR/family one disposition.
- [ ] Encode every accepted defer as exact public non-claim.
- [ ] Commands, flags, exit codes, paths, schemas, package/editor/server claims,
      and recovery routes match current implementation.
- [ ] Release notes distinguish preview/advisory languages and static evidence
      from runtime mutation results.
- [ ] Pre-cut copy names what must survive J without predicting source truth.
- [ ] Run doc, copy, command-catalog, schema, generated, and first-use checks.
- [ ] Plan the mandatory post-J replay; pre-join copy is historical wherever J
      changes relevant bytes or behavior.

### 4.9 Final adversarial audit — #3807

Start only after selected implementations and pre-cut packets converge.

- [ ] Freeze the audit denominator from #2766.
- [ ] Give every denominator row an independent current-merged-main review.
- [ ] For each row, reconstruct the invariant, strongest counter-read,
      input→authority→transformation→consumer chain, and discriminating
      negative/removal experiment.
- [ ] Complete all eight integration reviews required by #3807.
- [ ] Route each verified finding to one focused owner.
- [ ] Re-review every blocking repair on its exact merged head.
- [ ] Preserve falsifying evidence for refuted findings.
- [ ] Give bounded residuals an owner and public non-claim.
- [ ] Emit one audit packet/digest consumed by #2766/#2768/#1609/#2769 and the
      source sync.
- [ ] Require no unresolved blocking/control defect, severe false claim, or
      missing required proof at exit.

## 5. Freeze the final denominator — #2768

### 5.1 Entry criteria

- [ ] #2766 has no pending required row or undispositioned candidate-relevant
      PR.
- [ ] #3920 schema packet is terminal.
- [ ] #3921 pre-cut documentation packet is current.
- [ ] #3922 pre-cut Windows packet is terminal.
- [ ] #3924 lifecycle-consumer integration is terminal.
- [ ] Dependency, npm, advisory, performance, installed-journey Stage A,
      challenge selection/judgment, and agent-rule packets are terminal.
- [ ] #3807 audit is terminal.
- [ ] `ripr-swarm/main` is clean, protected, and reread immediately before
      freeze.

### 5.2 Final ledger

- [ ] Set `FINAL_SWARM_CUT` to the exact accepted `main` commit.
- [ ] Record its tree, parents, version, package, manifests, locks, and generated
      identities.
- [ ] Enumerate separately:
      - ordered first-parent integrations;
      - complete reachable commit set since
        `45b56c0957ad7e7360114edceca4b844c85f846e`;
      - merged PR/change-package inventory;
      - #2766 selected-claim rows;
      - every open PR at the cut;
      - final generated/artifact receipts.
- [ ] Give every commit/integration one record and every open PR one exact
      membership/disposition row.
- [ ] Separate commit disposition from final tree effect.
- [ ] Reject missing, duplicate, out-of-range, wrong-order, count-consistent but
      wrong, or free-form rows.
- [ ] Produce deterministic JSON and Markdown with one record-set digest.
- [ ] Hand the packet unchanged to #1609, #2769, and `ripr#1769`.

### 5.3 Invalidation

Any merge to `main` before the candidate ref is created extends the denominator
and requires #2768 revalidation. Product defects found later require a successor
cut; do not patch the frozen tree.

## 6. Materialize the immutable candidate — #1609

This stage includes one explicitly gated external ref mutation.

### 6.1 Authorization and preflight

- [ ] Obtain explicit authorization naming repository, exact
      `FINAL_SWARM_CUT`, candidate ref, actor, action, and no-publication
      boundary.
- [ ] Verify the candidate ref does not already exist.
- [ ] Verify the `release-transaction-pins` ruleset is active for the exact tag
      pattern and blocks update/deletion without bypass.
- [ ] Re-read #2766, #2768, #3807, dependency, version, package, and lifecycle
      packet identities.
- [ ] Require exact candidate version `0.11.0` in the workspace and editor
      manifests.
- [ ] Stop if any identity, authorization, or protection state is ambiguous.

### 6.2 Ref and manifest

- [ ] Create exactly:

```text
refs/tags/ripr-release-0.11.0-<full-candidate-sha>
```

- [ ] Read back the ref and prove it peels directly to `CANDIDATE_SHA`.
- [ ] Never force, move, reuse, or repoint a historical candidate ref.
- [ ] Register the exact object as `pinned_exact_candidate`; retire the active
      selection template to historical state without rewriting old rows.
- [ ] Generate machine manifest and human handoff from one DTO.
- [ ] Bind candidate repository/SHA/tree/ref, version/package/locks, #2766
      packet, #2768 denominator, #3807 audit, required receipts, open PR
      dispositions, included claims, non-claims, source survivors/exclusions,
      #2769, and source-sync consumers.
- [ ] Keep any later manifest/control commit distinct from candidate product
      code.
- [ ] Do not record or predict `SOURCE_PARENT`.
- [ ] Emit a creation/read-back/protection/manifest receipt.

## 7. Qualify the exact candidate — #2769

Run only against the #1609 ref. Never use floating `main`, a PR head, a mixed
worktree, or a nearby control commit.

### 7.1 Identity preflight

- [ ] Candidate ref, SHA, tree, manifest, lifecycle row, and handoff agree.
- [ ] #2766, #2768, #3807, dependency, version, and package identities agree.
- [ ] Installed/package jobs receive the exact candidate and cannot resolve a
      workspace binary.
- [ ] Every retained predecessor artifact is either candidate-independent by
      contract or rerun.
- [ ] Later control/main movement does not retarget the candidate.

### 7.2 Required matrix

- [ ] Rust format, workspace/all-target/all-feature check and test, nextest,
      strict Clippy, docs, and doctests.
- [ ] Complete current repository policy, static-language, evidence-honesty,
      no-panic/allow, architecture/public-API, capability, workflow,
      process/network, file/executable, spec/traceability, generated-clean,
      source-of-truth, command-catalog, support-tier, release-target, and
      proof-pack gates.
- [ ] Fixtures, goldens, test-oracle report, dogfood, precommit, and check-pr.
- [ ] `cargo package --list` and `cargo publish --dry-run`.
- [ ] Default, all-feature, Rust-only, and shipped feature profiles.
- [ ] Required Linux, native Windows, and required macOS rows.
- [ ] Exact Cargo and editor dependency graphs and advisory receipts.
- [ ] Canonical baseline, honesty corpora, Git/path/diff identity, and
      human/JSON/schema parity.
- [ ] Performance and cache candidate repeats.
- [ ] Installed `.crate` and CLI outside the checkout.
- [ ] Exact server subjects and actual VSIX inventory.
- [ ] Trusted and untrusted editor journeys.
- [ ] Producer-authentic before/after → verify → receipt negatives.
- [ ] Blind installed journey Stage B.
- [ ] Foreign-CWD Bash and native PowerShell Stage B.
- [ ] Exact frozen behavior challenge replay.
- [ ] Schema, lifecycle, Windows, docs, agent-rule, and audit packets.

### 7.3 Qualification bundle

- [ ] Record every command/job as ran/skipped/current, required/advisory, subject
      count, output digest, and artifact identity.
- [ ] Record host/toolchain/target identities and retry history.
- [ ] Inventory crate, binary, server, manifest, archive, checksum,
      provenance, VSIX, editor, fixture, journey, challenge, performance,
      review, and receipt artifacts.
- [ ] Emit JSON and Markdown from one DTO and one aggregate digest.
- [ ] End in exactly one #2769 result:
      `qualified`, `semantic_failure`, `policy_failure`, `missing_evidence`,
      `stale_or_wrong_head`, `infrastructure_tempfail`, or `incomparable`.
- [ ] Hand the exact bundle to #2379 and `ripr#1769`.

### 7.4 Failure law

- Product/candidate byte defect: reject, repair `ripr-swarm/main`, produce a
  successor #2768/#1609 generation, and rerun invalidated proof.
- Candidate-neutral harness defect: repair under its owner and rerun affected
  rows without changing admitted artifacts.
- Infrastructure tempfail: retain it and retry only the unchanged candidate.
- Never rebuild or mutate an admitted artifact while claiming the same identity.

## 8. Drain source preparation before P0

Work may proceed in parallel with swarm convergence, but all selected
source-preparation PRs are terminal before `ripr#1769` records
`SOURCE_PARENT`.

- [ ] Inventory every open `EffortlessMetrics/ripr` PR and assign include,
      defer, supersede, unrelated, or blocking disposition.
- [ ] Freshly review and converge `ripr#1774`, including source changelog,
      VSIX `target/**` exclusion/inventory gate, and `deny.toml` port.
- [ ] Complete `ripr#1502` exact final-server-subject inventory and provenance,
      including its nonpublishing rehearsal and least-privilege permissions.
- [ ] Converge every other selected source-preparation PR and generated
      consequence.
- [ ] Keep publication workflow topology #1646 moving early; it must be
      terminal before any `v*` tag, even if it is not a P0 input.
- [ ] Ensure source changelog inputs are intended for semantic union through J.
- [ ] Do not freeze `SOURCE_PARENT` while a selected source-preparation PR is
      unresolved.

## 9. Freeze source and produce P0 — `ripr#1769`

From P0 until `ripr#1773` completes, ordinary source merges stop. Any movement
of `ripr/main` invalidates P0, P1, tree qualification, J, and the merge PR.

- [ ] Re-read:

```text
SWARM_PARENT        = exact #1609 candidate
SWARM_QUALIFICATION = exact terminal #2769 bundle
SOURCE_PARENT       = exact current ripr/main
MERGE_BASE          = actual Git merge base
```

- [ ] Prove both parents are clean, reachable, and consistent with receipts.
- [ ] Record ordered and complete parent ranges.
- [ ] Keep textual conflicts, both-sides changes, source-only paths, and
      swarm-only paths as separate denominators.
- [ ] Inventory specs, schemas, commands, policies, workflows, manifests,
      lockfiles, fixtures, generated files, docs, editor/distribution state,
      release surfaces, selected non-claims, and open PR dispositions.
- [ ] Observe repository settings/rulesets/refs as control state, not tree
      authority.
- [ ] Emit deterministic unfinalized P0 with:

```text
reviewed_resolved_tree = null
reviewed_resolved_tree_verified = false
```

- [ ] Hand P0 and its digest unchanged to `ripr#1770`.
- [ ] Record and enforce the source hold.

## 10. Review the join tree — `ripr#1770`

- [ ] Give every textual conflict, both-sides change, semantic overlap,
      source-only survivor, swarm-only survivor, generated consequence, and
      authority-sensitive object one disposition.
- [ ] Never use bare “ours” or “theirs” as a semantic disposition.
- [ ] Preserve source public/release authority and accepted source-only fixes.
- [ ] Preserve accepted swarm product history and swarm-only development
      controls.
- [ ] Resolve seam-cache and other generation collisions to a new generation
      where both parents advanced semantics differently.
- [ ] Resolve `CHANGELOG.md` as a semantic union: both `Unreleased` bodies,
      every entry once, no new version section in J.
- [ ] Keep source settings, secrets, credentials, environments, and live
      publication authorization outside swarm/product tree authority.
- [ ] Regenerate derived bytes only through canonical producers.
- [ ] Produce one deterministic `REVIEWED_JOIN_TREE`, complete resolution
      manifest, and P1 with
      `reviewed_resolved_tree_verified = true`.
- [ ] Use focused independent reviewers for release, product, dependency,
      editor, workflow, generated, schema, and documentation surfaces.
- [ ] Any unresolved/unknown authority is blocking.

## 11. Qualify and construct J — `ripr#1771` → `#1772`

### 11.1 Exact-tree qualification — `ripr#1771`

- [ ] Recreate the reviewed tree from the manifest in a fresh environment.
- [ ] Run source full matrix including nextest, precommit, check-pr, Linux,
      native Windows, required macOS, crate, server, VSIX, workflows, packages,
      and release-control gates.
- [ ] Bind every result to `REVIEWED_JOIN_TREE`.
- [ ] Produce `TREE_QUALIFICATION`; no repair commit is appended to the
      candidate tree.

### 11.2 J construction — `ripr#1772`

Construct only through the guarded source-promotion authority:

```text
J.parent[0] = SOURCE_PARENT
J.parent[1] = SWARM_PARENT
J.tree      = REVIEWED_JOIN_TREE
```

- [ ] Use `admit-resolved-tree`, `construct-exact-join`, and
      `publish-candidate-ref` or their current governed equivalents.
- [ ] Do not use raw `git commit-tree`, ordinary `git merge` plus arbitrary
      push, squash, rebase, cherry-pick, patch replay, subtree copy, or a repair
      descendant.
- [ ] Publish one immutable PR whose head is exactly J and whose required
      method is **Create a merge commit**.
- [ ] Never rebase or force-update the reviewed J branch.
- [ ] Record exact expected-head/base merge command and all packet digests.
- [ ] `ripr#1772` does not merge.

## 12. Merge and prove J — `ripr#1773`

Immediately before the irreversible merge:

- [ ] Re-read P0, P1, tree qualification, J, PR head, checks, reviews,
      `ripr/main`, rulesets, branch protection, and merge methods.
- [ ] Require `ripr/main == SOURCE_PARENT`.
- [ ] Require PR head exactly J with no child commit.
- [ ] Require J's ordered parents and tree to match the reviewed packet.
- [ ] Require every substantive exact-head thread resolved with evidence.
- [ ] Give every open source PR one current disposition.
- [ ] Use guarded expected-head and expected-base **Create a merge commit**
      transport without admin bypass.
- [ ] If a temporary merge-method exception is required, obtain Steven's exact
      separate authorization and a before/apply/restore/read-back packet.
- [ ] Restore any temporary policy immediately after the merge operation.
- [ ] Treat failed policy restoration as an active incident and stop.

After merge:

- [ ] Record the outer source merge.
- [ ] Prove outer parents are `[SOURCE_PARENT, J]`.
- [ ] Prove outer tree equals `REVIEWED_JOIN_TREE`.
- [ ] Prove source and swarm histories are reachable.
- [ ] Prove live policy is restored.
- [ ] Retain merge actor/method, expected-head operation, graph/tree receipt,
      and no-publication assertion.
- [ ] Do not rewrite history to repair a bad merge; preserve evidence and use a
      focused reviewed fix-forward/revert transaction.

## 13. Reconcile integrated source and produce ship state

### 13.1 Post-J reconciliation

- [ ] Complete `ripr#1766`, `ripr#1767`, and current source reconciliation
      owners after J.
- [ ] Replay #3921 against integrated source.
- [ ] Verify every swarm and source changelog entry appears exactly once.
- [ ] Fold unioned `Unreleased` content into exactly one active staged
      `0.11.0` section under `ripr#1466`.
- [ ] Do not run a second version bump; the version write already landed.
- [ ] Refresh release notes, docs, install instructions, schemas, command help,
      and public non-claims on the integrated head.
- [ ] Run `docs/RELEASE_COPY_CHECKLIST.md`.

### 13.2 Source readiness

Run the current equivalents of:

```text
ripr#1466 → #1471 → #1472 → #1484 → #1485 → #1473 → #1467
```

- [ ] Produce exact `SOURCE_RELEASE_HEAD`.
- [ ] Bind version/changelog, tree/clean state, badge, runbook, readiness, and
      candidate identity.
- [ ] Repeat any package/editor/server rows affected by J.
- [ ] Require source exact-head checks and independent review.

### 13.3 Ship packet

Run:

```text
ripr#1578 → #1468 → #1469
```

- [ ] Consume exact admitted crate, server, manifest, checksum, provenance, and
      VSIX subjects.
- [ ] Consume immutable-candidate and integrated-source challenge/journey
      repeats.
- [ ] Prove public subjects are not rebuilt after admission.
- [ ] Produce one immutable ship-or-hold packet.
- [ ] A ship packet is evidence, not stable-channel authorization.

## 14. Converge publication topology before any tag — `ripr#1646`

No `v*` tag may be pushed until this stage is terminal.

- [ ] One typed event/ref/channel classifier owns release mode.
- [ ] Exactly one reachable GitHub tag/release/asset writer exists for an
      admitted transaction.
- [ ] Competing server/extension release writers, create-on-view-failure,
      `--clobber`, generic `v*`, and version-only authority paths are removed or
      unreachable.
- [ ] Real publication consumes exact validated subjects and performs no VSIX
      or server rebuild.
- [ ] RC, stable GitHub, VS Marketplace, and Open VSX remain separate
      authorities.
- [ ] Manual dispatch defaults to no-publish.
- [ ] RC cannot reach marketplace jobs, secrets, or stable channels.
- [ ] Secrets/environments become reachable only after exact subject and
      channel admission.
- [ ] Local pinned publication tools replace floating `npx -y` behavior.
- [ ] Concurrency prevents two runs from becoming writer for one transaction.
- [ ] Aggregate receipt rejects missing, duplicate, stale, skipped-required, or
      contradictory child evidence.
- [ ] Implementation proof is nonpublishing and requests no real release or
      marketplace credential.
- [ ] #1502 final-subject provenance is consumed before release upload.

## 15. Publish and verify the release candidate

### 15.1 RC entry

- [ ] Source ship packet is terminal.
- [ ] #1646 publication topology is terminal.
- [ ] Exact RC subject inventory is admitted.
- [ ] RC authority is explicit and distinct from stable authorization.
- [ ] No candidate-changing defect remains.

### 15.2 RC transaction

Run the current graph:

```text
ripr#1633
→ ripr#1634 v0.11.0-rc.1 GitHub prerelease
  ├─ ripr#1635 independent public-byte/platform verification
  └─ ripr#1636 frozen repair cohort
→ ripr#1637 stable_ready_same_candidate
```

- [ ] `v0.11.0-rc.1` peels to the authorized source SHA.
- [ ] The RC is GitHub-prerelease-only unless a later exact authority says
      otherwise.
- [ ] Verify public server archives, manifest, checksums, attestations, VSIX,
      install paths, and supported platforms independently.
- [ ] Retain missing/failed channel rows; do not infer from workflow success.
- [ ] Candidate-changing repair creates a new source/candidate generation.
- [ ] `stable_ready_same_candidate` is terminal before requesting stable
      authorization.

## 16. Authorize, publish, and independently verify stable

### 16.1 Owner authorization — `ripr#1470`

- [ ] Steven posts one exact record per channel:
      `authorized` or `not_authorized`.
- [ ] The record binds source SHA/tree, version, exact subjects, credentials/
      environment owner, prerequisites, and order.
- [ ] One channel's authorization does not imply another.
- [ ] RC success does not imply stable authorization.
- [ ] #1470 does not authorize generic `cargo publish`.

### 16.2 Execute authorized leaves in order

1. [ ] crates.io, only through the exact admitted package/request;
2. [ ] `v0.11.0` tag at the authorized SHA;
3. [ ] GitHub Release, server assets, manifest, checksums, attestations;
4. [ ] exact admitted VSIX asset;
5. [ ] VS Code Marketplace, when separately authorized;
6. [ ] Open VSX, when separately authorized.

For every leaf:

- [ ] Re-read exact authorization immediately before mutation.
- [ ] Verify the input subject digest and no-rebuild guarantee.
- [ ] Retain request, actor, run, response, and public read-back.
- [ ] Stop at any unauthorized or ambiguous leaf.
- [ ] If an API response is lost, read live state before retrying.
- [ ] Never use version-only “already published” as content identity.

### 16.3 Independent public verification — `ripr#1481`

- [ ] crates.io package bytes and metadata equal admitted subject.
- [ ] Stable tag peels to the authorized source head.
- [ ] GitHub Release body passes the release-copy checklist.
- [ ] Asset set is complete, no extras/missing subjects, with matching digests.
- [ ] Manifest and checksums describe the exact archives.
- [ ] Attestations bind repository, workflow, ref/SHA, subject name, and digest.
- [ ] VSIX asset and marketplace packages equal the admitted VSIX.
- [ ] Marketplace versions and install journeys are independently observable.
- [ ] Every channel ends terminally; missing propagation remains pending or
      failed, not passed.
- [ ] Produce the final channel/adoption matrix consumed by K.

## 17. Construct, merge, and prove back-sync K

### 17.1 K candidate — #3419

After `ripr#1481`:

```text
K.parent[0] = exact approved rolling swarm continuation
K.parent[1] = exact released source head
K.tree      = REVIEWED_K_TREE
```

- [ ] Freeze exact public source tag/head/tree, ship packet, final channel
      matrix, J/W7/source merge, and swarm continuation.
- [ ] Give every conflict, overlap, survivor, exclusion, generated, package,
      schema, workflow, and policy object one reviewed disposition.
- [ ] Preserve rolling swarm development authority.
- [ ] Import released source truth intended for the shared product tree.
- [ ] Exclude source settings, secrets, credentials, environments, and active
      publication authority from swarm authority.
- [ ] Use the existing K verifier and governed constructor.
- [ ] Publish one immutable PR whose head is exactly K.
- [ ] Run full product/editor/policy/package proof and exact-head review.
- [ ] Do not merge or restore the next version in #3419.

### 17.2 Protected K merge — #3313

- [ ] Independently reread K, source public identity, channel matrix, swarm
      base, checks, reviews, graph/tree, verifier, and live policy.
- [ ] Use normal protected **Create a merge commit** with exact expected head
      and base.
- [ ] Any temporary merge-method exception requires Steven's exact separate
      authorization and immediate restoration.
- [ ] Prove K, released source, J, W7, and rolling continuation remain reachable
      from resulting `ripr-swarm/main`.
- [ ] Prove K retains exact ordered parents and `REVIEWED_K_TREE`.
- [ ] Prove live policy is restored.
- [ ] Produce exact post-transport receipt for #3420.

### 17.3 Restore rolling development — #3420

- [ ] Start from exact post-K main.
- [ ] Inventory every release-affected tracked surface and assign retain,
      retire, normalize, regenerate, supersede, external-only, or blocking.
- [ ] Preserve public 0.11.0 history and durable receipts.
- [ ] Select the canonical next development baseline without implying a
      published or committed 0.11.1 patch.
- [ ] Align workspace, crate, editor, locks, changelog, and active version
      authorities.
- [ ] Retire/normalize only one-use tracked release state with exact ownership.
- [ ] Regenerate derived outputs through canonical producers.
- [ ] Restore ordinary swarm product, workflow, runner, and policy authority.
- [ ] Prove source settings/secrets/publication authority are inactive in
      swarm.
- [ ] Run the full rolling matrix and exact-head review.
- [ ] Close #2379 and the source release umbrella only after this receipt is
      terminal.

## 18. Exact-head review and external-review record

For every release-material PR:

- [ ] Review the exact published head, not a predecessor.
- [ ] Read all changed production, test stimulus, test oracle, public contract,
      policy, generated, workflow, and documentation surfaces.
- [ ] Record claim boundary, integration basis, inspected semantic owners,
      blocking findings, challenges/removal experiments, stale/refuted findings,
      non-blocking suggestions, and disposition.
- [ ] Author self-review is a COMMENT inspection record, not an approval.
- [ ] Automated review that is skipped, rate-limited, unavailable, stale, or
      ineligible is missing review.
- [ ] A green status from a bot that did not review is not review evidence.
- [ ] Every substantive thread receives inspect → fix/evidence-reject → reply
      with commit/proof → resolve.
- [ ] No blind bulk resolution.
- [ ] #3807 records the combined independent audit for ranges lacking external
      review.
- [ ] Release copy accurately discloses the external-review gap; it does not
      claim review that did not occur.

## 19. Stop-the-line and invalidation matrix

Stop immediately and retain a failure receipt when any of these occurs:

- [ ] repository, branch, SHA, tree, ref, version, packet, digest, or actor is
      ambiguous;
- [ ] a required packet is missing, stale, partial, zero-subject, or bound to
      another head;
- [ ] a mutable branch/latest/name/path is being substituted for content
      identity;
- [ ] `main` moves after a stage whose contract freezes it;
- [ ] source moves during the P0→J merge hold;
- [ ] candidate artifacts are rebuilt, altered, or selected by display name;
- [ ] workspace/PATH binary wins an installed proof;
- [ ] required Windows/macOS/installed/hosted evidence is skipped or replaced by
      another platform;
- [ ] a consumer strengthens producer evidence or limitation state;
- [ ] an irreversible operation has an ambiguous API response and live state has
      not been reread;
- [ ] a retry could duplicate a release, upload, registry, marketplace, tag, or
      merge action;
- [ ] required review is absent or a substantive thread remains unresolved;
- [ ] a temporary settings/policy exception cannot be restored exactly;
- [ ] a secret, credential, environment, or publication writer is reachable
      before exact authorization;
- [ ] public bytes differ from admitted subjects;
- [ ] cleanup cannot prove owned processes/handles/worktrees are terminal.

Recovery is stage-specific. Never repair a frozen candidate, J, K, protected
history, or public artifact in place when its contract requires a successor or
fix-forward transaction.

## 20. Required receipt index

The final transaction packet must retain at least:

| Receipt | Producer | Primary consumers |
| --- | --- | --- |
| selected-claim and open-PR packet | #2766 | #2768, #1609 |
| schema/producer packet | #3920 | #2766, #2769, source |
| lifecycle-consumer packet | #3924 | #2768, #1609, #2769, P0 |
| pre-cut Windows packet | #3922 | #2766, #1609 |
| Cargo graph and advisory packets | #3787/#3811 | #1609, #2769 |
| editor npm graph packet | #3813 | #1609, #2769, source |
| performance/operability packet | #3794/#3796 | #2766, #2769 |
| agent-rule runtime packet | #3926 | #2766, #2769 |
| behavior selection and judgment | #3805/#3806 | #3804, #2769 |
| installed journey Stage A | #3797 | #2766 |
| pre-cut docs/copy packet | #3921 | #2768, source |
| final adversarial audit | #3807 | #2768, #1609, #2769 |
| final denominator | #2768 | #1609, #2769, P0 |
| candidate ref/manifest/handoff | #1609 | #2769, P0 |
| candidate qualification bundle | #2769 | #2379, P0 |
| source preparation receipts | source owners | P0 |
| P0 source/swarm preflight | ripr#1769 | ripr#1770 |
| P1 resolution manifest/tree | ripr#1770 | ripr#1771/#1772 |
| reviewed-tree qualification | ripr#1771 | ripr#1772 |
| J construction packet | ripr#1772 | ripr#1773 |
| J merge/ancestry/tree/policy packet | ripr#1773 | source tail |
| post-J docs/copy receipt | #3921 | source readiness |
| source release head/readiness | ripr#1466–#1467 | #1578 |
| source qualification and ship packet | #1578/#1469 | RC |
| publication-topology receipt | ripr#1646 | RC/stable |
| RC transaction and public verification | ripr#1634–#1637 | #1470 |
| stable authorization | ripr#1470 | publication leaves |
| stable publication receipt | ripr#1479/#1480 | ripr#1481 |
| final channel/adoption matrix | ripr#1481 | #3419 |
| K candidate/verifier packet | #3419 | #3313 |
| K merge/ancestry/policy packet | #3313 | #3420 |
| rolling restoration receipt | #3420 | #2379 closeout |

Each receipt records its exact invalidators. A consumer may not reconstruct or
reinterpret a missing producer decision.

## Appendix A — current orientation snapshot

Observed on 2026-09-25:

```text
ripr-swarm/main
  106b45ebd13c872d0dfd58cc54976dcd60c6b889
  fix(analysis): handle TypeScript ESM/CJS extensions (#4094)

workspace version
  0.11.0

VS Code extension version
  0.11.0

current phase
  swarm_convergence
```

Visible open PR families include:

- crash/resource and TypeScript honesty/bounds work: #4108, #4107,
  #4098, #4097, #4093, #4056;
- LSP/diff identity work: #4090, #4088, #4087, #4063;
- evidence/admission/output work: #4069, #4068, #4062, #4059, #4058;
- retained historical do-not-merge evidence: #3529.

This list is not the #2766 packet and becomes stale with the next PR or merge.
Regenerate the complete live inventory before every cut decision.

## Appendix B — lane cleanup receipt

For every completed, superseded, or abandoned lane:

- [ ] owned background commands reached a terminal result;
- [ ] no owned Cargo/rustc/Node/VS Code/test process remains;
- [ ] no lock, open handle, temporary server, or child continuation remains;
- [ ] useful logs, fixtures, receipts, and mutation controls are retained;
- [ ] worktree is clean or its exact uncommitted disposition is recorded;
- [ ] worktree is removed;
- [ ] local lane branch is removed when policy permits;
- [ ] remote branch is removed only after merge/retention policy permits;
- [ ] worktree metadata is pruned;
- [ ] lane target/build directories are removed;
- [ ] compiler cache does not reference the deleted lane;
- [ ] scratch repositories and temporary packet files are removed or retained
      under their owning evidence record;
- [ ] primary checkout is clean;
- [ ] `git worktree list` contains only the primary checkout and active lanes;
- [ ] current `main` is fetched and its exact SHA is recorded.

## Appendix C — final completion statement

RIPR 0.11.0 is complete only when all are true:

- [ ] swarm selected scope and final denominator are terminal;
- [ ] one immutable direct-main swarm candidate is qualified;
- [ ] one fresh history-preserving source join is reviewed, qualified, and
      merged;
- [ ] one exact integrated source release head and ship packet are terminal;
- [ ] one public RC is independently verified;
- [ ] stable channels have explicit per-channel authorization;
- [ ] every authorized channel is published and independently verified;
- [ ] exact released source is back-synced through K;
- [ ] rolling swarm development is restored;
- [ ] all transaction identities and receipt digests are retained;
- [ ] #2379 and the source release umbrella close without strengthening any
      missing, deferred, limited, or unproved state.
