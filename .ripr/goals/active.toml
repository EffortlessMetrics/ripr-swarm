# Selected on 2026-06-06 during the queue reconciliation: the use-case
# spec spine (RIPR-SPEC-0065 through RIPR-SPEC-0073) and its
# implementation plan (RIPR-PLAN-0061) route all post-release
# implementation work. Maintainer decision 2026-06-06: the goal points
# at the plan; no implementation slice activates until after the 0.9.0
# release ships and the source back-merge completes.
id = "use-case-spec-spine"
title = "Evidence-to-Repair Use-Case Spec Spine"
status = "active"
lane = "lane1"
issue = "https://github.com/EffortlessMetrics/ripr-swarm/issues/1040"
no_current_goal = false

objective = """
Land the use-case spec spine that makes ripr's surfaces usable,
connected, and non-misleading, then drive implementation PR by PR from
plans/use-case-specs/implementation-plan.md. ripr's product is
evidence-to-repair routing, not raw findings: every surface projects
canonical actionability plus runtime completeness, fails closed into
named limitations, and states its non-claims next to its claims.
"""

end_state = [
  "specs RIPR-SPEC-0065 through RIPR-SPEC-0073 registered and merged",
  "plans/use-case-specs/implementation-plan.md merged and indexed",
  "active goals route through the plan, not a parallel product board",
  "implementation slices resume from the plan after the 0.9.0 release",
]

[[work_item]]
id = "docs/use-case-spec-spine"
status = "done"
branch = "docs/spec-use-case-roadmap"
stackable = true
acceptance = "Specs RIPR-SPEC-0065 (roadmap) through RIPR-SPEC-0073 (receipts/outcomes/route quality) merge via the stacked PR sequence with registry entries in docs/specs/README.md and policy/doc-artifacts.toml, adversarial-review fixes applied, and all spec gates green. Docs-only; no analyzer behavior changes."
specs = [
  "docs/specs/RIPR-SPEC-0065-evidence-to-repair-use-case-roadmap.md",
]
plan = "plans/use-case-specs/implementation-plan.md"
commands = [
  "cargo xtask check-spec-format",
  "cargo xtask check-spec-numbering",
  "cargo xtask check-doc-index",
  "cargo xtask check-doc-artifacts",
  "cargo xtask check-support-tiers",
  "cargo xtask check-static-language",
  "cargo xtask markdown-links",
]

[[work_item]]
id = "goals/route-through-use-case-plan"
status = "done"
branch = "goals/route-use-case-plan"
stackable = true
blocked_by = [
  "docs/use-case-spec-spine",
]
acceptance = "Active goals point at plans/use-case-specs/implementation-plan.md as the single sequencing artifact for post-release implementation. No implementation slice activates in this PR; slice 1 (review file:line hardening, RIPR-SPEC-0068) activates as a deliberate post-release decision after the 0.9.0 publish and source back-merge complete."
specs = [
  "docs/specs/RIPR-SPEC-0065-evidence-to-repair-use-case-roadmap.md",
]
plan = "plans/use-case-specs/implementation-plan.md"
commands = [
  "cargo xtask check-goals",
  "cargo xtask check-doc-artifacts",
  "cargo xtask check-traceability",
]
