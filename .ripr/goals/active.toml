# Compatibility pointer retained during the multi-campaign migration.
#
# This file is intentionally not an execution manifest. It does not select a
# current issue, lane, branch, PR, worktree, or execution wave. The referenced
# campaign is durable context only; live work must be selected from current
# portfolio evidence with an explicit campaign or issue filter.
campaign = ".ripr/goals/campaigns/rust-evidence-bound-repair.toml"
authority = "non-authoritative-compatibility-pointer"
