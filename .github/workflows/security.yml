name: Security

on:
  pull_request:
  push:
    branches: [main, master]

permissions:
  contents: read

jobs:
  cargo-deny:
    name: cargo-deny
    runs-on:
      group: em-ci-tiny
      labels: [self-hosted, Linux, X64, em-ci, trusted-pr, rust, rust-tiny]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: taiki-e/install-action@cargo-deny

      - run: cargo-deny check advisories licenses bans sources

  dependency-review:
    name: dependency-review
    if: github.event_name == 'pull_request'
    runs-on:
      group: em-ci-nano
      labels: [self-hosted, Linux, X64, em-ci, trusted-pr, policy-nano]
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v6

      # TODO: upgrade when GitHub publishes a Node-24-backed Dependency Review
      # release. v4 remains the official hard security gate for dependency diffs.
      - uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
          deny-licenses: GPL-2.0, GPL-3.0, AGPL-1.0, AGPL-3.0
