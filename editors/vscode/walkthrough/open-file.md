# Open a Rust file
Open any Rust file (or an enabled preview-language file) in your workspace.
ripr starts the server, resolves the matching binary if needed, and analyzes
the saved workspace in the background.
