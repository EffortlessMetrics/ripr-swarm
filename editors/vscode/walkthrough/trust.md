# Trust the workspace
ripr only starts its language server in a trusted workspace. If the window is
restricted, grant trust with the **Workspace Trust** button in the status area
or run **Workspaces: Manage Workspace Trust**. ripr starts one server session
automatically when trust is granted.
