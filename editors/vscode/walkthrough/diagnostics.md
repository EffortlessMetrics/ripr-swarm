# Read ripr diagnostics
Watch the status bar: it shows `ripr: queued`, then live counts. Problems
panel entries from ripr carry the source label `ripr` so you can filter them
from rust-analyzer entries. Hover a ripr diagnostic for the missing
discriminator and the evidence route.
