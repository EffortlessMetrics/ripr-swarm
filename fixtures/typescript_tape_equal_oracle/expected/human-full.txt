ripr static RIPR exposure analysis
mode: fast
root: fixtures/typescript_tape_equal_oracle/input

Summary: 1 probe(s), 1 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

Changed file(s) by language: rust: 0, typescript: 1.

WARNING src/score.ts:2

Changed
  after:      return total - penalty + 0;

Probe
  family: return_value
  delta:  value
  owner:  typescript:src/score.ts::score

Language
  language: typescript
  status: preview
  owner kind: function

Preview actionability
  authority: preview_advisory_only
  gap state: already_observed
  category: strong_oracle_observed
  repair packet ready: false
  why not actionable: related TypeScript `t.*` evidence already has a strong exact oracle; no repair packet should be emitted
  repair route: keep the finding advisory preview and verify the existing assertion still targets the changed behavior
  evidence needed: none for a repair packet; retain strong related-test evidence as non-actionable context
  raw evidence: src/score.ts:2 (typescript_preview_probe) source=probe:src_score.ts:typescript_preview:af44236d owner=score

Static exposure
  exposed (warning, confidence 0.60)

Evidence
  - reach yes: 1 related test(s) found for owner `score`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation yes: Strongest extracted oracle kind: `exact_value` (rank 5)
  - discriminator yes: Related test uses a `exact_value` oracle; static evidence suggests the changed behavior is discriminated.
  - local flow reaches returned value: return total - penalty + 0; (line 2)
  - related test tests/score.test.ts:3 scores the difference uses strong exact value oracle: t.equal(...)

Weakness
  - Related test reaches `score` with a `exact_value` oracle. Static evidence suggests the changed behavior is observed under an exact-value or exact-error-variant discriminator.

TypeScript preview card (advisory)
  card version: typescript_preview_card.v1
  authority: preview_advisory_only (typescript/preview)
  owner: score
  owner kind: function
  changed behavior: return_value changed at src/score.ts:2: `return total - penalty + 0;`
  related test: tests/score.test.ts:3 scores the difference
  oracle: exact_value (strong)
  suggested assertion shape: Exact-value evidence is present; verify it targets the changed discriminator.
  repair packet ready: false
  why not actionable: related TypeScript `t.*` evidence already has a strong exact oracle; no repair packet should be emitted
  repair route: keep the finding advisory preview and verify the existing assertion still targets the changed behavior
  limits:
    - Syntax-first TypeScript/JavaScript preview evidence only.
    - No tsc, tsserver, package graph, Jest/Vitest runtime, mutation execution, generated tests, provider calls, source edits, gate authority, or badge authority.
    - This card is advisory and is not a repair packet.

TypeScript repair packet (advisory)
  status: not actionable
  limitation: related TypeScript `t.*` evidence already has a strong exact oracle; no repair packet should be emitted
  next capability needed: none for a repair packet; retain strong related-test evidence as non-actionable context

Next step
  TypeScript preview advisory: changed behavior is observed under a strong oracle; verify the assertion targets the changed boundary value.


Note: 1 Typescript(s) analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
