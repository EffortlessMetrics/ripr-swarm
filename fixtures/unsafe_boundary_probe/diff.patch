diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -2,7 +2,7 @@
-    let mut total: u8 = 0;
+    let mut total: u8 = 1;
     unsafe {
         for offset in 0..len {
-            total = total.wrapping_add(*pointer.add(offset));
+            total = total.wrapping_add(*pointer.add(offset + 1));
         }
     }
     if total == 0 {
@@ -15,3 +15,3 @@
 pub fn read_first(pointer: *const u8) -> u8 {
-    let value = unsafe { *pointer };
+    let value = unsafe { *pointer.add(0) };
     value
 }
