ripr static RIPR exposure analysis
mode: fast
root: fixtures/typescript_disabled/input

Summary: 0 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

Analysis outcome: "no_behavioral_candidates" (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 0 candidate line(s), 0 probe(s), 0 finding(s).

No diff-derived static exposure probes found.

Note: this diff contains 1 Typescript(s). The Typescript adapter is preview and not enabled, so these files were not analyzed — this is NOT a clean Rust-grade result. Enable it in ripr.toml [languages] to analyze them.

To enable, add to ripr.toml:

[languages]
enabled = ["rust", "typescript"]
