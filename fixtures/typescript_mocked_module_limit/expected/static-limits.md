# TypeScript Preview Static Limits

- `mocked_module`: the related test file mocks `./api`.

The editor must show this static limit before suggested action language and
must not parse prose to create new action semantics.
