ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_no_config_repair_gap/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/promotions.py:2

Changed
  after:      if days >= minimum_days:

Probe
  family: predicate
  delta:  control
  owner:  python:src/promotions.py::trial_extension
  canonical gap: gap:python:src/promotions.py:trial_extension:predicate_boundary:predicate:days>=minimum_days

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `trial_extension`
  - infection yes: Changed Python predicate can alter branch selection: `if days >= minimum_days:`
  - propagation weak: Changed Python control flow can propagate by selecting a different branch; preview evidence does not prove the concrete downstream sink.
  - observation weak: Strongest extracted Python oracle kind: `smoke_only` (rank 2)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `days == minimum_days`.
  - local flow reaches unknown sink: if days >= minimum_days: (line 2)
  - related test tests/test_promotions.py:4 test_trial_extension_long_smoke uses smoke smoke only oracle: assert result

Weakness
  - Related Python test reaches `trial_extension` but the strongest extracted oracle is `smoke_only`; add or strengthen a focused assertion for the changed boundary.
  - missing discriminator days == minimum_days: changed Python equality-boundary at line 2 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:src/promotions.py:trial_extension:predicate_boundary:predicate:days>=minimum_days
  authority: preview_advisory_only (python/preview)
  repair action: strengthen_existing_test
  changed owner: trial_extension
  changed behavior: predicate_boundary changed at src/promotions.py:2: `if days >= minimum_days:`
  current test evidence: tests/test_promotions.py:4 test_trial_extension_long_smoke currently has oracle_strength=smoke, oracle_kind=smoke_only: assert result
  missing discriminator: days == minimum_days
  recommended test shape: Strengthen the existing pytest boundary assertion for `days == minimum_days`. Keep the equality case as the minimum repair; optional pytest parameterization can add `minimum_days - 1`, `minimum_days`, and `minimum_days + 1` rows when expected values are clear.
  suggested assertion: Assert the owner result or effect at `days == minimum_days` first. Optional pytest shape: @pytest.mark.parametrize("days, expected", [(minimum_days - 1, ...), (minimum_days, ...), (minimum_days + 1, ...)]); fill expected values from domain behavior only.
  suggested file: tests/test_promotions.py
  suggested test: test_trial_extension_long_smoke
  pytest node: tests/test_promotions.py::test_trial_extension_long_smoke
  verify: pytest tests/test_promotions.py::test_trial_extension_long_smoke (high)
  receipt: unavailable_until_python_gap_ledger
  receipt guidance: Save this `ripr check --format json` report, then run `ripr first-pr --check-output <check.json>` or `ripr reports gap-ledger --check-output <check.json>` to materialize a gap ledger with a concrete receipt command.
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
    - Stop before adding parametrized below/above rows if their expected values are not clear; keep only the equality-boundary assertion.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: strengthen the existing related test with a focused boundary assertion for missing discriminator `days == minimum_days`.
