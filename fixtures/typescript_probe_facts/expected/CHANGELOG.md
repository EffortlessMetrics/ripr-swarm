# Golden Output Changes

## Pending

Reason:
record TypeScript preview probe facts

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
record TypeScript preview probe facts

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
TypeScript computed member calls now carry dynamic_dispatch static-limit metadata while still suppressing discriminator candidates.

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
Keep computed-member probe fixture aligned with TypeScript static-limit repair route wording.

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
project TypeScript preview strict actionability fail-closed metadata

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
project TypeScript preview actionability metadata through check JSON and human output

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
align probe facts fixture with smoke-only advisory wording

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
re-bless TS/JS preview card goldens for additive schema fields (bun_cross_language_grip, missing_graph_legs, leg, sample, unlock_condition) already emitted by merged features and already in policy/output_contracts.txt; pure additions, no removals; fixes #1074 latent drift uncaught because goldens check ran in no required lane

Command:
`cargo xtask goldens bless typescript_probe_facts --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
