--- a/src/pricing.ts
+++ b/src/pricing.ts
@@ -1,6 +1,6 @@
 export function computePrice(base: number, factor: number): number {
-    if (base > 0) {
+    if (base >= 0) {
         return base * factor;
     }
     return 0;
