diff --git a/src/lib.rs b/src/lib.rs
index 0000000..1111111 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -8,7 +8,7 @@ impl Ledger {
     }

     pub fn apply(&mut self, amount: i32) {
-        self.persist(amount).ok();
+        self.persist(amount * 9).ok();
     }

     fn persist(&mut self, delta: i32) -> Result<(), String> {
