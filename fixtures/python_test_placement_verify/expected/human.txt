ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_test_placement_verify/input

Summary: 2 probe(s), 0 exposed, 2 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/pricing.py:2

Changed
  after:      if amount >= threshold:

Probe
  family: predicate
  delta:  control
  owner:  python:src/pricing.py::calculate_discount
  canonical gap: gap:python:src/pricing.py:calculate_discount:predicate_boundary:predicate:amount>=threshold

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `calculate_discount`
  - infection yes: Changed Python predicate can alter branch selection: `if amount >= threshold:`
  - propagation weak: Changed Python control flow can propagate by selecting a different branch; preview evidence does not prove the concrete downstream sink.
  - observation weak: Strongest extracted Python oracle kind: `smoke_only` (rank 2)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `amount == threshold`.
  - local flow reaches unknown sink: if amount >= threshold: (line 2)
  - related test tests/test_pricing.py:4 test_calculate_discount_smoke uses smoke smoke only oracle: assert result

Weakness
  - Related Python test reaches `calculate_discount` but the strongest extracted oracle is `smoke_only`; add or strengthen a focused assertion for the changed boundary.
  - missing discriminator amount == threshold: changed Python equality-boundary at line 2 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:src/pricing.py:calculate_discount:predicate_boundary:predicate:amount>=threshold
  authority: preview_advisory_only (python/preview)
  repair action: strengthen_existing_test
  changed owner: calculate_discount
  changed behavior: predicate_boundary changed at src/pricing.py:2: `if amount >= threshold:`
  current test evidence: tests/test_pricing.py:4 test_calculate_discount_smoke currently has oracle_strength=smoke, oracle_kind=smoke_only: assert result
  missing discriminator: amount == threshold
  recommended test shape: Strengthen the existing pytest boundary assertion for `amount == threshold`.
  suggested assertion: Assert the owner result or effect at the boundary `amount == threshold`.
  suggested file: tests/test_pricing.py
  suggested test: test_calculate_discount_smoke
  pytest node: tests/test_pricing.py::test_calculate_discount_smoke
  verify: pytest tests/test_pricing.py::test_calculate_discount_smoke (high)
  receipt: unavailable_until_python_gap_ledger
  receipt guidance: Save this `ripr check --format json` report, then run `ripr first-pr --check-output <check.json>` or `ripr reports gap-ledger --check-output <check.json>` to materialize a gap ledger with a concrete receipt command.
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: strengthen the existing related test with a focused boundary assertion for missing discriminator `amount == threshold`.

WARNING src/validation.py:3

Changed
  after:          raise ValueError("positive required")

Probe
  family: error_path
  delta:  control
  owner:  python:src/validation.py::require_positive
  canonical gap: gap:python:src/validation.py:require_positive:exception_path:error_path:valueerror_positive_required

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `require_positive`
  - infection yes: Changed Python error path can alter raised exception/control behavior: `raise ValueError("positive required")`
  - propagation yes: Changed Python error path propagates through the exception/control boundary.
  - observation weak: Strongest extracted Python oracle kind: `broad_error` (rank 3)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `raises ValueError matching "positive required"`.
  - local flow reaches error variant: raise ValueError("positive required") (line 3)
  - related test tests/test_validation.py:7 test_rejects_zero_value uses weak broad error oracle: self.assertRaises(ValueError)

Weakness
  - Related Python test reaches `require_positive` but the strongest extracted oracle is `broad_error`; add or strengthen a focused assertion for the exact exception type/message.
  - missing discriminator raises ValueError matching "positive required": changed Python exception at line 3 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:src/validation.py:require_positive:exception_path:error_path:valueerror_positive_required
  authority: preview_advisory_only (python/preview)
  repair action: strengthen_existing_test
  changed owner: require_positive
  changed behavior: exception_path changed at src/validation.py:3: `raise ValueError("positive required")`
  current test evidence: tests/test_validation.py:7 test_rejects_zero_value currently has oracle_strength=weak, oracle_kind=broad_error: self.assertRaises(ValueError)
  missing discriminator: raises ValueError matching "positive required"
  recommended test shape: Strengthen the existing unittest exception assertion for `raises ValueError matching "positive required"`.
  suggested assertion: with self.assertRaisesRegex(ValueError, "positive required"): ...
  suggested file: tests/test_validation.py
  suggested test: test_rejects_zero_value
  verify: python -m unittest tests.test_validation.TestValidation.test_rejects_zero_value (high)
  receipt: unavailable_until_python_gap_ledger
  receipt guidance: Save this `ripr check --format json` report, then run `ripr first-pr --check-output <check.json>` or `ripr reports gap-ledger --check-output <check.json>` to materialize a gap ledger with a concrete receipt command.
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: strengthen the existing related test with an exception assertion for missing discriminator `raises ValueError matching "positive required"`.
