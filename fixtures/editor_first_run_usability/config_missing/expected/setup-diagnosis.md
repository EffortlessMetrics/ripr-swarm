# RIPR setup diagnosis

Status: config missing; built-in Rust defaults are active.

Workspace: fixtures/editor_first_run_usability/config_missing/workspace
Server: ripr 0.5.0
Config: missing.
Artifacts: no saved-workspace artifacts found.

Next safe action: run saved-workspace analysis, or add `ripr.toml` only if default Rust behavior is not intended.

Limits: read-only setup diagnosis only; no source edits, generated tests, provider calls, mutation execution, or gate decision.
