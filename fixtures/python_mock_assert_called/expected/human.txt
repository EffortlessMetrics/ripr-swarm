ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_mock_assert_called/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/notify.py:2

Changed
  after:      callback("receipt.sent", order_id)

Probe
  family: side_effect
  delta:  effect
  owner:  python:src/notify.py::send_receipt
  canonical gap: gap:python:src/notify.py:send_receipt:call_or_output_effect:side_effect:callback_receipt.sent_order_id

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `send_receipt`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation weak: Strongest extracted Python oracle kind: `mock_expectation` (rank 4)
  - discriminator weak: Python preview adapter found no strong discriminator; use `assert ... == ...` or `self.assertEqual(...)` to escalate.
  - related test tests/test_notify.py:6 test_send_receipt_notifies_callback uses medium mock expectation oracle: callback.assert_called_once_with("receipt.sent", "ord-123")

Weakness
  - Related Python test reaches `send_receipt` but the strongest extracted oracle is `mock_expectation`; add or verify an exact-value assertion to make the preview finding stronger.

Next step
  Python preview: add or verify a focused exact-value assertion (`assert ... == ...` or `self.assertEqual(...)`) for the changed behavior.
