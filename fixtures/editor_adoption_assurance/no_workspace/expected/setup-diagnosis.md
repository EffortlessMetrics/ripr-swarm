# RIPR setup diagnosis

Status: no workspace is open; repair actions are suppressed.

Compatibility: server is compatible, but no workspace root is available for root-scoped artifacts.

Workspace: no workspace folder is active.

First PR packet: not projected because there is no workspace root.

Receipt: not projected because there is no workspace root.

Next safe action: open a workspace folder before running the saved-workspace repair loop.

Limits: advisory static projection only; not a gate decision; not runtime proof; no source edits, generated tests, provider calls, or mutation execution.
