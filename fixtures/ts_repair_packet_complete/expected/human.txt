ripr static RIPR exposure analysis
mode: fast
root: fixtures/ts_repair_packet_complete/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

Changed file(s) by language: rust: 0, typescript: 1.

Start here:
  State: preview_limited
  Safe next action: preview-language evidence is advisory; the repair packet is complete but remains advisory, so verify independently before acting.
  File: src/discount.ts:2
  Language: typescript
  Language status: preview
  Static exposure: weakly_exposed (warning, confidence 0.40)
  Changed behavior: if (amount >= threshold) {
  Missing discriminator: Related test reaches `applyDiscount` but the strongest extracted oracle is `relational_check`; upgrade by adding an exact-value (`toBe` / `toEqual` / `toStrictEqual`) assertion or …
  Related test: tests/discount.test.ts:3 applyDiscount applies discount when amount meets threshold
  Next step: TypeScript preview advisory: add or strengthen a focused assertion for missing discriminator `amount == threshold`; the repair packet is complete and delegatable (advisory).
  Evidence:
    - reach yes: 1 related test(s) found for owner `applyDiscount`
    - infection unknown: TypeScript preview adapter does not yet model infection.
    - 4 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain probe:src_discount.ts:typescript_preview:2396aec1
  ripr context --at probe:src_discount.ts:typescript_preview:2396aec1

More:
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json


Note: 1 Typescript(s) analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
