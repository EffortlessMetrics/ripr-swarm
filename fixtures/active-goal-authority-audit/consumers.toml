# Reviewed inventory rules for singleton active-goal consumers. More-specific
# selectors override broader directory selectors. Every discovered path still
# appears as its own row in the generated report.

[[consumer]]
id = "active-manifest"
selector = ".ripr/goals/active.toml"
classification = "live_selection_or_authorization_remove"
owner = "goals-control-plane"
dependent_issue = "#1701"
compatibility_period = "until campaign-record migration is independently demonstrated"
current_behavior = "stores campaign work-item status and ordering used by unqualified readers"
fields = "id,status,branch,lane,work_item.status,blocked_by,stackable"
authority = "can select and block work through singleton status"
target_behavior = "retain only as an explicitly non-authoritative compatibility pointer or remove"
positive_proof = "audit lists the manifest and every reader"
negative_proof = "hidden-singleton fixture remains blocked when active.toml is absent"

[[consumer]]
id = "goal-trackers"
selector = ".ripr/goals/**"
classification = "stable_campaign_definition"
owner = "goals-control-plane"
dependent_issue = "#1701"
compatibility_period = "durable campaign records remain readable"
current_behavior = "records campaign facts and historical work-item status"
fields = "campaign identity, work items, proof commands, dependencies"
authority = "must not grant current mutation or merge authority"
target_behavior = "preserve stable campaign context behind explicit campaign selection"
positive_proof = "explicit campaign drill-down fixture"
negative_proof = "legacy ready status does not make migration ready"

[[consumer]]
id = "xtask-readers"
selector = "xtask/**"
classification = "live_selection_or_authorization_remove"
owner = "xtask-goals"
dependent_issue = "#1701"
compatibility_period = "bounded compatibility reader through migration"
current_behavior = "parses, validates, reports, and generates PR context from the singleton manifest"
fields = "campaign and work-item fields"
authority = "unqualified commands can select, block, or project work"
target_behavior = "require explicit campaign or derived live portfolio input"
positive_proof = "focused parser, report, and command tests"
negative_proof = "unknown reader discovery blocks migration"

[[consumer]]
id = "historical-handoffs"
selector = "docs/handoffs/**"
classification = "historical_campaign_evidence"
owner = "documentation"
dependent_issue = "#1701"
compatibility_period = "indefinite historical readability"
current_behavior = "records prior campaign source paths and closeout state"
fields = "historical active-goal path and completed status"
authority = "read-only history; cannot select current work"
target_behavior = "retain source-linked historical explanation"
positive_proof = "historical-reference fixture is non-blocking"
negative_proof = "historical text never sets mutation authority"

[[consumer]]
id = "governing-docs"
selector = "docs/**"
classification = "stable_campaign_definition"
owner = "documentation"
dependent_issue = "#1701"
compatibility_period = "update atomically with consumer migration"
current_behavior = "documents singleton campaign selection and current-goal recovery"
fields = "active path, selection commands, status semantics"
authority = "guidance may cause agents to select or continue work"
target_behavior = "separate durable campaign context from live selection"
positive_proof = "doc index and active-goal authority report"
negative_proof = "unclassified documentation path blocks migration"

[[consumer]]
id = "plans"
selector = "plans/**"
classification = "stable_campaign_definition"
owner = "planning"
dependent_issue = "#1701"
compatibility_period = "durable plans remain source-linked"
current_behavior = "links implementation sequences to the singleton manifest"
fields = "campaign id, work-item ids, status, successor"
authority = "planning context only after migration"
target_behavior = "reference explicit campaign records without live authorization"
positive_proof = "migration plan maps every slice"
negative_proof = "plan status cannot authorize a writer"

[[consumer]]
id = "policy-and-metrics"
selector = "policy/**"
classification = "compatibility_reader_only"
owner = "repository-policy"
dependent_issue = "#1701"
compatibility_period = "until cargo-allow non-singleton profile is adopted"
current_behavior = "records checks or lanes that reference active-goal state"
fields = "configured paths and proof commands"
authority = "may block validation but cannot select work"
target_behavior = "consume explicit campaign or structural graph inputs"
positive_proof = "policy and command-catalog checks"
negative_proof = "missing configured consumer is reported"

[[consumer]]
id = "cargo-allow-profile"
selector = ".allow/**"
classification = "compatibility_reader_only"
owner = "cargo-allow-adoption"
dependent_issue = "#1701"
compatibility_period = "through upstream #2259/#2299 compatibility"
current_behavior = "configures the RIPR active-goal dialect root"
fields = "goal root and dialect identity"
authority = "structural validation only"
target_behavior = "consume upstream non-singleton structural model"
positive_proof = "cargo-allow advisory audit"
negative_proof = "profile cannot promote legacy ready to live authority"

[[consumer]]
id = "github-declarations"
selector = ".github/**"
classification = "compatibility_reader_only"
owner = "repository-automation"
dependent_issue = "#1701"
compatibility_period = "until templates and source-of-truth workflow migrate"
current_behavior = "templates and workflows name the active-goal contract"
fields = "active path and required planning fields"
authority = "workflow checks may block but do not select work"
target_behavior = "validate explicit campaign records and live evidence separately"
positive_proof = "workflow and template inventory rows"
negative_proof = "workflow reference omitted from inventory blocks migration"

[[consumer]]
id = "root-guidance"
selector = "AGENTS.md"
classification = "live_selection_or_authorization_remove"
owner = "agent-guidance"
dependent_issue = "#1701"
compatibility_period = "update before authority migration"
current_behavior = "instructs agents to recover work from the active goal"
fields = "active campaign and work-item state"
authority = "can direct implementation selection"
target_behavior = "require explicit issue/wave selection from current evidence"
positive_proof = "guidance inventory row"
negative_proof = "legacy status grants no writer authority"

[[consumer]]
id = "root-docs"
selector = "CLAUDE.md"
classification = "live_selection_or_authorization_remove"
owner = "agent-guidance"
dependent_issue = "#1701"
compatibility_period = "update before authority migration"
current_behavior = "mirrors root active-goal guidance"
fields = "active campaign state"
authority = "can direct implementation selection"
target_behavior = "require explicit current issue/wave selection"
positive_proof = "guidance inventory row"
negative_proof = "legacy fields cannot authorize work"

[[consumer]]
id = "changelog-history"
selector = "CHANGELOG.md"
classification = "historical_campaign_evidence"
owner = "release-documentation"
dependent_issue = "#1701"
compatibility_period = "indefinite historical readability"
current_behavior = "records prior active-goal changes"
fields = "historical path and behavior"
authority = "no current authority"
target_behavior = "retain as historical evidence"
positive_proof = "historical classification"
negative_proof = "changelog cannot select work"

[[consumer]]
id = "traceability"
selector = ".ripr/traceability.toml"
classification = "compatibility_reader_only"
owner = "traceability"
dependent_issue = "#1701"
compatibility_period = "until explicit campaign artifacts are registered"
current_behavior = "links specs and plans to active-goal artifacts"
fields = "artifact paths and evidence selectors"
authority = "validation only"
target_behavior = "link durable campaign and audit artifacts"
positive_proof = "check-traceability"
negative_proof = "traceability cannot select work"

[[consumer]]
id = "audit-fixtures"
selector = "fixtures/active-goal-authority-audit/**"
classification = "compatibility_reader_only"
owner = "active-goal-authority-audit"
dependent_issue = "#1697"
compatibility_period = "fixture contract lifetime"
current_behavior = "captures reviewed consumers and negative authority cases"
fields = "selectors, legacy status, historical paths, issue contracts"
authority = "test input only; cannot authorize work"
target_behavior = "remain a stale-detecting audit corpus"
positive_proof = "focused audit fixture tests"
negative_proof = "hidden singleton and legacy ready block readiness"

[[consumer]]
id = "capability-metadata"
selector = "metrics/**"
classification = "derived_current_view"
owner = "capability-ledger"
dependent_issue = "#1701"
compatibility_period = "until capability projections consume explicit campaigns"
current_behavior = "projects next-work metadata referencing active goals"
fields = "next-work and campaign references"
authority = "advisory derived view"
target_behavior = "derive from explicit campaign/live inputs"
positive_proof = "check-capabilities"
negative_proof = "metadata cannot authorize implementation"
