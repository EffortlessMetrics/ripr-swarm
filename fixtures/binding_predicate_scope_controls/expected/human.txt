ripr static RIPR exposure analysis
mode: fast
root: fixtures/binding_predicate_scope_controls/input

Summary: 5 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 5 unknown

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 10 changed line(s), 5 candidate line(s), 5 probe(s), 5 finding(s).

Start here:
  State: static_limited
  Safe next action: inspect the named static limitation before treating this as repair-ready.
  File: fixtures/binding_predicate_scope_controls/input/src/lib.rs:2
  Static exposure: static_unknown (note, confidence 0.20)
  Why static_unknown: the analysis could not determine a static exposure state
  Changed behavior: let end = input.trim_end().len();
  Analyzer limit: No relevant oracle was detected
  Next step: Escalate to real mutation testing or deep static analysis for this probe.
  Evidence:
    - reach no: No static test path found for the changed owner
    - infection unknown: Changed syntax is not mapped to a high-confidence probe family
    - 4 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain --root fixtures/binding_predicate_scope_controls/input --diff fixtures/binding_predicate_scope_controls/diff.patch --mode fast probe:src_lib.rs:static_unknown:c075f160
  ripr context --root fixtures/binding_predicate_scope_controls/input --diff fixtures/binding_predicate_scope_controls/diff.patch --mode fast --at probe:src_lib.rs:static_unknown:c075f160

Hidden:
  4 lower-priority finding(s) omitted from default human output.
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json


Note: ripr found no static test path for any of the 5 changed expression(s) in this diff. Scope
  analyzed: 1 changed Rust file(s), 5 changed expression(s), and 0 statically linked related
  test(s). This is not a coverage assessment. A test may already exercise these changes through
  macros, helper-call chains, or integration tests that ripr's static model does not yet trace; if
  none does, add co-located tests that observe the changed behavior.
