ripr static RIPR exposure analysis
mode: fast
root: fixtures/observation_verified_call_deletion/input

Summary: 2 probe(s), 0 exposed, 2 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING fixtures/observation_verified_call_deletion/input/src/lib.rs:15

Changed
  before: cache.insert("old_key", result);
  after:  cache.insert("result_key", result);

Probe
  family: call_deletion
  delta:  effect
  owner:  src/lib.rs::store_result

Static exposure
  weakly_exposed (warning, confidence 0.92)

Evidence
  - reach yes: Related tests appear to reach store_result: store_result_inserts_result_key
  - infection yes: Reachable tests can plausibly activate this changed behavior
  - propagation yes: Changed behavior appears to influence state write: cache.insert("result_key", result)
  - observation yes: A related test observes a value or effect near the changed behavior
  - discriminator weak: Only relational oracle found; it may not discriminate the changed value exactly
  - local flow reaches state write: cache.insert("result_key", result) (line 15)
  - related test tests/store_tests.rs:4 store_result_inserts_result_key uses weak relational check oracle: assert!(cache.inserted.iter().any(|entry| entry.contains("result_key")));
  - observed function argument value result = 42 at line 6
  - observed assertion argument value "result_key" at line 7

Weakness
  - No strong discriminator was detected

Next step
  Strengthen the related assertion so it discriminates the changed behavior.

WARNING fixtures/observation_verified_call_deletion/input/src/lib.rs:15

Changed
  before: cache.insert("old_key", result);
  after:  cache.insert("result_key", result);

Probe
  family: side_effect
  delta:  effect
  owner:  src/lib.rs::store_result

Static exposure
  weakly_exposed (warning, confidence 0.92)

Evidence
  - reach yes: Related tests appear to reach store_result: store_result_inserts_result_key
  - infection yes: Reachable tests can plausibly activate this changed behavior
  - propagation yes: Changed behavior appears to influence state write: cache.insert("result_key", result)
  - observation yes: A related test observes a value or effect near the changed behavior
  - discriminator weak: Only relational oracle found; it may not discriminate the changed value exactly
  - local flow reaches state write: cache.insert("result_key", result) (line 15)
  - related test tests/store_tests.rs:4 store_result_inserts_result_key uses weak relational check oracle: assert!(cache.inserted.iter().any(|entry| entry.contains("result_key")));
  - observed function argument value result = 42 at line 6
  - observed assertion argument value "result_key" at line 7

Weakness
  - No strong discriminator was detected

Next step
  Add a mock expectation, event receiver assertion, persisted-state check, or metric assertion for the changed effect.
