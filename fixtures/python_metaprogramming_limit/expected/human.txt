ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_metaprogramming_limit/input

Summary: 1 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 1 unknown

NOTE src/models.py:2

Changed
  after:      return type(name, (), {"kind": "preview"})

Probe
  family: return_value
  delta:  value
  owner:  python:src/models.py::build_model

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  static_unknown (note, confidence 0.20)

Evidence
  - reach yes: 1 related Python test(s) found for owner `build_model`
  - infection unknown: Static limit `metaprogramming` prevents a safe Python infection claim.
  - propagation unknown: Static limit `metaprogramming` prevents a safe Python propagation claim.
  - observation yes: Strongest extracted Python oracle kind: `exact_value` (rank 5)
  - discriminator unknown: Static limit `metaprogramming` prevents a safe Python discriminator claim.
  - related test tests/test_models.py:4 test_build_model_sets_name uses strong exact value oracle: assert build_model("Invoice").__name__ == "Invoice"

Weakness
  - Static limit `metaprogramming`: the Python preview adapter saw metaprogramming syntax and does not infer runtime-created behavior.

Stop reasons:
  - static_probe_unknown
