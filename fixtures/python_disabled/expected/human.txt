ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_disabled/input

Summary: 0 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

Analysis outcome: partial_with_limitations (analysis incomplete).
  Zero findings is not a clean result because the analyzed scope is incomplete.
  Limitation: language_adapter_unavailable at language_adapter; affected items: 1; recovery: enable_language — Enable the python preview adapter and re-run the analysis..

No diff-derived static exposure probes found.

Note: this diff contains 1 Python(s). The Python adapter is preview and not enabled, so these files were not analyzed — this is NOT a clean Rust-grade result. Enable it in ripr.toml [languages] to analyze them.

To enable, add to ripr.toml:

[languages]
enabled = ["rust", "python"]
