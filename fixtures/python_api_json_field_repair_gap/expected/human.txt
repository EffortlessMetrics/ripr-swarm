ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_api_json_field_repair_gap/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING app/checkout.py:13

Changed
  after:          return {"detail": "coupon expired"}

Probe
  family: field_construction
  delta:  value
  owner:  python:app/checkout.py::checkout
  canonical gap: gap:python:app/checkout.py:checkout:field_value:field_construction:return_detail_coupon_expired

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `checkout`
  - infection yes: Changed Python field or attribute construction can alter object state: `return {"detail": "coupon expired"}`
  - propagation weak: Changed Python field construction can propagate through returned or retained object state; exact runtime object flow is not resolved.
  - observation weak: Strongest extracted Python oracle kind: `smoke_only` (rank 2)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `response.json()["detail"] == "coupon expired"`.
  - local flow reaches constructed field: return {"detail": "coupon expired"} (line 13)
  - related test tests/test_checkout.py:1 test_expired_coupon_response_smoke uses smoke smoke only oracle: assert response

Weakness
  - Related Python test reaches `checkout` but the strongest extracted oracle is `smoke_only`; add or strengthen a focused assertion for the changed field or object value.
  - missing discriminator response.json()["detail"] == "coupon expired": changed Python field/object value at line 13 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:app/checkout.py:checkout:field_value:field_construction:return_detail_coupon_expired
  authority: preview_advisory_only (python/preview)
  repair action: strengthen_existing_test
  changed owner: checkout
  changed behavior: field_value changed at app/checkout.py:13: `return {"detail": "coupon expired"}`
  current test evidence: tests/test_checkout.py:1 test_expired_coupon_response_smoke currently has oracle_strength=smoke, oracle_kind=smoke_only: assert response
  missing discriminator: response.json()["detail"] == "coupon expired"
  recommended test shape: Strengthen the existing pytest response JSON field assertion for `response.json()["detail"] == "coupon expired"`.
  suggested assertion: Assert the response field directly: `assert response.json()["detail"] == "coupon expired"`.
  suggested file: tests/test_checkout.py
  suggested test: test_expired_coupon_response_smoke
  pytest node: tests/test_checkout.py::test_expired_coupon_response_smoke
  verify: pytest tests/test_checkout.py::test_expired_coupon_response_smoke (high)
  receipt: unavailable_until_python_gap_ledger
  receipt guidance: Save this `ripr check --format json` report, then run `ripr first-pr --check-output <check.json>` or `ripr reports gap-ledger --check-output <check.json>` to materialize a gap ledger with a concrete receipt command.
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: strengthen the existing related test with a field/object assertion for missing discriminator `response.json()["detail"] == "coupon expired"`.
