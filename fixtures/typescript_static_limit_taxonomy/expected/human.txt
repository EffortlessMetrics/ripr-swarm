ripr static RIPR exposure analysis
mode: fast
root: fixtures/typescript_static_limit_taxonomy/input

Summary: 4 probe(s), 0 exposed, 3 weak, 0 unrevealed, 1 no path, 0 unknown

WARNING src/decorated.tsx:7

Changed
  after:          return value.trim();

Probe
  family: return_value
  delta:  value
  owner:  typescript:src/decorated.tsx::save

Language
  language: typescript
  status: preview
  owner kind: method

Static exposure
  no_static_path (warning, confidence 0.40)

Evidence
  - reach no: 0 related test(s) found for owner `save`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation no: Strongest extracted oracle kind: `unknown` (rank 0)
  - discriminator no: TypeScript preview adapter found no strong discriminator; use `toBe` / `toEqual` / `toStrictEqual` to escalate. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - local flow reaches returned value: return value.trim(); (line 7)

Weakness
  - No trusted TypeScript method relation for `save`. Object method calls stay ambiguous in preview until method related-test matching lands.
  - Static limit `decorator_indirection`: owner `save` uses TypeScript decorators; syntax-first preview evidence does not resolve decorator-modified call behavior. Repair route: add decorator-aware owner modeling or verify decorator-modified behavior manually before issuing a repair packet.
  - TypeScript preview actionability `static_limitation` / `decorator_indirection`: static limit `decorator_indirection` prevents bounded TypeScript repair guidance. Repair route: add decorator-aware owner modeling or verify decorator-modified behavior manually before issuing a repair packet

Next step
  TypeScript preview advisory: static limit `decorator_indirection`; Repair route: add decorator-aware owner modeling or verify decorator-modified behavior manually before issuing a repair packet.; no actionable repair packet is emitted.

WARNING src/dispatch.ts:2

Changed
  after:      actions[key]();

Probe
  family: side_effect
  delta:  effect
  owner:  typescript:src/dispatch.ts::dispatch

Language
  language: typescript
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related test(s) found for owner `dispatch`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation weak: Strongest extracted oracle kind: `smoke_only` (rank 2)
  - discriminator weak: TypeScript preview adapter found no strong discriminator; use `toBe` / `toEqual` / `toStrictEqual` to escalate. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - related test tests/dispatch.test.ts:1 dispatch smoke uses smoke smoke only oracle: expect(...).toBeTruthy(...)

Weakness
  - Related test reaches `dispatch` but the strongest extracted oracle is `smoke_only`; upgrade by adding an exact-value (`toBe` / `toEqual` / `toStrictEqual`) assertion. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - Static limit `dynamic_dispatch`: the TypeScript preview adapter saw a computed member call such as `obj[name](...)`; syntax alone cannot resolve the called behavior. Repair route: inspect the concrete dispatch key or add analyzer support for explicit dispatch-map resolution before issuing a repair packet.
  - TypeScript preview actionability `static_limitation` / `dynamic_dispatch`: static limit `dynamic_dispatch` prevents bounded TypeScript repair guidance. Repair route: inspect the concrete dispatch key or add analyzer support for explicit dispatch-map resolution before issuing a repair packet

Next step
  TypeScript preview advisory: static limit `dynamic_dispatch`; Repair route: inspect the concrete dispatch key or add analyzer support for explicit dispatch-map resolution before issuing a repair packet.; no actionable repair packet is emitted.

WARNING src/label.jsx:3

Changed
  after:      return normalizeLabel(props.label);

Probe
  family: return_value
  delta:  value
  owner:  javascript:src/label.jsx::LabelView

Language
  language: javascript
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related test(s) found for owner `LabelView`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation weak: Strongest extracted oracle kind: `smoke_only` (rank 2)
  - discriminator weak: TypeScript preview adapter found no strong discriminator; use `toBe` / `toEqual` / `toStrictEqual` to escalate. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - local flow reaches returned value: return normalizeLabel(props.label); (line 3)
  - related test tests/label.test.jsx:1 LabelView smoke uses smoke smoke only oracle: expect(...).toBeTruthy(...)

Weakness
  - Related test reaches `LabelView` but the strongest extracted oracle is `smoke_only`; upgrade by adding an exact-value (`toBe` / `toEqual` / `toStrictEqual`) assertion. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - Static limit `missing_import_graph`: the changed line calls imported symbol `normalizeLabel` from `./labels`; the TypeScript preview adapter does not build a package or import graph for production implementation semantics. Repair route: add import graph support or inspect the imported implementation before issuing a repair packet.
  - TypeScript preview actionability `static_limitation` / `missing_import_graph`: static limit `missing_import_graph` prevents bounded TypeScript repair guidance. Repair route: add import graph support or inspect the imported implementation before issuing a repair packet

Next step
  TypeScript preview advisory: static limit `missing_import_graph`; Repair route: add import graph support or inspect the imported implementation before issuing a repair packet.; no actionable repair packet is emitted.

WARNING src/proxy.js:2

Changed
  after:      return new Proxy(target, handler);

Probe
  family: return_value
  delta:  value
  owner:  javascript:src/proxy.js::wrap

Language
  language: javascript
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related test(s) found for owner `wrap`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation weak: Strongest extracted oracle kind: `smoke_only` (rank 2)
  - discriminator weak: TypeScript preview adapter found no strong discriminator; use `toBe` / `toEqual` / `toStrictEqual` to escalate. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - local flow reaches returned value: return new Proxy(target, handler); (line 2)
  - related test tests/proxy.test.js:1 wrap smoke uses smoke smoke only oracle: expect(...).toBeTruthy(...)

Weakness
  - Related test reaches `wrap` but the strongest extracted oracle is `smoke_only`; upgrade by adding an exact-value (`toBe` / `toEqual` / `toStrictEqual`) assertion. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - Static limit `metaprogramming`: the TypeScript preview adapter saw Proxy, Reflect, or property-definition metaprogramming syntax and does not infer runtime-created behavior. Repair route: add metaprogramming-aware modeling or keep the finding as human-review-only before issuing a repair packet.
  - TypeScript preview actionability `static_limitation` / `metaprogramming`: static limit `metaprogramming` prevents bounded TypeScript repair guidance. Repair route: add metaprogramming-aware modeling or keep the finding as human-review-only before issuing a repair packet

Next step
  TypeScript preview advisory: static limit `metaprogramming`; Repair route: add metaprogramming-aware modeling or keep the finding as human-review-only before issuing a repair packet.; no actionable repair packet is emitted.
