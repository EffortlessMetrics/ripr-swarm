# Golden Output Changes

## Pending

Reason:
Add TypeScript preview static-limit taxonomy fixture for dynamic dispatch, metaprogramming, decorator indirection, and missing import graph.

Command:
`cargo xtask goldens bless typescript_static_limit_taxonomy --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
Add explicit TypeScript static-limit repair routes to the taxonomy fixture.

Command:
`cargo xtask goldens bless typescript_static_limit_taxonomy --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
Normalize TypeScript static-limit taxonomy fixture diff without trailing whitespace.

Command:
`cargo xtask goldens bless typescript_static_limit_taxonomy --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
project TypeScript preview strict actionability fail-closed metadata

Command:
`cargo xtask goldens bless typescript_static_limit_taxonomy --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
project TypeScript preview actionability metadata through check JSON and human output

Command:
`cargo xtask goldens bless typescript_static_limit_taxonomy --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
align TypeScript smoke-only advisory wording

Command:
`cargo xtask goldens bless typescript_static_limit_taxonomy --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
