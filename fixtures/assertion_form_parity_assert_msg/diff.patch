diff --git a/src/lib.rs b/src/lib.rs
new file mode 100644
--- /dev/null
+++ b/src/lib.rs
@@ -0,0 +1,7 @@
+pub fn discounted_total(amount: i32, discount_threshold: i32) -> i32 {
+    if amount >= discount_threshold {
+        amount - 10
+    } else {
+        amount
+    }
+}
diff --git a/tests/pricing.rs b/tests/pricing.rs
new file mode 100644
--- /dev/null
+++ b/tests/pricing.rs
@@ -0,0 +1,8 @@
+use parity_assert_msg::discounted_total;
+
+#[test]
+fn boundary_matches_expected() {
+    let actual = discounted_total(100, 100);
+    let expected = 90;
+    assert!(actual == expected, "actual={actual:?}");
+}
