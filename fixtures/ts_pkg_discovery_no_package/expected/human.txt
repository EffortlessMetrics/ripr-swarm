ripr static RIPR exposure analysis
mode: fast
root: fixtures/ts_pkg_discovery_no_package/input

Summary: 2 probe(s), 0 exposed, 2 weak, 0 unrevealed, 0 no path, 0 unknown

Changed file(s) by language: rust: 0, typescript: 1.

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 4 changed line(s), 2 candidate line(s), 2 probe(s), 2 finding(s).

Start here:
  State: preview_limited
  Safe next action: preview-language evidence is advisory; complete the missing repair-packet fields before acting.
  File: src/utils.ts:2
  Language: typescript
  Language status: preview
  Static exposure: weakly_exposed (warning, confidence 0.40)
  Why weakly_exposed: a related test reaches this change but does not observe the exact changed value
  Changed behavior: if (value <= min) {
  Missing discriminator: Related tests reach `clamp` with a strong oracle, but no strong assertion observes a `clamp(...)` call at the changed predicate boundary `value == min`; inputs on one side of the b…
  Related test: tests/utils.test.ts:3 clamp below min
  Next step: TypeScript preview advisory: add or strengthen a focused assertion for missing discriminator `value == min`; no actionable repair packet is emitted until verify, receipt, and edit-…
  Evidence:
    - reach yes: 1 related test(s) found for owner `clamp`
    - infection unknown: TypeScript preview adapter does not yet model infection.
    - 4 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain --root fixtures/ts_pkg_discovery_no_package/input --diff fixtures/ts_pkg_discovery_no_package/diff.patch --mode fast probe:src_utils.ts:typescript_preview:a1ef3b36
  ripr context --root fixtures/ts_pkg_discovery_no_package/input --diff fixtures/ts_pkg_discovery_no_package/diff.patch --mode fast --at probe:src_utils.ts:typescript_preview:a1ef3b36

Hidden:
  1 lower-priority finding(s) omitted from default human output.
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json


Note: 1 Typescript(s) analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
