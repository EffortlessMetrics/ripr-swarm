diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -7,6 +7,6 @@ pub enum ParseError {
 pub fn expect_ready(kind: &str, len: usize) -> Result<usize, ParseError> {
     if kind != "ready" {
-        return Err(ParseError::UnexpectedEof);
+        return Err(ParseError::InvalidData);
     }
     Ok(len)
 }
