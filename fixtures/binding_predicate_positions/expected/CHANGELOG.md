# Golden Output Changes

## Pending

Reason:
RIPR-SPEC-0157: new fixture pinning the binding_predicate_positions behavior of the changed-binding predicate relation

Command:
`cargo xtask goldens bless binding_predicate_positions --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
RIPR-SPEC-0157: review hardening — strict direct-operand semantics, both initializers in the relation evidence, compile-true fixture inputs, and the unchanged sibling-function control

Command:
`cargo xtask goldens bless binding_predicate_positions --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
RIPR-SPEC-0157: neutral binding_predicate_value_unresolved disclosure prefix (outside the structured limitation contract) and the else-if-let pattern rebind control

Command:
`cargo xtask goldens bless binding_predicate_positions --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending

Reason:
RIPR-SPEC-0160: the additive git_candidate_subject identity field (null for ordinary runs) in the check JSON identity block

Command:
`cargo xtask goldens bless binding_predicate_positions --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — binding_predicate_positions (2)

Reason:
RIPR-SPEC-0084: CheckInput default base is now None (was origin/main); --diff fixture envelopes honestly omit the inapplicable top-level base and record base_revision null. Only base/base_revision changed; findings, counts, and input_identity byte-identical.

Command:
`cargo xtask goldens bless binding_predicate_positions --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
