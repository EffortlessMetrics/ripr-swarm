diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,4 +1,6 @@
 pub fn one(x: i32) -> i32 {
-    x + 1
+    let y = x + 1;
+    let z = y * 2;
+    z
 }
@@ -5,4 +7,4 @@
 pub fn two(x: i32) -> bool {
-    if x > 0 {
+    if x >= 0 {
         true
     } else {
