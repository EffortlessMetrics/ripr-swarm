ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_mocked_module_limit/input

Summary: 1 probe(s), 1 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

INFO src/service.py:2

Changed
  after:      return client.total_with_tax()

Probe
  family: return_value
  delta:  value
  owner:  python:src/service.py::fetch_total

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  exposed (info, confidence 0.60)

Evidence
  - reach yes: 1 related Python test(s) found for owner `fetch_total`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation yes: Strongest extracted Python oracle kind: `exact_value` (rank 5)
  - discriminator yes: Related Python test uses a `exact_value` oracle; static evidence suggests the changed behavior is discriminated.
  - related test tests/test_service.py:7 test_fetch_total_uses_client_total uses strong exact value oracle: assert fetch_total(client) == 10

Weakness
  - Related Python test reaches `fetch_total` with a `exact_value` oracle. Static evidence suggests the changed behavior is observed under an exact-value discriminator.
  - Static limit `mocked_module`: a related Python test uses patch/mock-module syntax; the preview adapter does not resolve runtime substitution semantics.

Next step
  Python preview: changed behavior is observed under a strong oracle; verify the assertion targets the changed boundary value.
