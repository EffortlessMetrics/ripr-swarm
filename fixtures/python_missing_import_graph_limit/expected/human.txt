ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_missing_import_graph_limit/input

Summary: 1 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 1 unknown

NOTE src/totals.py:4

Changed
  after:      return remote_total(client, include_tax=True)

Probe
  family: return_value
  delta:  value
  owner:  python:src/totals.py::total

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  static_unknown (note, confidence 0.20)

Evidence
  - reach yes: 1 related Python test(s) found for owner `total`
  - infection unknown: Static limit `missing_import_graph` prevents a safe Python infection claim.
  - propagation unknown: Static limit `missing_import_graph` prevents a safe Python propagation claim.
  - observation yes: Strongest extracted Python oracle kind: `exact_value` (rank 5)
  - discriminator unknown: Static limit `missing_import_graph` prevents a safe Python discriminator claim.
  - related test tests/test_totals.py:4 test_total_includes_tax uses strong exact value oracle: assert total(client) == 12

Weakness
  - Static limit `missing_import_graph`: the changed line calls an imported symbol; the Python preview adapter does not build an import graph or resolve imported implementation semantics.

Stop reasons:
  - static_probe_unknown
