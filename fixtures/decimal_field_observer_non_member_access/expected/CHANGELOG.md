# Golden Output Changes

## Pending — decimal_field_observer_non_member_access (1)

Reason:
RIPR-SPEC-0108: pin decimal literals as non-member-access field-oracle controls for #2691

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (2)

Reason:
RIPR-SPEC-0147: publish typed analysis outcome in human and JSON output.

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (3)

Reason:
RIPR-SPEC-0147: align fixture outputs with the typed incomplete-outcome and unquoted human outcome contract.

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (4)

Reason:
RIPR-SPEC-0125: field-value missing discriminator producer added (#1601)

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (5)

Reason:
RIPR-SPEC-0023: classification hint added to digest (#2614)

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (6)

Reason:
RIPR-SPEC-0122: render the missing-discriminator value without restating the label

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (7)

Reason:
RIPR-SPEC-0151: additive per-finding source_currentness disposition (#3280); removed-only Rust probes record the base-side line coordinate

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (8)

Reason:
RIPR-SPEC-0152: preview findings resolve candidate_current from head-side delta evidence; base-deleted findings gain the human revision label and leave actionable projections

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (9)

Reason:
RIPR-SPEC-0152: hidden-count denominator keeps non-current findings; mutation candidates and review-comment authority require candidate_current

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (10)

Reason:
RIPR-SPEC-0160: the additive git_candidate_subject identity field (null for ordinary runs) in the check JSON identity block

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — decimal_field_observer_non_member_access (11)

Reason:
RIPR-SPEC-0001: #3161 PR-B production witness gate upgrades complete direct-flow propagate evidence and fails incomplete flows closed

Command:
`cargo xtask goldens bless decimal_field_observer_non_member_access --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
