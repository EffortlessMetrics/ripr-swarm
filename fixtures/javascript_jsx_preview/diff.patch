diff --git a/src/price-label.jsx b/src/price-label.jsx
index 0000000..1111111 100644
--- a/src/price-label.jsx
+++ b/src/price-label.jsx
@@ -1,5 +1,5 @@
 export function PriceLabel({ amount, threshold }) {
-  if (amount > threshold) {
+  if (amount >= threshold) {
     return <span>{amount - 10}</span>;
   }
   return <span>{amount}</span>;
