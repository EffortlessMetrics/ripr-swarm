ripr static RIPR exposure analysis
mode: fast
root: fixtures/javascript_jsx_preview/input

Summary: 1 probe(s), 1 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

INFO src/price-label.jsx:2

Changed
  after:    if (amount >= threshold) {

Probe
  family: predicate
  delta:  control
  owner:  javascript:src/price-label.jsx::PriceLabel

Language
  language: javascript
  status: preview
  owner kind: component

Static exposure
  exposed (info, confidence 0.60)

Evidence
  - reach yes: 1 related test(s) found for owner `PriceLabel`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation yes: Strongest extracted oracle kind: `exact_value` (rank 5)
  - discriminator yes: Related test uses a `exact_value` oracle; static evidence suggests the changed behavior is discriminated.
  - related test tests/price-label.test.jsx:1 renders discounted price uses strong exact value oracle: expect(...).toEqual(...)

Weakness
  - Related test reaches `PriceLabel` with a `exact_value` oracle. Static evidence suggests the changed behavior is observed under an exact-value or exact-error-variant discriminator.

Next step
  TypeScript preview: changed behavior is observed under a strong oracle; verify the assertion targets the changed boundary value.
