ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_unittest_basic/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/risk.py:2

Changed
  after:      if amount >= 100:

Probe
  family: predicate
  delta:  control
  owner:  python:src/risk.py::risk_score
  canonical gap: gap:python:src/risk.py:risk_score:predicate_boundary:predicate:amount>=100

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `risk_score`
  - infection yes: Changed Python predicate can alter branch selection: `if amount >= 100:`
  - propagation weak: Changed Python control flow can propagate by selecting a different branch; preview evidence does not prove the concrete downstream sink.
  - observation weak: Strongest extracted Python oracle kind: `smoke_only` (rank 2)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `amount == 100`.
  - local flow reaches unknown sink: if amount >= 100: (line 2)
  - related test tests/test_risk.py:7 test_risk_score_high uses smoke smoke only oracle: self.assertTrue(value)

Weakness
  - Related Python test reaches `risk_score` but the strongest extracted oracle is `smoke_only`; add or strengthen a focused assertion for the changed boundary.
  - missing discriminator amount == 100: changed Python equality-boundary at line 2 lacks a concrete repair discriminator

Next step
  Python preview: add a focused boundary assertion for missing discriminator `amount == 100`.
