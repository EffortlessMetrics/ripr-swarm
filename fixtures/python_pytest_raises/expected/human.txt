ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_pytest_raises/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/quantity.py:2

Changed
  after:      if value <= 0:

Probe
  family: predicate
  delta:  control
  owner:  python:src/quantity.py::normalize_quantity

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `normalize_quantity`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation weak: Strongest extracted Python oracle kind: `broad_error` (rank 3)
  - discriminator weak: Python preview adapter found no strong discriminator; use `assert ... == ...` or `self.assertEqual(...)` to escalate.
  - related test tests/test_quantity.py:6 test_rejects_zero_quantity uses weak broad error oracle: pytest.raises(ValueError)

Weakness
  - Related Python test reaches `normalize_quantity` but the strongest extracted oracle is `broad_error`; add or verify an exact-value assertion to make the preview finding stronger.

Next step
  Python preview: add or verify a focused exact-value assertion (`assert ... == ...` or `self.assertEqual(...)`) for the changed behavior.
