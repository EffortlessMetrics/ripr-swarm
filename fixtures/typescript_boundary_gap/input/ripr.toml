[languages]
# Enable the TypeScript preview adapter for this fixture so the adapter
# actually runs against `.ts` changed files. The default repo posture
# stays `["rust"]`; this fixture opts in explicitly per RIPR-SPEC-0026.
enabled = ["rust", "typescript"]
