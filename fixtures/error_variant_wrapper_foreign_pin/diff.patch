diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -36,5 +36,5 @@ pub fn unrelated_check(raw: &str) -> Result<(), OtherError> {
 }

 pub fn parse_summary(raw: &str) -> Result<ParseSummary, Box<dyn Error>> {
-    try_parse_summary(raw).map_err(|error| error.to_string().into())
+    try_parse_summary(raw).map_err(Into::into)
 }
