--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,7 +1,7 @@
 pub fn discounted_total(amount: i32, threshold: i32) -> i32 {
-    if amount > threshold {
+    if amount >= threshold {
         amount - 10
     } else {
         amount
     }
 }
