ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_same_line_duplicate_collapse/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/checkout.py:2

Changed
  after:      return {"status": "paid", "event": "receipt.sent", "id": order_id}

Probe
  family: field_construction
  delta:  value
  owner:  python:src/checkout.py::checkout_payload
  canonical gap: gap:python:src/checkout.py:checkout_payload:field_value:field_construction:return_status_paid_event_receipt.sent_id_order_id

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `checkout_payload`
  - infection yes: Changed Python field or attribute construction can alter object state: `return {"status": "paid", "event": "receipt.sent", "id": order_id}`
  - propagation weak: Changed Python field construction can propagate through returned or retained object state; exact runtime object flow is not resolved.
  - observation weak: Strongest extracted Python oracle kind: `smoke_only` (rank 2)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `status == "paid"`.
  - local flow reaches constructed field: return {"status": "paid", "event": "receipt.sent", "id": order_id} (line 2)
  - related test tests/test_checkout.py:4 test_checkout_payload_smoke uses smoke smoke only oracle: assert payload

Weakness
  - Related Python test reaches `checkout_payload` but the strongest extracted oracle is `smoke_only`; add or strengthen a focused assertion for the changed field or object value.
  - missing discriminator status == "paid": changed Python field/object value at line 2 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:src/checkout.py:checkout_payload:field_value:field_construction:return_status_paid_event_receipt.sent_id_order_id
  authority: preview_advisory_only (python/preview)
  repair action: strengthen_existing_test
  changed owner: checkout_payload
  changed behavior: field_value changed at src/checkout.py:2: `return {"status": "paid", "event": "receipt.sent", "id": order_id}`
  current test evidence: tests/test_checkout.py:4 test_checkout_payload_smoke currently has oracle_strength=smoke, oracle_kind=smoke_only: assert payload
  missing discriminator: status == "paid"
  recommended test shape: Strengthen the existing pytest field/object assertion for `status == "paid"`.
  suggested assertion: Assert the returned object or field satisfies `status == "paid"`.
  suggested file: tests/test_checkout.py
  suggested test: test_checkout_payload_smoke
  pytest node: tests/test_checkout.py::test_checkout_payload_smoke
  verify: pytest tests/test_checkout.py::test_checkout_payload_smoke (high)
  receipt: unavailable_until_python_gap_ledger
  receipt guidance: Save this `ripr check --format json` report, then run `ripr first-pr --check-output <check.json>` or `ripr reports gap-ledger --check-output <check.json>` to materialize a gap ledger with a concrete receipt command.
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: strengthen the existing related test with a field/object assertion for missing discriminator `status == "paid"`.
