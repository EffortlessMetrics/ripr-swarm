ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_unsupported_syntax_limit/input

Summary: 1 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 1 unknown

NOTE src/mapper.py:2

Changed
  after:      return lambda value: value + 2

Probe
  family: return_value
  delta:  value
  owner:  python:src/mapper.py::make_mapper

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  static_unknown (note, confidence 0.20)

Evidence
  - reach yes: 1 related Python test(s) found for owner `make_mapper`
  - infection unknown: Static limit `unsupported_syntax` prevents a safe Python infection claim.
  - propagation unknown: Static limit `unsupported_syntax` prevents a safe Python propagation claim.
  - observation yes: Strongest extracted Python oracle kind: `exact_value` (rank 5)
  - discriminator yes: Related Python test uses a `exact_value` oracle; static evidence suggests the changed behavior is discriminated.
  - related test tests/test_mapper.py:4 test_make_mapper_adds_two uses strong exact value oracle: assert make_mapper()(1) == 3

Weakness
  - Static limit `unsupported_syntax`: the changed line uses a Python syntax shape this preview adapter does not model precisely yet.

Stop reasons:
  - static_probe_unknown
