ripr static RIPR exposure analysis
mode: fast
root: fixtures/wrapper_seam_callee_call_attribution/input

Summary: 2 probe(s), 0 exposed, 2 weak, 0 unrevealed, 0 no path, 0 unknown

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 1 candidate line(s), 2 probe(s), 2 finding(s).

Start here:
  State: top_gap
  Safe next action: inspect or repair the selected non-exposed gap; this is static advisory evidence only.
  File: fixtures/wrapper_seam_callee_call_attribution/input/src/lib.rs:14
  Static exposure: weakly_exposed (warning, confidence 0.66)
  Why weakly_exposed: a related test reaches this change but does not observe the exact changed value
  Changed behavior: try_parse_summary(raw).map_err(|error| error.to_string().into())
  Missing discriminator: Typed static limitation (wrapper_error_binding_unresolved): the wrapper error conversion's variant binding is not statically established
  Related test: tests/attribution.rs:4 observes_callee_outcome
  Next step: Typed static limitation (wrapper_error_binding_unresolved): ripr cannot statically establish that this boxed wrapper conversion carries the callee's error variant, so an existing e…
  Evidence:
    - reach yes: Related tests appear to reach parse_summary: observes_callee_outcome
    - infection yes: Reachable tests can plausibly activate this changed behavior
    - 6 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain --root fixtures/wrapper_seam_callee_call_attribution/input --diff fixtures/wrapper_seam_callee_call_attribution/diff.patch --mode fast probe:src_lib.rs:error_path:463e8297
  ripr context --root fixtures/wrapper_seam_callee_call_attribution/input --diff fixtures/wrapper_seam_callee_call_attribution/diff.patch --mode fast --at probe:src_lib.rs:error_path:463e8297

Hidden:
  1 lower-priority finding(s) omitted from default human output.
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json
