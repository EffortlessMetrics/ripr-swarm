ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_parametrize_basic/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/tax.py:2

Changed
  after:      if amount >= 100:

Probe
  family: predicate
  delta:  control
  owner:  python:src/tax.py::tax_band
  canonical gap: gap:python:src/tax.py:tax_band:predicate_boundary:predicate:amount>=100

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `tax_band`
  - infection yes: Changed Python predicate can alter branch selection: `if amount >= 100:`
  - propagation weak: Changed Python control flow can propagate by selecting a different branch; preview evidence does not prove the concrete downstream sink.
  - observation weak: Strongest extracted Python oracle kind: `unknown` (rank 1)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `amount == 100`.
  - local flow reaches unknown sink: if amount >= 100: (line 2)
  - related test tests/test_tax.py:7 test_tax_band uses unknown unknown oracle: pytest.mark.parametrize

Weakness
  - Related Python test reaches `tax_band` but the strongest extracted oracle is `unknown`; add or strengthen a focused assertion for the changed boundary.
  - missing discriminator amount == 100: changed Python equality-boundary at line 2 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:src/tax.py:tax_band:predicate_boundary:predicate:amount>=100
  authority: preview_advisory_only (python/preview)
  repair action: strengthen_existing_test
  changed owner: tax_band
  changed behavior: predicate_boundary changed at src/tax.py:2: `if amount >= 100:`
  current test evidence: tests/test_tax.py:7 test_tax_band currently has oracle_strength=unknown, oracle_kind=unknown: pytest.mark.parametrize
  missing discriminator: amount == 100
  recommended test shape: Strengthen the existing pytest boundary assertion for `amount == 100`.
  suggested assertion: Assert the owner result or effect at the boundary `amount == 100`.
  suggested file: tests/test_tax.py
  suggested test: test_tax_band
  pytest node: tests/test_tax.py::test_tax_band
  verify: pytest tests/test_tax.py::test_tax_band (high)
  receipt: unavailable_until_python_gap_ledger
  receipt guidance: Save this `ripr check --format json` report, then run `ripr first-pr --check-output <check.json>` or `ripr reports gap-ledger --check-output <check.json>` to materialize a gap ledger with a concrete receipt command.
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: strengthen the existing related test with a focused boundary assertion for missing discriminator `amount == 100`.
