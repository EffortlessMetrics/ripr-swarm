ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_parametrize_basic/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/tax.py:2

Changed
  after:      if amount >= 100:

Probe
  family: predicate
  delta:  control
  owner:  python:src/tax.py::tax_band

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `tax_band`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation weak: Strongest extracted Python oracle kind: `unknown` (rank 1)
  - discriminator weak: Python preview adapter found no strong discriminator; use `assert ... == ...` or `self.assertEqual(...)` to escalate.
  - related test tests/test_tax.py:7 test_tax_band uses unknown unknown oracle: pytest.mark.parametrize

Weakness
  - Related Python test reaches `tax_band` but the strongest extracted oracle is `unknown`; add or verify an exact-value assertion to make the preview finding stronger.

Next step
  Python preview: add or verify a focused exact-value assertion (`assert ... == ...` or `self.assertEqual(...)`) for the changed behavior.
