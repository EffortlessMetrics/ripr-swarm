ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_broad_boolean_assertion/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/priority.py:2

Changed
  after:      return amount >= 100

Probe
  family: return_value
  delta:  value
  owner:  python:src/priority.py::is_priority
  canonical gap: gap:python:src/priority.py:is_priority:return_value:return_value:amount>=100

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `is_priority`
  - infection yes: Changed Python return expression can alter the owner return value: `return amount >= 100`
  - propagation yes: Changed Python return value is already at the owner output boundary.
  - observation weak: Strongest extracted Python oracle kind: `smoke_only` (rank 2)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `return value == amount >= 100`.
  - local flow reaches returned value: return amount >= 100 (line 2)
  - related test tests/test_priority.py:4 test_priority_amount uses smoke smoke only oracle: assert is_priority(100)

Weakness
  - Related Python test reaches `is_priority` but the strongest extracted oracle is `smoke_only`; add or strengthen a focused assertion for the returned value.
  - missing discriminator return value == amount >= 100: changed Python returned-value at line 2 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:src/priority.py:is_priority:return_value:return_value:amount>=100
  authority: preview_advisory_only (python/preview)
  changed owner: is_priority
  changed behavior: return_value changed at src/priority.py:2: `return amount >= 100`
  current test evidence: tests/test_priority.py:4 test_priority_amount currently has oracle_strength=smoke, oracle_kind=smoke_only: assert is_priority(100)
  missing discriminator: return value == amount >= 100
  recommended test shape: Add or strengthen a pytest exact return-value assertion for `return value == amount >= 100`.
  suggested assertion: Assert the returned value equals the expected value for the changed inputs.
  suggested file: tests/test_priority.py
  suggested test: test_is_priority_return_value
  pytest node: tests/test_priority.py::test_is_priority_return_value
  verify: pytest tests/test_priority.py::test_is_priority_return_value (high)
  receipt: unavailable_until_python_gap_ledger
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: add or strengthen an exact return-value assertion for missing discriminator `return value == amount >= 100`.
