ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_mock_interaction_shape/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/callbacks.py:4

Changed
  after:      callback = MagicMock(name="receipt.sent")

Probe
  family: side_effect
  delta:  effect
  owner:  python:src/callbacks.py::recording_callback
  canonical gap: gap:python:src/callbacks.py:recording_callback:call_or_output_effect:side_effect:callback=magicmock_name=receipt.sent

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `recording_callback`
  - infection yes: Changed Python call or output effect can alter observable side effects: `callback = MagicMock(name="receipt.sent")`
  - propagation weak: Changed Python call/output behavior can propagate through side effects; runtime target resolution is not inferred.
  - observation weak: Strongest extracted Python oracle kind: `mock_expectation` (rank 4)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `call includes "receipt.sent"`.
  - local flow reaches call effect: callback = MagicMock(name="receipt.sent") (line 4)
  - related test tests/test_callbacks.py:4 test_recording_callback_starts_idle uses medium mock expectation oracle: callback.assert_not_called()

Weakness
  - Related Python test reaches `recording_callback` but the strongest extracted oracle is `mock_expectation`; add or strengthen a focused assertion for the changed output/log/call effect.
  - missing discriminator call includes "receipt.sent": changed Python output/log/call effect at line 4 lacks a concrete repair discriminator

Next step
  Python preview: add or strengthen an output/log/call-effect assertion for missing discriminator `call includes "receipt.sent"`.
