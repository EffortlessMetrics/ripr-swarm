ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_unittest_oracle_shapes/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/notifications.py:3

Changed
  after:          return "coupon expired"

Probe
  family: return_value
  delta:  value
  owner:  python:src/notifications.py::warn_coupon
  canonical gap: gap:python:src/notifications.py:warn_coupon:return_value:return_value:coupon_expired

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `warn_coupon`
  - infection yes: Changed Python return expression can alter the owner return value: `return "coupon expired"`
  - propagation yes: Changed Python return value is already at the owner output boundary.
  - observation weak: Strongest extracted Python oracle kind: `relational_check` (rank 3)
  - discriminator weak: Python preview adapter found no strong discriminator; missing proof: `return value == "coupon expired"`.
  - local flow reaches returned value: return "coupon expired" (line 3)
  - related test tests/test_notifications.py:7 test_warn_coupon_output_message uses weak relational check oracle: self.assertIn("coupon expired", result.output)

Weakness
  - Related Python test reaches `warn_coupon` but the strongest extracted oracle is `relational_check`; add or strengthen a focused assertion for the returned value.
  - missing discriminator return value == "coupon expired": changed Python returned-value at line 3 lacks a concrete repair discriminator

Python repair card (preview/advisory)
  card version: python_repair_card.v1
  canonical gap: gap:python:src/notifications.py:warn_coupon:return_value:return_value:coupon_expired
  authority: preview_advisory_only (python/preview)
  repair action: strengthen_existing_test
  changed owner: warn_coupon
  changed behavior: return_value changed at src/notifications.py:3: `return "coupon expired"`
  current test evidence: tests/test_notifications.py:7 test_warn_coupon_output_message currently has oracle_strength=weak, oracle_kind=relational_check: self.assertIn("coupon expired", result.output)
  missing discriminator: return value == "coupon expired"
  recommended test shape: Strengthen the existing unittest exact return-value assertion for `return value == "coupon expired"`.
  suggested assertion: Assert the returned value equals the expected value for the changed inputs.
  suggested file: tests/test_notifications.py
  suggested test: test_warn_coupon_output_message
  verify: python -m unittest tests.test_notifications.NotificationTests.test_warn_coupon_output_message (high)
  receipt: unavailable_until_python_gap_ledger
  receipt guidance: Save this `ripr check --format json` report, then run `ripr first-pr --check-output <check.json>` or `ripr reports gap-ledger --check-output <check.json>` to materialize a gap ledger with a concrete receipt command.
  stop conditions:
    - Stop if imports, fixtures, or test setup cannot call the changed owner.
    - Stop if the expected value for the missing discriminator is ambiguous.
    - Stop if adding the test appears to require a production-code edit.
  limits:
    - Syntax-first Python preview evidence only.
    - No source edits, generated tests, mutation execution, provider calls, or gate authority.
    - Verify success alone is not a gap-closure receipt.

Next step
  Python preview: strengthen the existing related test with an exact return-value assertion for missing discriminator `return value == "coupon expired"`.
