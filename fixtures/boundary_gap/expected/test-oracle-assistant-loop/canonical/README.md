# Test-Oracle Assistant Loop Canonical Fixture

This replay corpus pins the Campaign 20 proof loop for the boundary-gap fixture.
It joins one changed predicate seam across review guidance, editor/agent handoff,
before/after static evidence, a local receipt, and PR ledger projection without
adding a report producer yet.

The corpus intentionally preserves the current static movement:

```text
weakly_gripped -> weakly_gripped
```

That is the honest current analyzer output for the checked before/after
snapshots. Later Campaign 20 slices may dogfood a repo-local receipt that moves
static evidence, but this fixture already fixes the proof packet shape and keeps
the advisory/no-edit boundaries visible.

