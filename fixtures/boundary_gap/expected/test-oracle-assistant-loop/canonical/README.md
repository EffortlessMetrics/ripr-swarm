# Test-Oracle Assistant Loop Canonical Fixture

This replay corpus pins the Campaign 20 proof loop for the boundary-gap fixture.
It joins one changed predicate seam across review guidance, editor/agent handoff,
before/after static evidence, a local receipt, and PR ledger projection without
adding a report producer yet.

The corpus preserves the current static movement from the checked before/after
snapshots:

```text
weakly_gripped -> strongly_gripped
```

That is the current analyzer output after the exact equality-boundary test is
added. The fixture pins the proof packet shape and keeps static movement,
focused-test execution, runtime mutation, and the advisory/no-edit boundaries
separate.
