# Missing input case

This case intentionally omits `missing-comments.json`. The checked
`gate-decision` fixtures prove the evaluator writes a config-error report for
missing required PR guidance rather than inventing evidence.
