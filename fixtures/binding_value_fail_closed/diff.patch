diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -2,1 +2,1 @@
-    let end = input.rfind(delim).map_or_else(|| 0, |idx| idx);
+    let end = input.rfind(delim).map_or_else(|| 1, |idx| idx);
@@ -12,1 +12,1 @@
-    let end = input.rfind(delim).map_or(0, |idx| idx);
+    let end = input.rfind(delim).map_or(0, |idx| idx + 1);
@@ -23,1 +23,1 @@
-    let end = input.rfind(needle.as_str()).map_or(0, |idx| idx);
+    let end = input.rfind(needle.as_str()).map_or(1, |idx| idx);
