ripr static RIPR exposure analysis
mode: fast
root: fixtures/ts_runner_detect_ava_script/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/fetch.ts:2

Changed
  after:      return `${base}/${path}/`;

Probe
  family: return_value
  delta:  value
  owner:  typescript:src/fetch.ts::buildUrl

Language
  language: typescript
  status: preview
  owner kind: function

Preview actionability
  authority: preview_advisory_only
  gap state: advisory
  category: incomplete_repair_packet
  repair packet ready: false
  why not actionable: TypeScript preview has owner, related-test, oracle, and probe evidence but lacks a complete repair packet contract
  repair route: project canonical TypeScript repair packet fields only after verify, receipt, evidence refs, and edit boundaries are available
  missing fields: canonical_gap_id, repair_kind, target_test_shape, related_test_or_observer, receipt_command, must_not_change, allowed_edit_surface, raw_evidence_refs
  evidence needed: canonical gap identity, repair kind, target test shape, related observer, verify command, receipt command, raw evidence refs, and edit constraints
  raw evidence: src/fetch.ts:2 (typescript_preview_probe) source=probe:src_fetch.ts:typescript_preview:9c3e9e7e owner=buildUrl

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related test(s) found for owner `buildUrl`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation weak: Strongest extracted oracle kind: `unknown` (rank 0)
  - discriminator weak: TypeScript preview adapter found no strong discriminator; missing proof: `return value == `${base}/${path}/``.
  - local flow reaches returned value: return `${base}/${path}/`; (line 2)
  - related test tests/fetch.test.ts:4 buildUrl joins base and path uses unknown unknown oracle

Weakness
  - Related test reaches `buildUrl` but the strongest extracted oracle is `unknown`; upgrade by adding an exact-value (`toBe` / `toEqual` / `toStrictEqual`) assertion or a `toThrow` form with an exact payload (string, object, or class reference).
  - missing discriminator return value == `${base}/${path}/`: changed TypeScript returned-value at line 2 lacks a concrete preview discriminator

TypeScript preview card (advisory)
  card version: typescript_preview_card.v1
  authority: preview_advisory_only (typescript/preview)
  owner: buildUrl
  owner kind: function
  changed behavior: return_value changed at src/fetch.ts:2: `return `${base}/${path}/`;`
  related test: tests/fetch.test.ts:4 buildUrl joins base and path
  oracle: unknown (unknown)
  missing discriminator: return value == `${base}/${path}/`
  suggested assertion shape: Add an exact return-value assertion for the changed output.
  verify: ava tests/fetch.test.ts
  repair packet ready: false
  why not actionable: TypeScript preview has owner, related-test, oracle, and probe evidence but lacks a complete repair packet contract
  repair route: project canonical TypeScript repair packet fields only after verify, receipt, evidence refs, and edit boundaries are available
  limits:
    - Syntax-first TypeScript/JavaScript preview evidence only.
    - No tsc, tsserver, package graph, Jest/Vitest runtime, mutation execution, generated tests, provider calls, source edits, gate authority, or badge authority.
    - This card is advisory and is not a repair packet.

TypeScript repair packet (advisory)
  status: not actionable
  limitation: TypeScript preview has owner, related-test, oracle, and probe evidence but lacks a complete repair packet contract
  next capability needed: canonical gap identity, repair kind, target test shape, related observer, verify command, receipt command, raw evidence refs, and edit constraints

Next step
  TypeScript preview advisory: add or strengthen a focused assertion for missing discriminator `return value == `${base}/${path}/``; no actionable repair packet is emitted until verify, receipt, and edit-boundary fields are available.


Note: 1 Typescript(s) analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
