ripr static RIPR exposure analysis
mode: fast
root: fixtures/ts_runner_detect_ava_script/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

Changed file(s) by language: rust: 0, typescript: 1.

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 1 candidate line(s), 1 probe(s), 1 finding(s).

Start here:
  State: preview_limited
  Safe next action: preview-language evidence is advisory; the repair packet is complete but remains advisory, so verify independently before acting.
  File: src/fetch.ts:2
  Language: typescript
  Language status: preview
  Static exposure: weakly_exposed (warning, confidence 0.40)
  Why weakly_exposed: a related test reaches this change but does not observe the exact changed value
  Changed behavior: return `${base}/${path}/`;
  Missing discriminator: propagation_unknown: changed value sinks to the owner return value or constructed object (``${base}/${path}/``); all strong assertions observe an unrelated expression, not the chan…
  Related test: tests/fetch.test.ts:4 buildUrl joins base and path
  Next step: TypeScript preview advisory: add or strengthen a focused assertion for missing discriminator `return value == `${base}/${path}/``; the repair packet is complete and delegatable (advisory).
  Evidence:
    - reach yes: 1 related test(s) found for owner `buildUrl`
    - infection unknown: TypeScript preview adapter does not yet model infection.
    - 5 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain --root fixtures/ts_runner_detect_ava_script/input --diff fixtures/ts_runner_detect_ava_script/diff.patch --mode fast probe:src_fetch.ts:typescript_preview:9c3e9e7e
  ripr context --root fixtures/ts_runner_detect_ava_script/input --diff fixtures/ts_runner_detect_ava_script/diff.patch --mode fast --at probe:src_fetch.ts:typescript_preview:9c3e9e7e

More:
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json


Note: 1 TypeScript file analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
