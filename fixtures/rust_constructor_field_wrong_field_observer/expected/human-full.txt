ripr static RIPR exposure analysis
mode: fast
root: fixtures/rust_constructor_field_wrong_field_observer/input

Summary: 1 probe(s), 0 exposed, 0 weak, 0 unrevealed, 1 no path, 0 unknown

WARNING fixtures/rust_constructor_field_wrong_field_observer/input/src/lib.rs:15

Changed
  before: storage: Storage::Local,
  after:  storage,

Probe
  family: return_value
  delta:  value
  owner:  src/lib.rs::lower_statement

Static exposure
  no_static_path (warning, confidence 0.35)

Evidence
  - reach no: No static test path found for the changed owner
  - infection unknown: No reachable tests were found, so infection cannot be established
  - propagation unknown: Propagation is not statically obvious from syntax-first analysis
  - observation no: No reachable test oracle found
  - discriminator no: No assertion can discriminate the changed behavior without a reachable test
  - local flow reaches unknown sink: unknown sink (line 15)

Weakness
  - No relevant oracle was detected
  - No static test path reaches the changed owner
  - No strong discriminator was detected

Stop reasons:
  - transitive_reach_unresolved

Static limitation
  rust_integration_public_api_path_unresolved — An integration test may reach this change through a crate public API or test-helper path ripr cannot fully trace. This is a named limitation, not a coverage claim.

Where to look
  For example, the test `lower_ast_only_checks_name` (tests/lower.rs:4) calls `lower_ast`, an entry point that may lead here. Inspect it to judge whether this change is observed.

Limitation detail
  last established edge: test `lower_ast_only_checks_name` (tests/lower.rs:4) -> entry `lower_ast`
  first unresolved edge: entry `lower_ast` -> owner `lower_statement` through a transitive Rust helper path
  analyzer route: analysis/rust-public-api-transitive-reach
  non-claim: named limitation only; ripr cannot confirm or deny that this path observes the change

Next step
  ripr found no static test path to this change — this is not a coverage assessment. A test may already exercise it through macros, helper-call chains, or integration tests that ripr's static model does not yet trace. If none does, add a co-located test that reaches and observes the changed behavior so a discriminator exists.


Note: ripr found no static test path for any of the 1 changed expression(s) in this diff. Scope analyzed: 1 changed Rust file(s), 1 changed expression(s), and 0 statically linked related test(s). This is not a coverage assessment. A test may already exercise these changes through macros, helper-call chains, or integration tests that ripr's static model does not yet trace; if none does, add co-located tests that observe the changed behavior.
