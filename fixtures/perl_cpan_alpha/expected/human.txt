ripr static RIPR exposure analysis
mode: fast
root: fixtures/perl_cpan_alpha/input

Summary: 0 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

No diff-derived static exposure probes found.

Note: this diff contains 1 Perl file. The Perl adapter is preview and not enabled, so these files were not analyzed — this is NOT a clean Rust-grade result. Enable it in ripr.toml [languages] to analyze them.

To enable, add to ripr.toml:

[languages]
enabled = ["rust", "perl"]
