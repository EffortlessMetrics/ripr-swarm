--- a/lib/Pricing.pm
+++ b/lib/Pricing.pm
@@ -7,6 +7,7 @@
 sub calculate_discount {
     my ($amount) = @_;
+    my $method = 'calculate_discount';
+    return shift->$method();
     if ($amount >= 100) {
