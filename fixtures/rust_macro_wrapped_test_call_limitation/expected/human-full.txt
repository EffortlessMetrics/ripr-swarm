ripr static RIPR exposure analysis
mode: fast
root: fixtures/rust_macro_wrapped_test_call_limitation/input

Summary: 2 probe(s), 0 exposed, 0 weak, 0 unrevealed, 2 no path, 0 unknown

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 1 candidate line(s), 2 probe(s), 2 finding(s).

WARNING fixtures/rust_macro_wrapped_test_call_limitation/input/src/internal.rs:2

Changed
  before: if a > b {
  after:  if a >= b {

Probe
  family: predicate
  delta:  control
  owner:  src/internal.rs::inner

Static exposure
  no_static_path (warning, confidence 0.48)

Evidence
  - reach no: No static test path found for the changed owner
  - infection unknown: No statically reachable test path was found, so activation/infection cannot be estimated
  - propagation yes: Changed behavior appears to influence returned value: a - b
  - observation no: No reachable test oracle found
  - discriminator no: No assertion can discriminate the changed behavior without a reachable test
  - local flow reaches returned value: a - b (line 3)

Weakness
  - No detected boundary input for the changed predicate
  - No relevant oracle was detected
  - No static test path reaches the changed owner
  - No strong discriminator was detected

Stop reasons:
  - macro_reach_unresolved

Static limitation
  rust_macro_wrapped_test_call_unresolved — A test directly invokes a Rust macro whose definition mentions this change, but ripr cannot expand the macro to confirm the path. This is a named limitation, not a coverage claim.

Where to look
  For example, the test `test_macro_returns_difference` (tests/it.rs:2) directly invokes macro `call_inner!` at tests/it.rs:3 whose definition lexically mentions the changed owner name. The macro path may lead here. Inspect it to judge whether this change is observed.

Limitation detail
  last established edge: test `test_macro_returns_difference` (tests/it.rs:2) -> direct macro `call_inner!` at tests/it.rs:3
  first unresolved edge: macro `call_inner!` expansion toward owner `inner`
  analyzer route: analysis/rust-macro-aware-reach
  non-claim: named limitation only; ripr cannot confirm or deny that the macro-generated path observes the change

Next step
  ripr found no static test path to this change — this is not a coverage assessment. A test may already exercise it through macros, helper-call chains, or integration tests that ripr's static model does not yet trace. If none does, add a co-located test that reaches and observes the changed behavior so a discriminator exists.

WARNING fixtures/rust_macro_wrapped_test_call_limitation/input/src/internal.rs:2

Changed
  before: if a > b {
  expr:   if a > b {

Probe
  family: predicate
  delta:  control
  owner:  src/internal.rs::inner

Static exposure
  no_static_path (warning, confidence 0.48)

Evidence
  - reach no: No static test path found for the changed owner
  - infection unknown: No statically reachable test path was found, so activation/infection cannot be estimated
  - propagation yes: Changed behavior appears to influence returned value: a - b
  - observation no: No reachable test oracle found
  - discriminator no: No assertion can discriminate the changed behavior without a reachable test
  - local flow reaches returned value: a - b (line 3)

Weakness
  - No detected boundary input for the changed predicate
  - No relevant oracle was detected
  - No static test path reaches the changed owner
  - No strong discriminator was detected

Stop reasons:
  - macro_reach_unresolved

Static limitation
  rust_macro_wrapped_test_call_unresolved — A test directly invokes a Rust macro whose definition mentions this change, but ripr cannot expand the macro to confirm the path. This is a named limitation, not a coverage claim.

Where to look
  For example, the test `test_macro_returns_difference` (tests/it.rs:2) directly invokes macro `call_inner!` at tests/it.rs:3 whose definition lexically mentions the changed owner name. The macro path may lead here. Inspect it to judge whether this change is observed.

Limitation detail
  last established edge: test `test_macro_returns_difference` (tests/it.rs:2) -> direct macro `call_inner!` at tests/it.rs:3
  first unresolved edge: macro `call_inner!` expansion toward owner `inner`
  analyzer route: analysis/rust-macro-aware-reach
  non-claim: named limitation only; ripr cannot confirm or deny that the macro-generated path observes the change

Next step
  ripr found no static test path to this change — this is not a coverage assessment. A test may already exercise it through macros, helper-call chains, or integration tests that ripr's static model does not yet trace. If none does, add a co-located test that reaches and observes the changed behavior so a discriminator exists.


Note: ripr found no static test path for any of the 2 changed expression(s) in this diff. Scope
  analyzed: 1 changed Rust file(s), 2 changed expression(s), and 0 statically linked related
  test(s). This is not a coverage assessment. A test may already exercise these changes through
  macros, helper-call chains, or integration tests that ripr's static model does not yet trace; if
  none does, add co-located tests that observe the changed behavior.
