diff --git a/src/pricing.ts b/src/pricing.ts
index 0000000..1111111 100644
--- a/src/pricing.ts
+++ b/src/pricing.ts
@@ -1,3 +1,9 @@
+export interface FeeRule {
+  bps: number;
+  cap: number;
+}
+
+export const DEFAULT_FEE_RULE: FeeRule = { bps: 300, cap: 50 };
+
 export function computeFee(amount: number): number {
   return amount * 0.03;
 }
@@ -6,7 +12,7 @@ export function computeFee(amount: number): number {
 
 export function applyDiscount(amount: number, threshold: number): number {
-  if (amount > threshold) {
+  if (amount >= threshold) {
     return amount * 0.9;
   }
   return amount;
