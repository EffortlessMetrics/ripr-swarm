ripr static RIPR exposure analysis
mode: fast
root: fixtures/rust_transitive_reach_test_helper_chain/input

Summary: 2 probe(s), 0 exposed, 0 weak, 0 unrevealed, 2 no path, 0 unknown

WARNING fixtures/rust_transitive_reach_test_helper_chain/input/src/eval.rs:3

Changed
  before: return value > limit;
  after:  return value >= limit;

Probe
  family: predicate
  delta:  control
  owner:  src/eval.rs::matches_greater

Static exposure
  no_static_path (warning, confidence 0.48)

Evidence
  - reach no: No static test path found for the changed owner
  - infection unknown: No tests were found, so activation/infection cannot be estimated
  - propagation yes: Changed behavior appears to influence returned value: value > limit
  - observation no: No reachable test oracle found
  - discriminator no: No assertion can discriminate the changed behavior without a reachable test
  - local flow reaches returned value: value > limit (line 3)

Weakness
  - No detected boundary input for the changed predicate
  - No relevant oracle was detected
  - No static test path reaches the changed owner
  - No strong discriminator was detected

Stop reasons:
  - transitive_reach_unresolved

Static limitation
  rust_transitive_reach_unresolved — A test may reach this change through an internal helper-call chain ripr cannot fully trace (macros, generics, trait dispatch, or depth greater than 3). This is a named limitation, not a coverage claim.

Where to look
  For example, the test `greater_than_patch_examples` (tests/version_req.rs:18) calls `assert_public_api_accepts`, an entry point that may lead here. Inspect it to judge whether this change is observed.

Next step
  ripr found no static test path to this change — this is not a coverage assessment. A test may already exercise it through macros, helper-call chains, or integration tests that ripr's static model does not yet trace. If none does, add a co-located test that reaches and observes the changed behavior so a discriminator exists.

WARNING fixtures/rust_transitive_reach_test_helper_chain/input/src/eval.rs:3

Changed
  before: return value > limit;
  after:  return value >= limit;

Probe
  family: return_value
  delta:  value
  owner:  src/eval.rs::matches_greater

Static exposure
  no_static_path (warning, confidence 0.48)

Evidence
  - reach no: No static test path found for the changed owner
  - infection unknown: No reachable tests were found, so infection cannot be established
  - propagation yes: Changed behavior appears to influence returned value: value > limit
  - observation no: No reachable test oracle found
  - discriminator no: No assertion can discriminate the changed behavior without a reachable test
  - local flow reaches returned value: value > limit (line 3)

Weakness
  - No relevant oracle was detected
  - No static test path reaches the changed owner
  - No strong discriminator was detected

Stop reasons:
  - transitive_reach_unresolved

Static limitation
  rust_transitive_reach_unresolved — A test may reach this change through an internal helper-call chain ripr cannot fully trace (macros, generics, trait dispatch, or depth greater than 3). This is a named limitation, not a coverage claim.

Where to look
  For example, the test `greater_than_patch_examples` (tests/version_req.rs:18) calls `assert_public_api_accepts`, an entry point that may lead here. Inspect it to judge whether this change is observed.

Next step
  ripr found no static test path to this change — this is not a coverage assessment. A test may already exercise it through macros, helper-call chains, or integration tests that ripr's static model does not yet trace. If none does, add a co-located test that reaches and observes the changed behavior so a discriminator exists.


Note: ripr found no static test path for any of the 2 changed expression(s) in this diff. This is not a coverage assessment. A test may already exercise these changes through macros, helper-call chains, or integration tests that ripr's static model does not yet trace; if none does, add co-located tests that observe the changed behavior.
