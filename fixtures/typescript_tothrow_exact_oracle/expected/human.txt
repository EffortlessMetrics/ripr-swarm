ripr static RIPR exposure analysis
mode: fast
root: fixtures/typescript_tothrow_exact_oracle/input

Summary: 1 probe(s), 1 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

INFO src/parser.ts:11

Changed
  after:      if (input.trim() === "") {

Probe
  family: predicate
  delta:  control
  owner:  typescript:src/parser.ts::parseUser

Language
  language: typescript
  status: preview
  owner kind: function

Preview actionability
  authority: preview_advisory_only
  gap state: already_observed
  category: strong_oracle_observed
  repair packet ready: false
  why not actionable: related Jest/Vitest evidence already has a strong exact oracle; no repair packet should be emitted
  repair route: keep the finding advisory preview and verify the existing assertion still targets the changed behavior
  evidence needed: none for a repair packet; retain strong related-test evidence as non-actionable context
  raw evidence: src/parser.ts:11 (typescript_preview_probe) source=probe:src_parser.ts:typescript_preview:c440ffa1 owner=parseUser

Static exposure
  exposed (info, confidence 0.60)

Evidence
  - reach yes: 3 related test(s) found for owner `parseUser`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation yes: Strongest extracted oracle kind: `exact_error_variant` (rank 5)
  - discriminator yes: Related test uses a `exact_error_variant` oracle; static evidence suggests the changed behavior is discriminated.
  - related test tests/parser.test.ts:5 empty user throws exact message uses strong exact error variant oracle: expect(...).toThrow("empty user")
  - related test tests/parser.test.ts:11 empty user throws with error code uses strong exact error variant oracle: expect(...).toThrow({ code: "EMPTY_INPUT" })
  - related test tests/parser.test.ts:17 empty user throws ParseError class uses strong exact error variant oracle: expect(...).toThrow(ParseError)

Weakness
  - Related test reaches `parseUser` with a `exact_error_variant` oracle. Static evidence suggests the changed behavior is observed under an exact-value or exact-error-variant discriminator.

TypeScript preview card (advisory)
  card version: typescript_preview_card.v1
  authority: preview_advisory_only (typescript/preview)
  owner: parseUser
  owner kind: function
  changed behavior: predicate changed at src/parser.ts:11: `if (input.trim() === "") {`
  related test: tests/parser.test.ts:17 empty user throws ParseError class
  oracle: exact_error_variant (strong)
  suggested assertion shape: Exact error payload evidence is present; no repair packet is emitted without strict fields.
  repair packet ready: false
  why not actionable: related Jest/Vitest evidence already has a strong exact oracle; no repair packet should be emitted
  repair route: keep the finding advisory preview and verify the existing assertion still targets the changed behavior
  limits:
    - Syntax-first TypeScript/JavaScript preview evidence only.
    - No tsc, tsserver, package graph, Jest/Vitest runtime, mutation execution, generated tests, provider calls, source edits, gate authority, or badge authority.
    - This card is advisory and is not a repair packet.

TypeScript repair packet (advisory)
  status: not actionable
  limitation: related Jest/Vitest evidence already has a strong exact oracle; no repair packet should be emitted
  next capability needed: none for a repair packet; retain strong related-test evidence as non-actionable context

Next step
  TypeScript preview advisory: changed behavior is observed under a strong oracle; verify the assertion targets the changed boundary value.


Note: 1 Typescript(s) analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
