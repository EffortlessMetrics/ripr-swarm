# Golden Output Changes

## Pending — error_path_sibling_oracle_fake_clean (1)

Reason:
RIPR-SPEC-0107: initial golden for error_path fake-clean repro fixture; error_path with only sibling ExactValue oracle correctly reports weakly_exposed via observation_unverified

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — error_path_sibling_oracle_fake_clean (2)

Reason:
bound default human output to start-here triage; human-full preserves exhaustive evidence

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`

## Pending — error_path_sibling_oracle_fake_clean (3)

Reason:
add human-full golden for exhaustive evidence-promotion projection while default human stays bounded

Command:
cargo xtask goldens check

Updated:
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (4)

Reason:
restrict CallDeletion probes to standalone call statements; refresh affected goldens and record intentional output changes

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (5)

Reason:
#2103: additive changed_files_by_language field and changed_rust_files now Rust-only count

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (6)

Reason:
Issue #2598: default human output now exposes bounded explain and context follow-up commands for the selected finding.

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (7)

Reason:
Issue #2659: finding navigation commands now preserve the analyzed root, diff or artifact scope and shell-safe identity.

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (8)

Reason:
RIPR-SPEC-0147: publish typed analysis outcome in human and JSON output.

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (9)

Reason:
RIPR-SPEC-0147: align fixture outputs with the typed incomplete-outcome and unquoted human outcome contract.

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (10)

Reason:
RIPR-SPEC-0023: classification hint added to digest (#2614)

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (11)

Reason:
RIPR-SPEC-0122: render the missing-discriminator value without restating the label

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (12)

Reason:
RIPR-SPEC-0151: rebless check JSON for the additive source_currentness field; classifications, stages, confidence, counts, and recorded coordinates remain unchanged.

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`

## Pending — error_path_sibling_oracle_fake_clean (13)

Reason:
RIPR-SPEC-0160: the additive git_candidate_subject identity field (null for ordinary runs) in the check JSON identity block

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (14)

Reason:
RIPR-SPEC-0001: #3161 PR-B production witness gate upgrades complete direct-flow propagate evidence and fails incomplete flows closed

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`

## Pending — error_path_sibling_oracle_fake_clean (15)

Reason:
RIPR-SPEC-0107: Err-construction return-value guidance names the missing reaching input and exact error variant instead of the false broad-assertion claim (PR review XQFi)

Command:
`cargo xtask goldens bless error_path_sibling_oracle_fake_clean --reason "..."`

Updated:
- `expected/check.json`
- `expected/human.txt`
- `expected/human-full.txt`
