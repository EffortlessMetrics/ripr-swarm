describe("applyDiscount", () => {
    test("threshold documented elsewhere", () => {
        expect(90).toBe(90);
    });
});

describe("application discounting", () => {
    test("discount boundary", () => {
        expect(90).toBe(90);
    });
});
