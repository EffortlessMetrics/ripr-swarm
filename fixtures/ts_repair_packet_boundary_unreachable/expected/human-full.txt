ripr static RIPR exposure analysis
mode: fast
root: fixtures/ts_repair_packet_boundary_unreachable/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

Changed file(s) by language: rust: 0, typescript: 1.

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 1 candidate line(s), 1 probe(s), 1 finding(s).

WARNING src/auth.ts:2

Changed
  after:    if (user.length >= 3) {

Probe
  family: predicate
  delta:  control
  owner:  typescript:src/auth.ts::login

Language
  language: typescript
  status: preview
  owner kind: function

Preview actionability
  authority: preview_advisory_only
  gap state: advisory
  category: incomplete_repair_packet
  repair packet ready: false
  why not actionable: TypeScript preview has owner, related-test, oracle, and probe evidence but lacks a complete repair packet contract; validator: is not agent-packet eligible: observed call input `login('alice')` does not reach the missing discriminator `user.length == 3`; derive an input that hits the boundary before the packet is delegatable
  repair route: project canonical TypeScript repair packet fields only after verify, receipt, evidence refs, and edit boundaries are available
  missing fields: canonical_gap_id, repair_kind, target_test_shape, related_test_or_observer, receipt_command, must_not_change, allowed_edit_surface, raw_evidence_refs
  evidence needed: canonical gap identity, repair kind, target test shape, related observer, verify command, receipt command, raw evidence refs, and edit constraints
  raw evidence: src/auth.ts:2 (typescript_preview_probe) source=probe:src_auth.ts:typescript_preview:23e9836b owner=login

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related test(s) found for owner `login`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation weak: Strongest extracted oracle kind: `relational_check` (rank 3)
  - discriminator weak: TypeScript preview adapter found no strong discriminator; missing proof: `user.length == 3`.
  - related test tests/auth.test.ts:3 login returns a long session token uses weak relational check oracle: expect(...).toBeGreaterThan(...)

Weakness
  - Related test reaches `login` but the strongest extracted oracle is `relational_check`; upgrade by adding an exact-value (`toBe` / `toEqual` / `toStrictEqual`) assertion or a `toThrow` form with an exact payload (string, object, or class reference).
  - missing discriminator user.length == 3: changed TypeScript equality-boundary at line 2 lacks a concrete preview discriminator

TypeScript preview card (advisory)
  card version: typescript_preview_card.v1
  authority: preview_advisory_only (typescript/preview)
  owner: login
  owner kind: function
  changed behavior: predicate changed at src/auth.ts:2: `if (user.length >= 3) {`
  related test: tests/auth.test.ts:3 login returns a long session token
  oracle: relational_check (weak)
  missing discriminator: user.length == 3
  suggested assertion shape: Add an exact boundary assertion for `user.length == 3`.
  verify: jest tests/auth.test.ts
  repair packet ready: false
  why not actionable: TypeScript preview has owner, related-test, oracle, and probe evidence but lacks a complete repair packet contract; validator: is not agent-packet eligible: observed call input `login('alice')` does not reach the missing discriminator `user.length == 3`; derive an input that hits the boundary before the packet is delegatable
  repair route: project canonical TypeScript repair packet fields only after verify, receipt, evidence refs, and edit boundaries are available
  limits:
    - Syntax-first TypeScript/JavaScript preview evidence only.
    - No tsc, tsserver, package graph, Jest/Vitest runtime, mutation execution, generated tests, provider calls, source edits, gate authority, or badge authority.
    - This card is advisory and is not a repair packet.

TypeScript repair packet (advisory)
  status: not actionable
  limitation: TypeScript preview has owner, related-test, oracle, and probe evidence but lacks a complete repair packet contract
  target shape (not delegatable): login(/* boundary input for user.length == 3 */).toBe(expected)
  next capability needed: canonical gap identity, repair kind, target test shape, related observer, verify command, receipt command, raw evidence refs, and edit constraints

Next step
  TypeScript preview advisory: add or strengthen a focused assertion for missing discriminator `user.length == 3`; no actionable repair packet is emitted until verify, receipt, and edit-boundary fields are available.


Note: 1 TypeScript file analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
