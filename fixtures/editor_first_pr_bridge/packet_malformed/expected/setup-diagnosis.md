# RIPR setup diagnosis

Status: first-pr packet is malformed.

First PR packet: malformed at `target/ripr/first-pr/start-here.json`.

Next safe action: regenerate the first-pr packet before carrying evidence into
PR review.

Limits: read-only editor projection only; no source edits, generated tests,
provider calls, mutation execution, gate decisions, PR comments, or merge
readiness claims.
