ripr static RIPR exposure analysis
mode: fast
root: fixtures/typescript_return_value_shape/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/rounding.ts:2

Changed
  after:      return Math.round(amount * 100) / 100;

Probe
  family: return_value
  delta:  value
  owner:  typescript:src/rounding.ts::roundCents

Language
  language: typescript
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related test(s) found for owner `roundCents`
  - infection unknown: TypeScript preview adapter does not yet model infection.
  - propagation unknown: TypeScript preview adapter does not yet model propagation.
  - observation weak: Strongest extracted oracle kind: `unknown` (rank 1)
  - discriminator weak: TypeScript preview adapter found no strong discriminator; missing proof: `return value == Math.round(amount * 100) / 100`.
  - local flow reaches returned value: return Math.round(amount * 100) / 100; (line 2)
  - related test tests/rounding.test.ts:6 rounds two decimals uses unknown unknown oracle

Weakness
  - Related test reaches `roundCents` but the strongest extracted oracle is `unknown`; upgrade by adding an exact-value (`toBe` / `toEqual` / `toStrictEqual`) assertion. TypeScript `toThrow` forms remain broad error evidence until payload inspection lands.
  - missing discriminator return value == Math.round(amount * 100) / 100: changed TypeScript returned-value at line 2 lacks a concrete preview discriminator

Next step
  TypeScript preview: add or strengthen a focused assertion for missing discriminator `return value == Math.round(amount * 100) / 100`.
