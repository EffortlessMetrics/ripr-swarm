ripr static RIPR exposure analysis
mode: fast
root: fixtures/observation_unverified_side_effect/input

Summary: 2 probe(s), 0 exposed, 2 weak, 0 unrevealed, 0 no path, 0 unknown

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 1 candidate line(s), 2 probe(s), 2 finding(s).

Start here:
  State: top_gap
  Safe next action: inspect or repair the selected non-exposed gap; this is static advisory evidence only.
  File: fixtures/observation_unverified_side_effect/input/src/lib.rs:11
  Static exposure: weakly_exposed (warning, confidence 0.92)
  Changed behavior: notifier.send(order_id)
  Missing discriminator: No strong discriminator was detected
  Related test: tests/process_tests.rs:4 process_order_succeeds
  Next step: Replace broad assertions with exact equality or a property that constrains the changed returned value.
  Evidence:
    - reach yes: Related tests appear to reach process_order: process_order_succeeds
    - infection yes: Reachable tests can plausibly activate this changed behavior
    - 6 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain --root fixtures/observation_unverified_side_effect/input --diff fixtures/observation_unverified_side_effect/diff.patch --mode fast probe:src_lib.rs:return_value:880ed6e6
  ripr context --root fixtures/observation_unverified_side_effect/input --diff fixtures/observation_unverified_side_effect/diff.patch --mode fast --at probe:src_lib.rs:return_value:880ed6e6

Hidden:
  1 lower-priority finding(s) omitted from default human output.
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json
