ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_adversarial_changed_sink_non_delta_operand/input

Summary: 1 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 1 unknown

Changed file(s) by language: python: 1, rust: 0.

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 1 candidate line(s), 1 probe(s), 1 finding(s).

NOTE src/account.py:7

Changed
  after:          return max(0, self._balance)

Probe
  family: return_value
  delta:  value
  owner:  python:src/account.py::Account.balance

Language
  language: python
  status: preview
  owner kind: method

Static exposure
  static_unknown (note, confidence 0.20)

Evidence
  - reach no: 0 related Python test(s) found for owner `balance`
  - infection unknown: Static limit `decorator_indirection` prevents a safe Python infection claim.
  - propagation unknown: Static limit `decorator_indirection` prevents a safe Python propagation claim.
  - observation no: Strongest extracted Python oracle kind: `unknown` (rank 0)
  - discriminator no: Static limit `decorator_indirection` prevents a safe Python discriminator claim.
  - owner: Account.balance
  - owner_kind: method
  - owner_decorators: property
  - static_limit decorator_indirection: `property`

Weakness
  - Static limit `decorator_indirection`: owner `Account.balance` is decorated with `property`; syntax-first preview evidence does not resolve decorator-modified call behavior.

Stop reasons:
  - static_probe_unknown

Static limitation
  decorator_indirection — A decorator wraps the changed owner, so ripr cannot statically confirm a test exercises the underlying behavior.


Note: ripr found no static test path for any of the 1 changed expression(s) in this diff. Scope
  analyzed: 1 changed expression(s) and 0 statically linked related test(s). This is not a coverage
  assessment. A test may already exercise these changes through macros, helper-call chains, or
  integration tests that ripr's static model does not yet trace; if none does, add co-located tests
  that observe the changed behavior.

Note: 1 Python file analyzed under preview support — preview evidence is advisory and may be incomplete. An empty result here is NOT a clean Rust-grade result.
