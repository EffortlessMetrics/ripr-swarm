ripr static RIPR exposure analysis
mode: fast
root: fixtures/weak_error_oracle_assert_matches/input

Summary: 2 probe(s), 0 exposed, 2 weak, 0 unrevealed, 0 no path, 0 unknown

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 2 changed line(s), 1 candidate line(s), 2 probe(s), 2 finding(s).

Start here:
  State: top_gap
  Safe next action: inspect or repair the selected non-exposed gap; this is static advisory evidence only.
  File: fixtures/weak_error_oracle_assert_matches/input/src/lib.rs:7
  Static exposure: weakly_exposed (warning, confidence 0.66)
  Why weakly_exposed: the evidence path is partially complete — see full form for details
  Changed behavior: return Err(AuthError::RevokedToken);
  Related test: tests/errors.rs:10 empty_token_rejects_with_revoked_token
  Next step: Replace broad assertions with exact equality or a property that constrains the changed returned value.
  Evidence:
    - reach yes: Related tests appear to reach authenticate: empty_token_rejects_with_revoked_token
    - infection yes: Reachable tests can plausibly activate this changed behavior
    - 7 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain --root fixtures/weak_error_oracle_assert_matches/input --diff fixtures/weak_error_oracle_assert_matches/diff.patch --mode fast probe:src_lib.rs:return_value:e21dcccc
  ripr context --root fixtures/weak_error_oracle_assert_matches/input --diff fixtures/weak_error_oracle_assert_matches/diff.patch --mode fast --at probe:src_lib.rs:return_value:e21dcccc

Hidden:
  1 lower-priority finding(s) omitted from default human output.
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json
