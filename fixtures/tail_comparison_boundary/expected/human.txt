ripr static RIPR exposure analysis
mode: fast
root: fixtures/tail_comparison_boundary/input

Summary: 4 probe(s), 1 exposed, 3 weak, 0 unrevealed, 0 no path, 0 unknown

Analysis outcome: complete_with_findings (analysis complete).
  Counts: 1 changed file(s), 4 changed line(s), 2 candidate line(s), 4 probe(s), 4 finding(s).

Start here:
  State: top_gap
  Safe next action: inspect or repair the selected non-exposed gap; this is static advisory evidence only.
  File: fixtures/tail_comparison_boundary/input/src/lib.rs:3
  Static exposure: weakly_exposed (warning, confidence 0.92)
  Why weakly_exposed: the evidence path is partially complete — see full form for details
  Changed behavior: items >= 10
  Missing discriminator: items == 10
  Related test: tests/orders.rs:4 big_order_ships_free
  Next step: Add boundary tests for below, equal, and above the changed threshold with exact assertions.
  Evidence:
    - reach yes: Related tests appear to reach ships_free: big_order_ships_free, five_items_earn_a_gift, small_order_pays_shipping
    - infection weak: Related tests contain input values, but the equality-boundary discriminator is missing
    - 16 more evidence line(s) hidden

Next: drill into the top finding:
  ripr explain --root fixtures/tail_comparison_boundary/input --diff fixtures/tail_comparison_boundary/diff.patch --mode fast probe:src_lib.rs:predicate:ca3e18b4
  ripr context --root fixtures/tail_comparison_boundary/input --diff fixtures/tail_comparison_boundary/diff.patch --mode fast --at probe:src_lib.rs:predicate:ca3e18b4

Hidden:
  3 lower-priority finding(s) omitted from default human output.
  Full evidence: rerun with --format human-full
  Machine data: rerun with --format json
